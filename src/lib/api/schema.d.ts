/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/v1/admin/config": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List operator runtime configuration
         * @description Returns the closed configuration catalog together with desired and
         *     effective values. Sensitive settings are redacted. This is an
         *     operator-only route and is not part of the customer API.
         */
        get: operations["listOperatorRuntimeConfig"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/config/{key}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Update an operator runtime setting
         * @description Applies a catalogued setting through the zero-downtime runtime
         *     configuration path. Hot settings return the applied entry; graceful
         *     and rolling settings return a durable operation.
         */
        patch: operations["updateOperatorRuntimeConfig"];
        trace?: never;
    };
    "/v1/admin/config-operations/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read a runtime configuration apply operation
         * @description Polls a durable graceful or rolling configuration apply operation.
         */
        get: operations["getOperatorRuntimeConfigOperation"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/config/{key}/rollback": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Roll back a runtime setting to a previous revision
         * @description Applies a historical value as a new version through the hot-apply path.
         */
        post: operations["rollbackOperatorRuntimeConfig"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/config/{key}/revisions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List runtime configuration revisions
         * @description Read-only append-only version history for one catalogued setting.
         */
        get: operations["listOperatorRuntimeConfigRevisions"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/account": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Whoami.
         * @description Returns the calling account, its plan, and quota limits.
         */
        get: operations["getAccount"];
        put?: never;
        post?: never;
        /**
         * Stage account deletion (30-day grace).
         * @description Stages deletion. The account becomes `deleted_pending` for 30 days
         *     during which the customer can call `POST /v1/account/restore`. After
         *     the grace period, all rows are GC'd.
         */
        delete: operations["deleteAccount"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/account/plan": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Change billing plan.
         * @description Switch the account between `free`, `hobby`, `pro`, and `scale`. The
         *     Stripe subscription is updated server-side; the response carries the
         *     new plan only.
         */
        patch: operations["changePlan"];
        trace?: never;
    };
    "/v1/account/slo": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Account-wide SLO rollup (issue
         * @description Flat scalar SLO rollup for the authenticated account. The
         *     same wire shape as the per-app endpoint without the
         *     `app_id` / `app_slug` fields — the rollup is sum-based
         *     across every app the account owns. `instance_hours` and
         *     `gb_hours` are summed from `usage_minutes` over the
         *     window; the per-app equivalent is the explicit
         *     `/v1/apps/{slug}/slo` endpoint.
         *
         *     `window` is the same closed vocabulary as the per-app
         *     endpoint: `1h` | `24h` (default) | `7d`. Auth chain:
         *     `usage:read` scope + MFA.
         *
         *     On Prometheus failure the endpoint returns 200 with
         *     zeroed fields and `source: "degraded: <reason>"`. When
         *     Postgres is down but the PromQL pass succeeded, only
         *     `instance_hours` / `gb_hours` are zeroed and `source` is
         *     `"degraded: postgres unavailable"`.
         */
        get: operations["getAccountSLO"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/account/export": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Export full account data (GDPR).
         * @description Returns a JSON bundle containing every resource the account owns
         *     (apps, deployments, builds, instances, usage, domains, crons, API
         *     keys, app secrets) plus the GDPR audit trail. Available to
         *     `deleted_pending` accounts so the customer can take a final export
         *     during the 30-day grace window.
         */
        get: operations["exportAccount"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/account/restore": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Restore a `deleted_pending` account.
         * @description Cancels a staged deletion. Returns 409 `account_not_restorable` if the 30-day grace has elapsed.
         */
        post: operations["restoreAccount"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/account/mfa/enroll": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Start MFA enrollment.
         * @description Returns the TOTP secret, QR PNG, otpauth URL, and 10
         *     recovery codes exactly once. The plaintext secret is
         *     shown only here; the server stores a sealed copy. Call
         *     `/v1/account/mfa/confirm` with the customer's first
         *     6-digit code to commit the enrollment.
         */
        post: operations["mfaEnroll"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/account/mfa/confirm": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Finish MFA enrollment with the first TOTP code.
         * @description Verifies the 6-digit code against the sealed secret
         *     and stamps mfa_enrolled_at. Re-issues the session
         *     cookie without the mfa_pending flag.
         */
        post: operations["mfaConfirm"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/account/mfa/verify": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Step up an mfa_pending session.
         * @description For already-enrolled customers whose session cookie is
         *     mfa_pending. Same TOTP check as /confirm, but does NOT
         *     stamp mfa_enrolled_at. Re-issues the cookie without
         *     mfa_pending so subsequent requests pass requireMFA.
         */
        post: operations["mfaVerify"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/account/mfa/recover": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Burn a recovery code to regain access.
         * @description Use when the customer's TOTP device is lost. Removes
         *     one matching SHA-256 hash from the stored set; if the
         *     customer burns the last code, /recover still works but
         *     /disable via recovery_code no longer does.
         */
        post: operations["mfaRecover"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/account/mfa/disable": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Opt out of MFA.
         * @description Clears the TOTP secret, recovery codes, and
         *     mfa_enrolled_at. Body must include exactly one of
         *     `password` (re-verified against account_passwords)
         *     or `recovery_code` (consumed). Leaves mfa_required
         *     untouched so the plan-upgrade / 2nd-deploy
         *     chokepoints can re-arm.
         */
        post: operations["mfaDisable"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/login": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Email + password sign-in.
         * @description Verifies the email + password against the `account_passwords`
         *     row (Argon2id, PHC string). Sets the `faas_sid` session
         *     cookie on success. The response body carries only
         *     `{account_id, plan}` — no API key. The SDK's device-code
         *     flow is the programmatic-auth path; the dashboard cookie is
         *     the only auth artifact on the browser side.
         *
         *     Anti-enumeration: unbound email, wrong password, and
         *     passwordless (OAuth-only) accounts all return 401
         *     `invalid_credentials` with the same body. Every code path
         *     runs one Argon2id verify under identical parameters so the
         *     timing oracle stays closed.
         */
        post: operations["passwordLogin"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/signup": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Create account with email + password.
         * @description Creates an account and signs the caller in. On a colliding
         *     email + matching password, the call is idempotent and signs
         *     in. On a colliding email + different password, the response
         *     is 401 `invalid_credentials` (never 409 — the surface
         *     forbids account enumeration).
         *
         *     Password floor: 12 characters (NIST-style; no complexity
         *     rules).
         */
        post: operations["passwordSignup"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/login/forgot": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Request a password-reset email.
         * @description Always returns 200 with the same body regardless of
         *     whether the email is bound to an account. The reset URL
         *     is mailed via the platform's outbound mailer; the response
         *     never leaks account presence.
         */
        post: operations["passwordForgot"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/signup": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Programmatic signup (JSON-only, bearer-key CLI path).
         * @description Issue #311 / `gregale signup` — JSON-only endpoint that
         *     creates an account (or signs the caller in idempotently),
         *     mints a fresh programmatic API key, and returns the
         *     `ProgrammaticAuthResponse` payload. The CLI persists the
         *     plaintext via `saveToken()` without a dashboard round-trip.
         *
         *     Anti-enumeration posture mirrors `/signup`:
         *       - email unbound: create + set password + mint key.
         *       - email bound + same password: idempotent sign-in + mint key.
         *       - email bound + different password: 401 `invalid_credentials`.
         *     No Set-Cookie header; bearer-key only.
         */
        post: operations["programmaticSignup"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/login": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Programmatic login (JSON-only, bearer-key CLI path).
         * @description Issue #311 / `gregale login` mirror — JSON-only endpoint
         *     that authenticates an email + password and returns a
         *     `ProgrammaticAuthResponse` payload. Same response shape as
         *     `/v1/auth/signup` so the CLI can reuse its unmarshaler.
         *
         *     Anti-enumeration posture mirrors `/login`: Argon2id pad
         *     on the no-row branch, identical 401 on wrong-password vs
         *     unbound email.
         */
        post: operations["programmaticLogin"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/signup/magic-link": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Magic-link signup (JSON-only, no password).
         * @description Issue #311 / `gregale signup --email-only EMAIL` — emails
         *     a one-time signup link to the given address. Always
         *     returns 200 with the same body regardless of whether the
         *     email is bound, unbound, malformed, or missing in the
         *     request — the response cannot be used to enumerate
         *     accounts.
         *
         *     On a real-account hit, the handler creates the account if
         *     unbound, mints a 32-byte token, persists its SHA-256 via
         *     `IssueLoginToken` (15-minute TTL), and emails the
         *     `/auth/verify?token=...` link through the platform mailer.
         */
        post: operations["programmaticSignupMagicLink"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/auth/reset": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Render the password-reset form.
         * @description Validates the token shape; renders the
         *     `password_reset_form.html` template on a valid token.
         *     Invalid / expired / consumed tokens return 410 Gone.
         */
        get: operations["passwordResetForm"];
        put?: never;
        /**
         * Submit a new password against a reset token.
         * @description Atomically consumes the token (one-shot, replay returns
         *     410), Argon2id-encodes the new password, sets it on the
         *     account, and signs the caller in.
         */
        post: operations["passwordResetConfirm"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/google": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * GitHub-style Google OAuth 2.0 consent redirect.
         * @description Sets a 16-byte CSRF state cookie scoped to
         *     `/v1/auth/google/callback` and 302s to the Google consent
         *     screen. The callback consumes the state cookie, exchanges
         *     the code at `oauth2.googleapis.com/token`, fetches the
         *     userinfo, and verifies `email_verified=true` before
         *     minting a session (issue #165 PR #2, ADR-032).
         *
         *     Returns 503 `oauth_provider_unavailable` when the operator
         *     did not configure Google sign-in on this host (both
         *     `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET` unset at
         *     boot, issue #419 / ADR-046). The same shape is returned
         *     for the half-set case, which fails to boot.
         */
        get: operations["googleAuthRedirect"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/google/callback": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Google OAuth 2.0 callback.
         * @description Verifies state, exchanges the code, fetches the profile,
         *     enforces `email_verified=true`, and signs the user in.
         *     Sub-first lookup against `oauth_links` enforces the §11
         *     anti-takeover invariant.
         */
        get: operations["googleAuthCallback"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/github": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * GitHub OAuth 2.0 consent redirect.
         * @description Sets a 16-byte CSRF state cookie scoped to
         *     `/v1/auth/github/callback` and 302s to the GitHub consent
         *     with `scope=read:user user:email`. The callback requires
         *     a primary && verified email before minting a session
         *     (issue #165 PR #2, ADR-032).
         *
         *     Returns 503 `oauth_provider_unavailable` when the operator
         *     did not configure GitHub sign-in on this host (both
         *     `GITHUB_CLIENT_ID` and `GITHUB_CLIENT_SECRET` unset at
         *     boot, issue #419 / ADR-046).
         */
        get: operations["githubAuthRedirect"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/github/callback": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * GitHub OAuth 2.0 callback.
         * @description Verifies state, exchanges the code, fetches `/user` and
         *     `/user/emails`, filters the primary && verified email,
         *     and signs the user in. Sub-first lookup against
         *     `oauth_links` enforces the §11 anti-takeover invariant.
         *
         *     On success, `auth.login` is appended to the events table
         *     (ADR-035) with `method=github`, `email=<verified email>`,
         *     and `login=<GitHub username>` so the audit dashboard and
         *     the corroborating slog line reference one identifier.
         */
        get: operations["githubAuthCallback"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/capabilities": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Sign-in OAuth capability signal for the dashboard.
         * @description Returns the boot-resolved OAuth provider state for this apid
         *     host (issue #419 / ADR-046). The dashboard reads this on
         *     `/login` to decide whether to render the "Sign in with
         *     Google" / "Sign in with GitHub" buttons.
         *
         *     Mounted behind the dashboard's session-cookie auth — a
         *     scanner without a session gets 302 to `/login` first, so
         *     this is not a brute-force amplification surface even though
         *     it surfaces provider enablement. The set of provider names
         *     is closed (`google`, `github`); future providers land as
         *     new keys, not by adding a list.
         *
         *     `enabled=true` means the provider's `/v1/auth/<provider>`
         *     consent route will issue a 302 to the upstream consent
         *     screen on a fresh request. `enabled=false` means it will
         *     return 503 `oauth_provider_unavailable`.
         */
        get: operations["getAuthCapabilities"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/logout": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Log the current dashboard session out.
         * @description Revokes the calling session row in the `sessions` table
         *     (one row per dashboard login; ADR-039 / IAM-3) and
         *     clears the `faas_sid` cookie. Always 204 on success,
         *     even if the row was already revoked (idempotent — the
         *     cookie is cleared either way). Other sessions on the
         *     same account are NOT touched; use
         *     `POST /v1/auth/sessions/revoke_all` for that.
         *
         *     CSRF: action `logout` (verify via the
         *     `faas_csrf` cookie + body `csrf_token` field or
         *     `X-CSRF-Token` header).
         *
         *     Emits `auth.session.revoke` with
         *     `reason: "logout"`.
         */
        post: operations["postAccountLogout"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/csrf": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Issue an action-bound CSRF token for the dashboard.
         * @description Returns a short-lived CSRF token bound to the authenticated
         *     account and the requested browser mutation. The matching
         *     `faas_csrf` cookie is HttpOnly; clients send the returned
         *     `csrf_token` in the mutation's JSON body. This route remains
         *     reachable while the session is `mfa_pending` so the dashboard
         *     can complete MFA enrollment or recovery.
         */
        get: operations["issueBrowserCSRFToken"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/sessions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List active sessions for the calling account.
         * @description Returns one `SessionInfo` row per active login (one row
         *     per dashboard login; ADR-039 / IAM-3). Newest first. The
         *     row whose `id` matches the calling cookie's `sid` is
         *     flagged `current_session: true`. Revoked rows are NOT
         *     returned; the `audit-events` endpoint is the timeline
         *     for those.
         */
        get: operations["getAccountSessions"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/sessions/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Revoke a single session by id.
         * @description Revokes the `sessions` row whose `id` is `{id}` and
         *     whose `account_id` matches the calling envelope. Cross-
         *     account DELETE returns 404 (not 403) — the handler
         *     never confirms a row exists in another account. Revoking
         *     the current session is allowed: the calling cookie is
         *     cleared on the wire (same as `/v1/auth/logout`).
         *
         *     CSRF: action `session_revoke`.
         *
         *     Emits `auth.session.revoke` with
         *     `reason: "explicit"`.
         */
        delete: operations["deleteAccountSession"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/auth/sessions/revoke_all": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Revoke every session except the calling one.
         * @description Revokes all `sessions` rows for the calling account
         *     whose `id` is NOT the calling cookie's `sid`. The
         *     caller's session stays active. Returns the count of
         *     rows revoked so the dashboard can render "Signed out
         *     N other devices".
         *
         *     CSRF: action `sessions_revoke_all`.
         *
         *     Emits `auth.sessions.revoke_all` with
         *     `{revoked_count, retained_sid}`.
         */
        post: operations["postAccountSessionsRevokeAll"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/dashboard/account/set-password": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Set a password on the authenticated account.
         * @description Authenticated. Lets OAuth-only customers opt into password
         *     login. The same Argon2id path as `/auth/reset`, anchored
         *     to the session's account rather than a reset token.
         */
        post: operations["setPassword"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** List apps on the account. */
        get: operations["listApps"];
        put?: never;
        /** Create an app. */
        post: operations["createApp"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /** Fetch one app. */
        get: operations["getApp"];
        put?: never;
        post?: never;
        /** Delete an app. */
        delete: operations["deleteApp"];
        options?: never;
        head?: never;
        /** Partial-update an app. */
        patch: operations["updateApp"];
        trace?: never;
    };
    "/v1/apps/{slug}/metrics": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /**
         * Per-app request metrics (issue
         * @description Time-windowed rollup of one app's gateway activity. The `range`
         *     parameter is a closed vocabulary bounded by Prometheus
         *     retention (`prom_retention_days: 15`):
         *
         *       `5m` (default) | `15m` | `1h` | `6h` | `24h` | `7d` | `15d`
         *
         *     Wake latency (`wake_p95_ms`) is the FLEET p95
         *     (`gateway_wake_latency_seconds` is unlabeled). On Prometheus
         *     failure the endpoint returns 200 with zeroed fields and
         *     `source: "degraded: <reason>"`, matching the public status
         *     page contract.
         */
        get: operations["getAppMetrics"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/routes": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /**
         * Per-route breakdown for opt-in apps (ADR-093).
         * @description Returns the `routes` array of the per-app metrics surface
         *     directly. Reverse-proxies the gatewayd-internal loopback
         *     control listener at `GET /v1/internal/apps/{slug}/routes`.
         *     The array is empty when `route_metrics_enabled` is false
         *     on the app (the gatewayd handler returns 200 + empty
         *     rows rather than 404 — the customer-facing "feature off"
         *     state is not a 404). The route label is method + raw
         *     path (pre-rewrite, ADR-093 D6); the `__route_other__`
         *     bucket surfaces the wildcard-path signal.
         */
        get: operations["getAppRoutes"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/streaming-cap": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /**
         * Per-app streaming classification probe (ADR-102 D6).
         * @description Returns the streaming-status enum (one of `streaming`,
         *     `accept-json-downgrade`, `flag-disabled`, `plan-disallows`,
         *     `operator-disabled`, `upgrade-bypass`) the gatewayd handler
         *     would stamp on the `Streaming-Status` response header for a
         *     representative request to this app, plus the effective
         *     response-body cap (in bytes) and the per-gate flags.
         *
         *     The probe is a pure read against the apid cache (the
         *     per-account `Plan` and the per-app `streaming_enabled`
         *     flag). It does NOT dial gatewayd-internal — the operator
         *     opt-in (`FAAS_GATEWAY_STREAMING` env) and per-edge-rule
         *     cap override are gatewayd-side state, so `effective_cap_bytes`
         *     reflects the plan cap (`cap_kind="plan"`) on every probe.
         *     A customer evaluating "will my next request stream?" must
         *     consider the operator-side flag separately; the canonical
         *     signal is the `Streaming-Status` response header on a real
         *     request, not this probe.
         *
         *     `status=plan-disallows` means the customer's plan tier
         *     forbids `streaming_enabled=true`; the CreateApp gate (D5)
         *     already returns 403 `CodePlanStreamingNotAllowed` so this
         *     row should be unreachable from a properly-validated app,
         *     but the probe still reflects the persisted state for
         *     audits and pinned-SDK migrations.
         */
        get: operations["getAppStreamingCap"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/slo": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /**
         * Per-app customer-facing SLO panel (issue
         * @description Closed-set windowed SLO panel for one app — the
         *     customer-facing equivalent of AWS CloudWatch
         *     per-function / GCP Cloud Run per-service. Distinct from
         *     `GET /v1/apps/{slug}/metrics` (issue #273 / ADR-042) which
         *     is the 5m-window dashboard panel. The /slo surface is the
         *     "yesterday's SLO" / "this week's SLO" summary, with the
         *     customer-facing SLO signals co-located with the
         *     billing-derivable `instance_hours` / `gb_hours` fields.
         *
         *     The `window` parameter is a closed vocabulary, a strict
         *     subset of the /metrics range vocabulary:
         *
         *       `1h` | `24h` (default) | `7d`
         *
         *     `wake_queue_p95_ms` is the FLEET p95
         *     (`gateway_wake_queue_wait_seconds` is unlabeled). On
         *     Prometheus failure the endpoint returns 200 with zeroed
         *     fields and `source: "degraded: <reason>"`, matching the
         *     public status page contract. When Postgres is down but
         *     the PromQL pass succeeded, only `instance_hours` /
         *     `gb_hours` are zeroed and `source` is
         *     `"degraded: postgres unavailable"`.
         */
        get: operations["getAppSLO"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/errors/summary": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /**
         * Per-app customer-facing automatic error grouping summary (ADR-096 / PR-B).
         * @description Sentry-style grouped error view scoped to a customer's
         *     app. One row per `(account_id, app_id, fingerprint)` over
         *     the requested `[since, until]` window, sorted by `count
         *     DESC, last_seen_at DESC, fingerprint ASC`. Distinct from
         *     `GET /v1/apps/{slug}/slo` (issue #696 / ADR-082) which is
         *     the closed-set SLO summary (`1h` / `24h` / `7d`) — the
         *     errors summary uses a continuous `[since, until]` window
         *     with an explicit RFC3339Nano stamp instead.
         *
         *     The window is clamped to `AppErrorsWindowMaxHours` (168h).
         *     When the clamp fires, `window_clamped` is true so the
         *     dashboard can render a "you widened the window past the
         *     cap" tile. The endpoint returns 200 with `items: []`
         *     when no fingerprints are present in the window — never
         *     404. Cross-account slug is a 404 (IDOR-safe; the error
         *     is byte-identical to a real "no such app" 404).
         *
         *     Fingerprints are derived at write time as
         *     `sha256(route_template || "\x1f" || http_status ||
         *     "\x1f" || error_class)`. The route is the matched
         *     template (e.g. `/users/{id}`), NEVER the expanded URL —
         *     this is the load-bearing cardinality fix that keeps the
         *     top-N bounded.
         */
        get: operations["getAppErrorsSummary"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/errors/{fingerprint}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 64-hex-char SHA-256 fingerprint of the error group; sha256(route_template || 0x1f || status || 0x1f || error_class). */
                fingerprint: string;
            };
            cookie?: never;
        };
        /**
         * Per-fingerprint drill-down rows (ADR-096 / PR-B).
         * @description Cursor-paginated drill-down over the request rows that
         *     landed on this fingerprint. Returns 404 when the
         *     fingerprint has been purged by the retention cron or
         *     never existed; the cross-account slug case is also 404
         *     (IDOR-safe byte-identical to a real "no such app" 404).
         */
        get: operations["listAppErrorRequests"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/errors/{fingerprint}/first": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 64-hex-char SHA-256 fingerprint to inspect; the oldest request row for this group is returned with its redacted headers. */
                fingerprint: string;
            };
            cookie?: never;
        };
        /**
         * Single oldest sample row + redacted headers (ADR-096 / PR-B).
         * @description Returns the OLDEST request row for the fingerprint plus
         *     the redacted `headers_sample` (jsonb-decoded) and the
         *     list of `redactions_applied` pattern names so the
         *     dashboard can render a "we redacted X / Y / Z" badge.
         *     Returns 404 when the fingerprint has been purged.
         */
        get: operations["getAppErrorSample"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/deployments": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Create a deployment.
         * @description Two content-types are accepted:
         *     - `application/json` (`CreateDeploymentRequest` with an `image` field): prebuilt OCI reference.
         *     - `multipart/form-data`: source tarball upload (or Dockerfile escape hatch).
         *     Source size is plan-capped (Free/Hobby 100 MB, Pro/Scale 250 MB).
         */
        post: operations["createDeployment"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/deployments/source-ref": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Create a deployment from a Git source-ref (headless).
         * @description Headless deploy path (issue #739 / DEPLOY-PROV-4 / ADR-092).
         *     Resolves the GitHub install bound to the caller's account,
         *     fetches the (repo, ref) tarball via the githubd bridge, spools
         *     it under the per-plan SourceTarballMaxMB cap, validates shape,
         *     and enqueues a build (Kind=DeploymentKindGitHub) pinned to
         *     the resolved 40-char commit SHA.
         *
         *     Designed for CI runners: bearer token only, no GitHub env
         *     vars required. Idempotency-Key collapses concurrent / retried
         *     CI jobs into one build row.
         *
         *     Distinct from the dashboard bind path (`POST
         *     /v1/apps/{slug}/deployments` with a `source` multipart
         *     upload) which goes through the browser + UI bind picker.
         */
        post: operations["createDeploymentFromSourceRef"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/diff": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Read-only preview of what a deploy would change.
         * @description PR-1 of the deploy-diff cluster. Server-side equivalent of
         *     `gregale deploy --diff` — runs the same engine (pkg/deploydiff)
         *     and returns the same wire shape (DiffResponse).
         *
         *     Read-only: no DB writes, no audit row, no deployment row. The
         *     app-not-found case is 200 with `diff.changes[]` containing a
         *     `would_create_app` row + the quota gate firing against the
         *     customer's plan — a CI consumer can preview a fresh deploy the
         *     same way the CLI does.
         *
         *     Auth: bearer-key with `apps:read` (or admin). NO MFA on this
         *     endpoint, mirroring GET /v1/apps/{slug}/metrics.
         *
         *     Schema-break detection is text-only in PR-1 (handler / entrypoint
         *     / env-key changes → warn-severity Breaks). Structural OpenAPI
         *     response-schema diff lands in PR-2.
         */
        post: operations["diffApp"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/rollback": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Roll back to the previous deployment. */
        post: operations["rollbackApp"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/park": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Manually park all running instances. */
        post: operations["parkApp"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/wake": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Manually wake an instance. */
        post: operations["wakeApp"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/account/overage-cap": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Set or clear the account's spend cap (issue
         * @description Sets accounts.overage_cap_cents in integer cents. Body shape:
         *
         *         {"overage_cap_cents": <int|null>}
         *
         *     Pass `null` (or omit the field) to clear the cap (NULL).
         *     Pass 0 to set "no overage allowed." Passing a positive integer
         *     sets the monthly ceiling. The migration CHECK constraint at
         *     `migrations/00054_account_credits.sql` pins
         *     `overage_cap_cents IS NULL OR overage_cap_cents >= 0`; a
         *     negative value is rejected at the apid validator before the
         *     store ever sees it, returning 400 `validation_failed`.
         *
         *     Once current-month overage meets/exceeds the cap, schedd refuses
         *     new wakes with `code: admission_refused` (HTTP 402). The cap is
         *     account-self-scoped (no admin scope required) and the response
         *     is the post-update account state. Audit row
         *     `overage.cap_changed` is emitted on every successful call.
         */
        post: operations["raiseOverageCap"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/rename": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Rename an app. */
        post: operations["renameApp"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/install/repos/list": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * List repos the user's GitHub App installation can see.
         * @description Cookie-session-authenticated (NOT API-key). Hydrates the
         *     dashboard bind picker's repo dropdown. Returns the repos
         *     visible to the user's GitHub App installation — githubd
         *     resolves the per-install token from the session's account.
         *
         *     §11 anti-takeover: the handler re-runs
         *     githubd.VerifyInstallation with the session's github_login
         *     as `expected_login` before listing. Mismatch → 403 forged.
         */
        post: operations["listInstallableRepos"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/install/bind": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Persist the (account, app, installation, repo, branch) bind row.
         * @description Cookie-session-authenticated (NOT API-key). Persists the
         *     GitHub install binding via githubd.BindAppRepo (which
         *     writes through to pkg/state.PgStore.UpsertGithubInstallBinding
         *     per PR-B).
         *
         *     §11 anti-takeover: the handler re-runs
         *     githubd.VerifyInstallation with the session's github_login
         *     as `expected_login` before persisting. Mismatch → 403 forged.
         *     Empty github_login → 403 github_login_required.
         *
         *     On success emits `auth.install.bound` for the audit trail.
         */
        post: operations["bindAppInstall"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/instances": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /** Read-only instance list for an app. */
        get: operations["listInstances"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/logs": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /**
         * Stream app logs (SSE).
         * @description Server-Sent Events stream of instance logs. NOTE: this endpoint is
         *     currently mounted behind `s.authLimited` and is documented here for
         *     reference; the dashboard and CLI also consume it directly.
         *
         *     Two modes share this URL:
         *
         *     - **Live (default)** — `?follow=1` holds the connection open and
         *       streams new entries from the per-instance ring buffer. The
         *       stream terminates with `event: end` when the backstop fires
         *       (10 minutes idle), the schedd returns NotFound (parked app), or
         *       the connection closes.
         *
         *     - **Archive (`?archive=1`)** — fetches a single day's
         *       per-instance log batch from the S3 bucket the apid shipper
         *       writes into. `?instance=<id>` selects the Firecracker instance
         *       id; `?date=YYYY-MM-DD` selects the day. The response is the
         *       same SSE shape as the live stream (`event: log` per line,
         *       `event: end` terminal with `archive_complete` /
         *       `archive_missing` / `archive_degraded` reasons) so the SDK
         *       decoder treats the two paths interchangeably. Archive is
         *       gated by `Plan.LogArchiveEnabled()` — Free customers receive
         *       402 + `plan_log_archive_not_allowed`. The per-plan retention
         *       cap (Hobby 7d / Pro 30d / Scale 90d) refuses `?date=` values
         *       outside the window with 403 + `log_archive_retention_exceeded`.
         */
        get: operations["streamAppLogs"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/secrets": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /** List sealed secrets on an app. */
        get: operations["listSecrets"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/secrets/{key}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description Secret key. Must start with a letter; A-Z, 0-9, underscore. */
                key: components["parameters"]["SecretKey"];
            };
            cookie?: never;
        };
        get?: never;
        /**
         * Set a sealed secret.
         * @description Seals the plaintext value against the host X25519 recipient and
         *     persists the ciphertext. The plaintext never lands in PG.
         */
        put: operations["setSecret"];
        post?: never;
        /** Delete a sealed secret. */
        delete: operations["deleteSecret"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/secrets/{key}/rotate": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description Secret key. Must start with a letter; A-Z, 0-9, underscore. */
                key: components["parameters"]["SecretKey"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Re-seal a sealed secret under the current host identity.
         * @description Re-seals the `(app_id, key)` row under the current host X25519
         *     recipient and stamps the kid column. Emits `secret.rotated`
         *     audit kind when the row already had a value; emits `secret.set`
         *     when the row was previously empty (first-time rotation). The
         *     same byte cap as PUT applies (`SecretValueMaxBytes`).
         */
        post: operations["rotateAppSecret"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/registry-credentials": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /**
         * List sealed private-registry credentials on an app.
         * @description Returns every (registry, username, timestamps) triple on the app.
         *     Plaintext passwords are never returned — imaged transiently
         *     unseals the ciphertext in the pull path. Quota metadata
         *     (`quota_max`, `count`) is included so the CLI can render a
         *     progress bar without a second call.
         */
        get: operations["listRegistryCredentials"];
        /**
         * Set or replace a sealed private-registry credential.
         * @description Seals the plaintext password against the host X25519 recipient
         *     (namespace `"registry_creds"`) and upserts the `(app_id, registry)`
         *     row. The plaintext never lands in PG. Re-PUTs of an existing
         *     `(app, host)` replace the ciphertext and bump `updated_at`
         *     WITHOUT consuming a new quota slot.
         */
        put: operations["setRegistryCredential"];
        post?: never;
        /**
         * Delete a sealed private-registry credential.
         * @description Removes the `(app_id, registry)` row. The registry host is the
         *     URL resource and is supplied via the `?registry=` query param
         *     (URL-encoded). Hosts may carry a `:port` — path segments don't
         *     escape cleanly.
         */
        delete: operations["deleteRegistryCredential"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/security": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Toggle the require_signed flag for an app (admin + MFA).
         * @description Operator-only surface for the per-app cosign signature-enforcement
         *     flag (issue #472 / ADR-054). Mounted with `authLimited → requireMFA →
         *     requireScope(ScopesAdminOnly...)`. The customer PATCH /v1/apps/{slug}
         *     endpoint silently drops the field — flipping it through that surface
         *     is a no-op — so the only path that persists `require_signed=true` is
         *     this one.
         *
         *     `nil` = no field set (no-op 200). Non-nil = atomic overwrite.
         *
         *     Audit event: `app.security_updated` carries old/new values.
         */
        patch: operations["patchAppSecurity"];
        trace?: never;
    };
    "/v1/apps/{slug}/trusted_signers": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /**
         * List the per-app trusted-publisher list (admin).
         * @description Lists every (signer_name, public_key_pem) row for this app
         *     (issue #472 / ADR-054). Admin-scoped; the wire form is the
         *     base64-encoded DER SPKI bytes, NOT a PEM-armoured block.
         *     Empty list is the EXPECTED state for any app with
         *     require_signed=false. Mounted with `authLimited → requireMFA →
         *     requireScope(ScopesAdminOnly...)`.
         */
        get: operations["listAppTrustedSigners"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/trusted_signers/{name}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description Lower-case signer label. Matches the on-disk filename (without .pem) under /etc/faas/secrets/trusted-publishers/. */
                name: string;
            };
            cookie?: never;
        };
        get?: never;
        /**
         * Onboard (or replace) a trusted publisher (admin + MFA).
         * @description Writes the (app_id, signer_name) row with the supplied base64-DER
         *     blob (issue #472 / ADR-054). apid decodes the blob and persists it
         *     verbatim; imaged mirrors the row to `/etc/faas/secrets/trusted-publishers/{name}.pem`
         *     and refreshes its in-memory trust cache on `pg_notify('trusted_signer_changed')`.
         *     PUT semantics: idempotent re-PUT replaces the previous blob.
         *
         *     Audit event: `app.trusted_signer_added`.
         */
        put: operations["putAppTrustedSigner"];
        post?: never;
        /**
         * Offboard a trusted publisher (admin + MFA).
         * @description Deletes the (app_id, signer_name) row. 404 returns the canonical
         *     `trusted_signer_not_found` Problem so `gregale trusted-publishers remove`
         *     can treat absent rows as idempotent success.
         *
         *     Audit event: `app.trusted_signer_removed`.
         */
        delete: operations["deleteAppTrustedSigner"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/env": {
        parameters: {
            query?: {
                /**
                 * @description Env-var scope (ADR-090). A domain-valid slug (3..40 chars,
                 *     lowercase alnum + dash, no leading/trailing dash) — e.g.
                 *     `default`, `staging`, `prod-eu`. Or the reserved sentinel
                 *     `__all__` on GET only, which returns the nested
                 *     `env_by_scope` response shape (every scope on the app).
                 *     Omitted = `scope=default` (pre-PR-B behavior).
                 */
                scope?: components["parameters"]["EnvScope"];
            };
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /**
         * List env vars on an app.
         * @description Returns every env var key + timestamps on the app. The plaintext
         *     value NEVER appears in the response — guest-init reads the value
         *     at process start from `/etc/faas/env.json` inside the guest.
         *
         *     **ADR-090 PR-B scope filter.** The optional `?scope=`
         *     query param selects which scope to read. Omitted = the
         *     default scope (pre-PR-B behavior, byte-identical wire).
         *     `?scope=__all__` returns the nested `env_by_scope` response
         *     shape with every scope on the app; the flat `env` array
         *     is empty in that arm (discriminated union). Any other
         *     `?scope=<slug>` filters to that one scope. Invalid scope
         *     values return 400 `env_scope_invalid`.
         */
        get: operations["listEnv"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/env/{key}": {
        parameters: {
            query?: {
                /**
                 * @description Env-var scope (ADR-090). A domain-valid slug (3..40 chars,
                 *     lowercase alnum + dash, no leading/trailing dash) — e.g.
                 *     `default`, `staging`, `prod-eu`. Or the reserved sentinel
                 *     `__all__` on GET only, which returns the nested
                 *     `env_by_scope` response shape (every scope on the app).
                 *     Omitted = `scope=default` (pre-PR-B behavior).
                 */
                scope?: components["parameters"]["EnvScope"];
            };
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description Secret key. Must start with a letter; A-Z, 0-9, underscore. */
                key: components["parameters"]["SecretKey"];
            };
            cookie?: never;
        };
        get?: never;
        /**
         * Set an env var.
         * @description Persists the plaintext value verbatim in the app_envs table (no
         *     seal step). Env vars are non-sensitive runtime config by contract
         *     — credentials stay on `/v1/apps/{slug}/secrets/{key}`. Applies on
         *     next wake (cold-boot OR snapshot-restore); the running instance
         *     is unaffected.
         *
         *     **ADR-090 PR-B scope filter.** The optional `?scope=`
         *     query param selects which scope to write. Omitted = the
         *     default scope (pre-PR-B behavior). The reserved sentinel
         *     `__all__` is rejected with 400 `env_scope_reserved` on
         *     writes (it has no meaning on a single-row write). Invalid
         *     scope shapes return 400 `env_scope_invalid`.
         */
        put: operations["setEnv"];
        post?: never;
        /**
         * Delete an env var.
         * @description Removes the (app_id, scope, key) row. `?scope=` selects
         *     which scope; omitted = the default scope. `?scope=__all__`
         *     is rejected (400 `env_scope_reserved`) — same reason as
         *     on PUT: the sentinel has no meaning on a single-row
         *     delete. Returns 400 `env_var_not_found` (not 404) when
         *     no row matches — the URL resource is the env-var, not
         *     the app.
         */
        delete: operations["deleteEnv"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/upstreams": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /**
         * List data upstreams on an app.
         * @description Returns every captured (host_redacted_hash, kind, port) tuple
         *     on the app. The plaintext host NEVER appears in the response —
         *     only the SHA-256 hash of (salt||host). See spec §11.
         *
         *     The list is bounded by the per-plan `api.DataPlacementHintsPerApp`
         *     quota (Free=0, Hobby=3, Pro=10, Scale=50). When FAAS_DATA_PLACEMENT
         *     is on the classifier derives entries on env mutation; when OFF
         *     the table stays empty and the response is `[]`.
         */
        get: operations["listAppDataUpstreams"];
        /**
         * Add a data upstream to an app.
         * @description Captures a (host, kind, port) tuple so the meterd probe loop
         *     can dial it (PR-C) and schedd can bias wake placement by the
         *     probed RTT (PR-D). Plaintext host is hashed via
         *     `sha256(HostHashSalt||host)` before insert; the response
         *     returns the hashed form only (§11 invariant).
         *
         *     **Plan limits.** Free plan returns 402
         *     `plan_data_upstreams_not_allowed`. Hobby/Pro/Scale hit their
         *     per-app cap (3/10/50) before the request body is parsed —
         *     server returns 403 `plan_limit_data_upstreams` with the
         *     observed count. Invalid inputs return 400
         *     `upstream_invalid_{kind,host,port}`.
         */
        put: operations["createAppDataUpstream"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/upstreams/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description Upstream id (UUID). */
                id: string;
            };
            cookie?: never;
        };
        /**
         * Get one data upstream.
         * @description Returns the single upstream row by id. Plaintext host NEVER
         *     appears in the response (§11 invariant).
         */
        get: operations["getAppDataUpstream"];
        put?: never;
        post?: never;
        /**
         * Delete a data upstream.
         * @description Removes one row by id. Cascades into the probe set
         *     (next probe tick no longer dials the host).
         */
        delete: operations["deleteAppDataUpstream"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/deployments": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List deployments across all apps on the account.
         * @description Paged backwards (newest first). `next_before` is an opaque cursor
         *     (RFC3339Nano of the `created_at`); pass it on the next request to
         *     page backwards. Empty `next_before` means end of list.
         */
        get: operations["listDeployments"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/deployments/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /** Fetch one deployment. */
        get: operations["getDeployment"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Set the per-deployment cold-wake floor.
         * @description Update the deployment's min_instances (issue #557 closure /
         *     ADR-072). The only mutable field on a deployment post-create;
         *     image / digest / overrides / sidecars stay immutable (a new
         *     deployment is the canonical way to change them). Pass
         *     min_instances=0 to inherit from the parent app's floor.
         *     Validated against the parent app's plan MaxMinInstances cap.
         */
        patch: operations["updateDeploymentMinInstances"];
        trace?: never;
    };
    "/v1/deployments/{id}/traffic": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Set the per-deployment traffic-split weight.
         * @description Update the deployment's traffic_percent (issue #556 PR-A).
         *     PR-A uses the zero-siblings rebalance form: setting row R's
         *     traffic_percent to N forces every other live row in the same
         *     app to 0, keeping Σ = 100 by construction. Pro/Scale only —
         *     Free/Hobby are rejected at 403 `plan_traffic_split_not_allowed`.
         *     Range-check [0, 100] is enforced at the handler (422
         *     `invalid_traffic_percent`). The Σ invariant is asserted
         *     post-write as a defensive backstop (409
         *     `traffic_percent_sum_invalid`) — structurally unreachable
         *     with zero-siblings, but pinned by the test suite.
         */
        patch: operations["updateDeploymentTraffic"];
        trace?: never;
    };
    "/v1/deployments/{id}/logs": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /**
         * Stream build logs (SSE).
         * @description Server-Sent Events stream of build logs. `follow=1` holds the
         *     connection open until the build completes.
         */
        get: operations["streamDeploymentLogs"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/deployments/{id}/scan": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /**
         * Get per-deploy grype scan.
         * @description Returns the per-deploy grype CVE scan payload (issue #464 /
         *     ADR-055). The scan runs on the per-app layer ext4 in imaged's
         *     deploy-complete path (after `SetDeploymentRootfs`, before the
         *     pending→snapshotting transition) and lands on the
         *     `deployments` row.
         *
         *     Status field is the closed enum:
         *       - `complete` — full payload (SeverityCounts + Vulnerabilities).
         *       - `failed` — payload carries `error` only; SeverityCounts is
         *         all-zero, Vulnerabilities is nil. Rendered as the
         *         "scan failed" chip on the dashboard.
         *       - `skipped` — pre-feature backfill row
         *         (migrations/00135 stamps `scan_status='skipped'` on every
         *         pre-#464 row). Payload carries the reason sentinel.
         *
         *     A 404 is returned when:
         *       - the deployment row does not exist,
         *       - the deployment belongs to a different account (IDOR-safe;
         *         no account-existence leak),
         *       - no scan has run yet (the deploy is still mid-pipeline or
         *         the row predates #464 entirely).
         */
        get: operations["getDeploymentScan"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/deployments/{id}/secret-scan": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /**
         * Get per-deploy image-layer secret scan.
         * @description Returns the per-deploy secret-scan audit row (PR-A /
         *     ADR-101). The scan runs on the per-app ext4 in imaged's
         *     deploy-complete path (after `SetDeploymentRootfs`, before
         *     the pending→snapshotting transition) using the same
         *     `pkg/secretscan` engine the apid source-tree rejection
         *     path uses — same patterns, same providers, same Severity
         *     table, same snippet policy.
         *
         *     Status field is the closed enum:
         *       - `complete` — image layer walked clean; `findings=[]`.
         *         Stamped on every scan (clean OR hit) so the dashboard
         *         renders the audit row immediately after the build.
         *       - `complete_with_redactions` — at least one finding
         *         landed in the audit row; `error_code =
         *         'image_secret_detected'` on the deployment. The
         *         deploy's pending→snapshotting transition does NOT
         *         fire.
         *
         *     A 404 is returned when:
         *       - the deployment row does not exist,
         *       - the deployment belongs to a different account
         *         (IDOR-safe; no account-existence leak),
         *       - no scan has run yet (the deploy is still
         *         mid-pipeline or the row predates PR-A entirely).
         *
         *     Each finding carries a `layer` label that attributes the
         *     finding to the per-walk source (`app` for the main image,
         *     `sidecar-<slug>` for each sidecar). Pre-PR-A rows
         *     (rejected source-tree bytes via the v2 422 path) carry
         *     `layer` empty or absent.
         */
        get: operations["getDeploymentSecretScan"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/builds": {
        parameters: {
            query?: {
                /** @description Filter to a single app slug. Cross-account slug renders 404. */
                app?: string;
                /** @description Filter to a single status. Omit for any status. */
                status?: "queued" | "running" | "succeeded" | "failed";
                /** @description Opaque cursor token from a previous response's `next_before`. Format: `<rfc3339nano>|<id_hex>` (pipe-separated). Empty started_at segment encodes a queued-tail cursor (the `<id_hex>` part alone). Round-trip verbatim — do NOT re-parse or re-encode. */
                before?: string;
                /** @description Page size (default 50, capped at 200). */
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List builds (operator view).
         * @description Returns every build the authenticated account owns, ordered
         *     started_at DESC (nulls last — queued builds stay at the
         *     bottom of the first page). Optional ?app=<slug> narrows to
         *     one app; optional ?status=<s> filters to the 4-value status
         *     enum (queued|running|succeeded|failed; omit for any status).
         *     Cursor pagination via ?before=<opaque token>; limit defaults
         *     to 50, capped at 200.
         *
         *     The response shape mirrors /v1/deployments: items + a
         *     next_before cursor (empty when end of list). The cursor is
         *     the opaque tuple `<rfc3339nano>|<id_hex>` of the LAST row
         *     on this page — server-emitted, round-tripped verbatim. The
         *     id tiebreaker makes the keyset deterministic for queued
         *     tails (started_at IS NULL) and for sub-second collisions
         *     on started_at. See ADR-091 §3.
         *
         *     BuildResponse.started_at (the per-row wire field) is
         *     RFC3339 (whole-second) for backward compatibility with
         *     `GET /v1/builds/{id}`. The cursor's started_at segment is
         *     RFC3339Nano (sub-second preserved) so the keyset
         *     sub-second clause is reachable on rows whose started_at
         *     falls in the same wall-clock second. The two are
         *     deliberately different and the cursor's higher precision
         *     is intentional.
         */
        get: operations["getBuilds"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/builds/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /**
         * Get build status.
         * @description Returns the lifecycle row for a build id — current status,
         *     enqueued/started/finished timestamps, failure_class (when
         *     status='failed'), and a server-computed duration_seconds
         *     (only set when both started_at and finished_at are
         *     populated). Companion to /v1/builds/{id}/provenance (post-
         *     mortem export) and /v1/builds/{id}/sbom (post-mortem
         *     blob); this one is the LIFECYCLE surface CI scripts call
         *     to fail-fast on build error without scraping SSE.
         *
         *     The status enum is `queued|running|succeeded|failed` (the
         *     builds_status_check CHECK constraint; no 'cancelled' value).
         *     failure_class, when present, is one of `oom|timeout|
         *     user_error|infra` (the builds_failure_class_check CHECK
         *     constraint). error_message is intentionally NOT in the
         *     response — it lives on deployments; call GET
         *     /v1/deployments/{id} for the per-failure string.
         */
        get: operations["getBuild"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/builds/{id}/provenance": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /**
         * Get build provenance.
         * @description Returns the ADR-038 `build_provenance` row for a single build.
         *     Each successful build produces exactly one provenance row
         *     (builderd's populator runs at the `markSucceeded` sites); the
         *     row is the customer-visible "what ran?" record: buildkit /
         *     railpack version, base / runner digests, source URL + commit
         *     SHA, plan, builder node ID, and the build's started_at /
         *     finished_at timestamps.
         *
         *     A 404 with `code=build_provenance_not_found` is returned when
         *     the build exists but no provenance row landed (the populator
         *     logs a WARN inside builderd on a failed INSERT — the build
         *     itself still succeeded). A 404 with `code=not_found` is
         *     returned when no build row matches the id, or when the
         *     build's owning app belongs to a different account.
         */
        get: operations["getBuildProvenance"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/builds/{id}/sbom": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /**
         * Get build SBOM (CycloneDX JSON).
         * @description Streams the CycloneDX 1.5 SBOM JSON generated by `imaged`'s
         *     post-build syft pass (issue #299). The body is the raw SBOM
         *     document; the response content-type is
         *     `application/vnd.cyclonedx+json` so external tooling
         *     (cyclonedx-cli validate, jq with `@cyclonedx-json`, Grype's
         *     `--from-file=cyclonedx-json`) can dispatch on header alone.
         *
         *     Three failure modes, each with its own code so the SDK and
         *     `faas build sbom <id>` can branch:
         *
         *       - `404 not_found` — the build id does not exist, OR the build
         *         belongs to another account (the handler returns the same
         *         surface on both so account-existence isn't probeable).
         *       - `503 build_sbom_unavailable` — the build row exists for this
         *         account but imaged's syft populator did not persist a
         *         CycloneDX document (pre-PR build, or best-effort WARN).
         *         The CLI prints "no SBOM for this build" and exits 1; the
         *         operator's job is to re-deploy imaged with Phase 3 active.
         *       - `503 capacity_unavailable` — the SBOM exists but the file
         *         was unreadable from disk (storage backend returned a
         *         transient error). The customer retries once.
         */
        get: operations["getBuildSbom"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/domains": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** List custom domain bindings. */
        get: operations["listDomains"];
        put?: never;
        /** Bind a custom domain. */
        post: operations["createDomain"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/domains/{domain}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The custom domain string (e.g. `app.example.com`). */
                domain: string;
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Remove a custom domain binding. */
        delete: operations["deleteDomain"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/crons": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** List cron triggers. */
        get: operations["listCrons"];
        put?: never;
        /** Create a cron trigger. */
        post: operations["createCron"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/crons/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /**
         * Get one cron trigger.
         * @description Single-row read for one cron (issue #791 PR-E / ADR-090
         *     closure). Backs `gregale crons info <id>` and any dashboard
         *     drill-down. The wire shape matches `CronResponse` so SDK
         *     clients decode it with the existing struct.
         */
        get: operations["getCron"];
        put?: never;
        post?: never;
        /** Delete a cron. */
        delete: operations["deleteCron"];
        options?: never;
        head?: never;
        /** Partial-update a cron. */
        patch: operations["updateCron"];
        trace?: never;
    };
    "/v1/crons/{id}/runs": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /**
         * List recent runs of a cron.
         * @description Execution history for one cron, newest first. Each row reports
         *     when the fire started, how long it took (`duration_ms`,
         *     computed server-side), and a normalized `outcome` — so a
         *     timeout is distinguishable from a generic failure without
         *     parsing `error`.
         *
         *     Paginated by `?before=<id>` (the LAST id of the returned
         *     slice). Defaults to 10 per page; capped at 100. For a wider,
         *     cross-source view use `GET /v1/invocations`.
         */
        get: operations["listCronRuns"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/crons/{id}/run": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The cron id (UUID hex, no dashes). */
                id: string;
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Manually fire a cron now (bypasses the schedule boundary).
         * @description Issue #791 PR-C / ADR-090. Inserts a pending row into
         *     `cron_fire_now_requests` and emits `db.NotifyCronRunNow`;
         *     schedd claims the row on the next LISTEN delivery and calls
         *     `RunCronNow` in its own process. The response is the
         *     immediate 202 with the request id; the customer's
         *     `GET /v1/crons/{id}/runs` will surface the matching
         *     `cron.fired.manually` audit row once schedd stamps the
         *     terminal state.
         *
         *     Idempotent: a replay with the same Idempotency-Key returns
         *     the stored 202 without enqueuing a second fire.
         *
         *     Scoped to `deploy:write` (or `admin`); no new `cron:write`
         *     scope is added (ADR-090 §Sub-decisions 1). The fire does
         *     NOT shift `last_fired_at` — the next scheduled boundary is
         *     unaffected.
         */
        post: operations["fireCron"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/cron-fire-now-requests/{request_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get fire-now request state
         * @description Polling surface for the row that `POST /v1/crons/{id}/run`
         *     inserted (issue #791 PR-D / ADR-090 §Sub-decision 7).
         */
        get: operations["getFireCronRequest"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects/scan": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Scan an uploaded tarball and return a deploy plan.
         * @description Dry-run. Accepts a multipart upload (`source=<tar.gz>`,
         *     `project_slug`, `production_branch`, `install_id`, `only`)
         *     and returns a PlanResponse with the discovered workloads,
         *     managed services, derived scan_source, and a plan_token
         *     that the apply endpoint can echo back to skip the
         *     second extract.
         *
         *     On over-quota the response carries `can_apply=false`
         *     (and `crons_not_allowed=true` for Free plan) so the CLI
         *     can branch without a second request.
         */
        post: operations["scanProject"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/projects": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Apply a deploy plan in one transaction.
         * @description Accepts the same multipart body as /scan plus an optional
         *     `plan_token` query parameter echoing the dry-run token. On
         *     success the response carries the inserted project_id and
         *     per-app IDs so the CLI's `--yes` flow can render
         *     `applied: <slug> → <app_id>`. On quota the response is
         *     the matching RFC 7807 problem (402 Free crons, 403 apps
         *     or cron cap) with zero rows inserted.
         *
         *     The apply handler resolves workload-name → app_id from
         *     the just-inserted apps and inserts crons in a follow-up
         *     pass; the quota check ran inside ApplyProjectPlan's Tx.
         */
        post: operations["applyProject"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/alerts": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /**
         * List alert rules visible at this app.
         * @description Returns both app-scoped rules (app_id == <slug>) and
         *     account-wide rules (app_id == ""). Account-wide rules
         *     apply to every app on the account.
         */
        get: operations["listAlertRules"];
        put?: never;
        /**
         * Create an alert rule.
         * @description Plaintext webhook_secret arrives in the body, is sealed
         *     via the host age recipient, and never appears in a
         *     response. webhook_url is SSRF-guarded at write time
         *     (loopback + metadata ranges denied).
         */
        post: operations["createAlertRule"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/alerts/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /** Fetch one alert rule by id. */
        get: operations["getAlertRule"];
        put?: never;
        post?: never;
        /** Delete an alert rule. */
        delete: operations["deleteAlertRule"];
        options?: never;
        head?: never;
        /**
         * Partial-update an alert rule.
         * @description Every field is optional. metric cannot cross families
         *     (e.g. error_rate_pct → failed_invocations) — returns
         *     400 alert_rule_invalid.
         */
        patch: operations["updateAlertRule"];
        trace?: never;
    };
    "/v1/apps/{slug}/alerts/{id}/rotate-secret": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Mint a new webhook HMAC secret.
         * @description Server-mints a 32-byte secret, base64-encodes it, and
         *     overwrites the row's sealed ciphertext in place. The
         *     plaintext is NEVER returned in the response — the body
         *     carries the masked constant + rotated_at only.
         */
        post: operations["rotateAlertRuleSecret"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/edge-rules": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List every edge rule owned by the caller.
         * @description Account-wide listing. The dashboard overview pane uses this;
         *     the CLI uses it for `gregale edge-rules list`. Free plans only
         *     see rule kinds their plan unlocks — the server still lists
         *     them.
         */
        get: operations["listEdgeRules"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/edge-rules": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /** List every edge rule bound to one app. */
        get: operations["listEdgeRulesForApp"];
        put?: never;
        /**
         * Create an edge rule on an app.
         * @description Kind is one of {route, rewrite, redirect, headers, cors, jwt, ip,
         *     validate, geo}. `action` is a kind-tagged jsonb body — the per-kind
         *     shape is documented under components/schemas. Plan-kind gate:
         *     jwt/ip return 402 plan_edge_rule_kind_not_allowed on Free; geo
         *     is allowed on Free with a tighter per-app quota. Per-app
         *     quota returns 402 plan_limit_edge_rules once EdgeRulesPerApp
         *     is reached.
         */
        post: operations["createEdgeRule"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/edge-rules/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /** Fetch one edge rule by id. */
        get: operations["getEdgeRule"];
        put?: never;
        post?: never;
        /** Delete an edge rule. */
        delete: operations["deleteEdgeRule"];
        options?: never;
        head?: never;
        /**
         * Partial-update an edge rule.
         * @description Every field is optional. `kind` is NOT patchable — rotating
         *     kind mid-life would break the action union. To change kind,
         *     delete and recreate. `action` replaces the jsonb column
         *     whole (no partial-update shape).
         */
        patch: operations["updateEdgeRule"];
        trace?: never;
    };
    "/v1/apps/{slug}/throttle-suggestions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Per-route throttle recommender (ADR-091 D20.5 amendment,
         *     issue #881). Read-only: returns a suggested rps/burst per
         *     route over the window, clamped to the customer's plan
         *     ceiling so the suggestion is always settable.
         *
         *     The recommender is ADVICE-ONLY — it never auto-applies.
         *     Customers confirm via POST /v1/apps/{slug}/edge-rules.
         */
        get: operations["getAppThrottleSuggestions"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/webhooks": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /** List outbound webhook subscriptions for this app. */
        get: operations["listAppWebhooks"];
        put?: never;
        /**
         * Create a new outbound webhook subscription.
         * @description target_url is SSRF-guarded at write time (loopback / RFC1918
         *     / link-local / metadata IPs are rejected unless
         *     FAAS_EGRESS_ALLOW_LOOPBACK=1). webhook_secret arrives in
         *     the body, is sealed with secretbox.SealBytes under the
         *     APP_WEBHOOK namespace, and is NEVER returned in plaintext
         *     — the response shape carries the masked constant. event_filter
         *     is an optional allowlist; empty subscribes to every event in
         *     the closed vocabulary.
         */
        post: operations["createAppWebhook"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/webhooks/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /** Fetch one webhook subscription by id. */
        get: operations["getAppWebhook"];
        put?: never;
        post?: never;
        /**
         * Delete a webhook subscription.
         * @description Cascades into app_webhook_deliveries via FK CASCADE — the
         *     customer can no longer read the delivery history once the
         *     subscription is gone. Mirrors the alert-rule DELETE shape.
         */
        delete: operations["deleteAppWebhook"];
        options?: never;
        head?: never;
        /**
         * Partial-update a webhook subscription.
         * @description Every field is optional. To rotate the secret in place, send
         *     `webhook_secret` (the handler seals it; the response carries
         *     the masked constant). To rotate via the dedicated endpoint,
         *     use POST /webhooks/{id}/rotate-secret.
         */
        patch: operations["updateAppWebhook"];
        trace?: never;
    };
    "/v1/apps/{slug}/webhooks/{id}/rotate-secret": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Server-mint a new webhook HMAC secret.
         * @description Server-mints a 32-byte secret, seals it, and overwrites the
         *     row's sealed ciphertext in place. The plaintext is NEVER
         *     returned in the response — the body carries the masked
         *     constant + rotated_at only.
         */
        post: operations["rotateAppWebhookSecret"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/webhooks/{id}/deliveries": {
        parameters: {
            query?: {
                /** @description Page size (1..100; default 50). */
                page_size?: number;
                /** @description Opaque cursor from the previous page's next_token. */
                page_token?: string;
            };
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /**
         * List recent deliveries for this webhook.
         * @description Cursor-paginated; most-recent-first. The delivery status
         *     follows the closed vocabulary `pending | in_flight |
         *     succeeded | failed | dead`. Dead rows can be re-armed with
         *     POST /webhooks/{id}/deliveries/{did}/retry.
         */
        get: operations["listAppWebhookDeliveries"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/webhooks/{id}/deliveries/{did}/retry": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
                /** @description Delivery id (32-hex-char UUID). */
                did: string;
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Re-arm a dead delivery.
         * @description Resets the row to `pending` with attempt=0 +
         *     next_attempt_at=now() so the dispatcher picks it up at the
         *     next tick. Only valid on rows with status='dead'; returns
         *     400 app_webhook_invalid otherwise.
         */
        post: operations["retryAppWebhookDelivery"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/invoke": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Sync-invoke an app; long-poll for the result.
         * @description Enqueues an invocation row and waits for the drain to drive it
         *     to a terminal state. Server-side cap is 30s on paid plans, 5s
         *     on Free. Returns 504 (long_poll_timeout) when the cap elapses;
         *     the customer can immediately re-call /v1/invocations/{id}
         *     to pick up the eventual result.
         */
        post: operations["invokeApp"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/invoke/async": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Async-invoke an app; returns id + status URL.
         * @description Enqueues an invocation row and returns immediately with the
         *     id. The customer polls /v1/invocations/{id} (or uses the
         *     dashboard SSE) for the eventual row state.
         */
        post: operations["invokeAppAsync"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/queues/send": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Enqueue a row on the per-app FIFO queue.
         * @description Cap-checked against the plan's MaxQueueDepth (Hobby 5, Pro 25,
         *     Scale 100). The drain re-checks at dispatch tick.
         */
        post: operations["queueSend"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/queues/receive": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Long-poll for the next dispatched row.
         * @description Returns 200 with the dequeued row, or 204 (no body) if the
         *     server-side 30s budget elapses with no event. The customer
         *     retries on 204.
         */
        post: operations["queueReceive"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/queues/{id}/ack": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Ack a queue row (idempotent). */
        post: operations["queueAck"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/queues/state": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read queue depth, in-flight count, and oldest pending age.
         * @description Read-only depth / in-flight / oldest-pending stats. NO lease is
         *     acquired and no row is mutated — the response can be polled at
         *     any cadence without affecting drain behaviour. Free plans can
         *     call this for diagnostics even though they cannot send.
         */
        get: operations["queueState"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/queues/peek": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List pending queue rows without acquiring a lease.
         * @description Read-only peek at pending rows, oldest first. Repeated calls
         *     return the same rows in the same order — the underlying SQL has
         *     no FOR UPDATE / FOR SHARE / advisory lock, so attempts is
         *     never incremented and no row state changes. Cursor pagination
         *     matches the existing `?before=<id>` convention. NOT equivalent
         *     to `queues/receive` — peek never leases.
         */
        get: operations["queuePeek"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/queues/dead_letter": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List queue rows that exhausted the plan's retry budget.
         * @description Read-only list of rows in `state='dead_letter'`, newest first.
         *     The drain transitions a row here once it has failed
         *     `MaxQueueAttempts` times for the app's plan (Hobby 3, Pro 10,
         *     Scale 25). NO lease is acquired and no row is mutated. Replaying
         *     a dead-letter row is out of scope for this endpoint — the
         *     customer re-sends via `queues/send`.
         */
        get: operations["queueDeadLetter"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/delayed-tasks": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Schedule a delayed task to fire at a future time.
         * @description Cap-checked against the plan's MaxDelayedTasksPerApp (Hobby 5,
         *     Pro 50, Scale 1_000_000). The drain re-checks at dispatch.
         */
        post: operations["delayedTaskCreate"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/invocations": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List recent invocations on the account.
         * @description Paginated by `?before=<id>` (the LAST id of the returned slice).
         *     Defaults to 20 per page; capped at 200.
         */
        get: operations["listInvocations"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/instances": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List every live instance across the caller's account.
         * @description Replaces the per-app fan-out from `/v1/apps/{slug}/instances`
         *     with one account-scoped read (issue #393). Each instance carries
         *     its `app_id`; cross-account isolation is enforced by the SQL
         *     `apps.account_id = $1` join (test: see
         *     `TestListInstancesForAccount_CrossAccountIsolation`).
         *
         *     Cursor: `?before=<id>` (the instances.id UUIDv7). Defaults to 25
         *     per page; capped at 100; invalid `limit` returns 400
         *     `code_validation` with `limit=100` and the observed value
         *     (RFC 7807 strict mode, matching `/v1/invoices`).
         *
         *     Rate-limit tiering: one call now replaces N per-app calls,
         *     so per-page-load token spend drops from N to 1. The
         *     per-account bucket (ADR-040) still applies at the gatewayd
         *     edge; this route charges 1 token via the apid authLimited
         *     middleware, same as every other `/v1/*` route.
         */
        get: operations["listInstancesForAccount"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/secrets": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List every sealed secret across the caller's account.
         * @description Replaces the per-app fan-out from `/v1/apps/{slug}/secrets`
         *     with one account-scoped read (issue #393). Each row carries
         *     the owning app's `app_id` and `app_slug` so the dashboard
         *     can render "foo-app / DATABASE_URL" without a parallel
         *     `/v1/apps` round-trip.
         *
         *     **Plaintext never appears here** — only the age-sealed
         *     envelope (base64). The plaintext value lives transiently in
         *     the PUT handler and never crosses the apid wire.
         *
         *     Cursor: `?before=<slug>|<key>` — the (app_slug, key) pair,
         *     pipe-separated. The SQL splits it back via `split_part`.
         *     Sort order is (app_slug ASC, key ASC). Default limit 25,
         *     max 100 (strict 400 on bad input, matching `/v1/invoices`).
         */
        get: operations["listSecretsForAccount"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/metrics": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Account-wide per-app metrics rollup.
         * @description One call replaces N per-app `/v1/apps/{slug}/metrics` calls
         *     (issue #393). Returns the same `AppMetricsResponse` shape
         *     per app, keyed by `app_slug`, so the dashboard can render
         *     all apps on a single page without a per-app fan-out.
         *
         *     Range is the closed vocabulary from the per-app endpoint:
         *     `5m` (default) | `15m` | `1h` | `6h` | `24h` | `7d` | `15d`.
         *     Prometheus failure short-circuits the entire response
         *     (never partial-populated) and emits `source:
         *     "degraded: <reason>"` with zeroed `apps`, matching the
         *     per-app contract exactly.
         *
         *     PromQL cost: 6 round-trips regardless of N apps (vs. 7N
         *     for the naive per-app loop) — see `pkg/promql.Client.QueryMap`
         *     and `Client.QueryBuckets`.
         */
        get: operations["getAppsMetrics"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/invocations/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Read a single invocation by id (account-scoped). */
        get: operations["getInvocation"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/invocations/{id}/replay": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Re-issue a failed or dead_letter invocation.
         * @description Accepts no request body. The replayed row carries the original's
         *     payload + headers + method + path verbatim and is enqueued against
         *     the same app. The new row's Source is `replay` (issue #315).
         *
         *     Only invocations whose current state is `failed` or `dead_letter`
         *     can be replayed — re-running a successful or in-flight invocation
         *     would be a customer bug, not a flow we want to enable by accident.
         *     A replay attempt on any other state returns 409
         *     `invocation_not_replayable`.
         */
        post: operations["replayInvocation"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/delayed-tasks/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /** Read a single delayed task by id. */
        get: operations["delayedTaskGet"];
        put?: never;
        post?: never;
        /**
         * Cancel a pending delayed task.
         * @description Idempotent: a re-cancel (or a cancel of an already-fired row)
         *     is a 204. The drain ignores cancelled rows at dispatch.
         */
        delete: operations["delayedTaskCancel"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/keys": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** List API keys (no plaintext). */
        get: operations["listKeys"];
        put?: never;
        /**
         * Mint a new API key.
         * @description Returns the plaintext key **once**. The plaintext is never stored
         *     and cannot be retrieved; subsequent GETs return only the prefix.
         */
        post: operations["createKey"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/keys/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Revoke an API key. */
        delete: operations["deleteKey"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/keys/{id}/rotate": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Rotate an API key.
         * @description Mints a new key (status='active') and demotes the old key in
         *     a single transaction (issue #189 / IAM-5). The new key
         *     inherits the predecessor's label + scopes so the customer's
         *     CI config does not need to chase a label change.
         *
         *     The old key's `expires_at` is OVERWRITTEN to the grace
         *     deadline (now + grace_window_days, where grace_window_days
         *     resolves from the per-account override or the 7-day plan
         *     default). Status flips to 'grace' for the window, then to
         *     'revoked' at the deadline. Setting
         *     `accounts.key_grace_window_days = 0` makes rotation atomic
         *     (old key revoked immediately).
         *
         *     Returns the new plaintext exactly once — the old plaintext
         *     is not re-issued (we only store the SHA-256 hash). The
         *     customer's CI script captures the new plaintext at the
         *     moment of rotation and uses the new key thereafter; the old
         *     key remains valid only for the grace window.
         *
         *     Quota: rotation is quota-neutral (-1 +1 = 0 net). A
         *     customer AT the per-account cap can still rotate.
         */
        post: operations["rotateKey"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{slug}/keys": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /**
         * List API keys minted against the active org.
         * @description Returns every key the org owns (active + grace + revoked).
         *     Mirrors `GET /v1/keys`; PR 6's canonical path. The `org_id`
         *     on every row will match `{slug}` because the store filters
         *     server-side on the loaded membership.
         */
        get: operations["listOrgAPIKeys"];
        put?: never;
        /**
         * Mint a new API key for the active org.
         * @description Returns the plaintext exactly once (same as `POST /v1/keys`).
         *     The new row's `org_id` is the loaded membership's org; personal
         *     orgs are mintable (the `org_personal_immutable` 409 applies to
         *     mutations on the org row, not key mints against it).
         */
        post: operations["createOrgAPIKey"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{slug}/keys/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /**
         * Fetch a single API key by id (org-scoped).
         * @description Lookup mirror of `GET /v1/keys/{id}` (the legacy path does not
         *     exist by id in pre-PR-6 — this path is the canonical single-key
         *     read). The response is the standard `APIKeyResponse` (no
         *     plaintext). Cross-org probes collapse to 404.
         */
        get: operations["getOrgAPIKey"];
        put?: never;
        post?: never;
        /**
         * Revoke an API key (org-scoped).
         * @description Soft-delete mirror of `DELETE /v1/keys/{id}`. Status flips to
         *     'revoked'; subsequent bearer-auth attempts hit `ErrAPIKeyRevoked`
         *     (401 unauthenticated). The audit row carries `org_id` (PR 6
         *     closes the ADR-061 §E "audit scoped to org" gap).
         */
        delete: operations["revokeOrgAPIKey"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{slug}/keys/{id}/rotate": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Rotate an API key (org-scoped).
         * @description Org-scoped counterpart of `POST /v1/keys/{id}/rotate`. Mints a
         *     new key (status='active') and demotes the predecessor into the
         *     grace window in one transaction. The new key inherits the
         *     predecessor's `org_id` — rotation never silently rebinds across
         *     orgs. Quota is neutral (-1 +1 = 0).
         */
        post: operations["rotateOrgAPIKey"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/account/keys/grace_window_days": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read the per-account rotation grace-window override.
         * @description Returns the customer's current override (the value stored in
         *     `accounts.key_grace_window_days`) and the plan default
         *     alongside. `days=null` means "no override" — the rotation
         *     handler uses the plan default (api.DefaultAPIKeyGraceWindowDays = 7).
         */
        get: operations["getGraceWindow"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Set the per-account rotation grace-window override.
         * @description Writes the override and invalidates the in-process cache so
         *     the next rotation observes the new value. `days=0` is atomic
         *     rotation (no grace); `days=null` (or omission) clears the
         *     override and falls back to the plan default.
         */
        patch: operations["setGraceWindow"];
        trace?: never;
    };
    "/v1/account/egress_allowlist_extra": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read the per-account egress allowlist extra budget.
         * @description Returns the per-account additive budget on top of the
         *     plan's `apps.egress_allowlist` cap (issue #679 / PR-B /
         *     ADR-082). The plan cap (Pro 16 / Scale 64) is
         *     authoritative for the default case; the additive budget
         *     lets admins raise one account's effective cap without
         *     changing the plan default for everyone. `extra=0`
         *     means "no override" — the plan cap is authoritative.
         *
         *     Admin scope is required.
         */
        get: operations["getAccountEgressAllowlistExtra"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /**
         * Set the per-account egress allowlist extra budget.
         * @description Writes the per-account additive budget. `extra=0` clears
         *     the override (the plan cap is authoritative again);
         *     negative values or values above `max_extra` (1024) are
         *     rejected with `account_egress_allowlist_extra_out_of_range`.
         *     The PATCH emits an `account.egress_allowlist_extra_set`
         *     audit row carrying `old_extra`, `new_extra`, `plan_cap`,
         *     and `max_extra` so the dashboard can render the toggle
         *     history.
         *
         *     Admin scope + MFA are required.
         */
        patch: operations["setAccountEgressAllowlistExtra"];
        trace?: never;
    };
    "/v1/audit-events": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the caller's auth audit events.
         * @description Newest-first. Returns the customer's own security event timeline:
         *     logins, logouts, key mints/deletes, secret sets/deletes, plan
         *     changes, and account deletion scheduling/restore.
         *
         *     The events table is append-only (spec §5 / §6.1). The response
         *     is bounded by `limit` (default 50, max 100). For a full-history
         *     pull, use `GET /v1/account/export`, which unions these events
         *     with the customer's GDPR-action rows.
         */
        get: operations["listAuditEvents"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/audit-events/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Event row id (bigint as string). */
                id: string;
            };
            cookie?: never;
        };
        /**
         * Fetch one auth audit event by id.
         * @description Returns the single event with that id if and only if it
         *     belongs to the caller's account. Cross-account access is
         *     indistinguishable from "not found" — a customer cannot
         *     enumerate other accounts' row counts by ID-probing.
         */
        get: operations["getAuditEvent"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/audit-log": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List the caller's audit-log entries (post-deletion evidence).
         * @description Newest-first. Reads the FK-free `audit_log` table
         *     (migrations/00163_audit_log.sql), distinct from
         *     `/v1/audit-events` which reads the live `events` table. The
         *     `audit_log` table is append-only by spec (ISO 27001 SoA
         *     A.5.33 — retention forever) and a regulator / DPO can replay
         *     post-deletion state from the row alone.
         *
         *     Scope: session cookie (implicitly admin) or any API key
         *     carrying `{admin, apps:read}` (`api.ScopesReadSurface`).
         *     MFA-gated. Cross-account invisibility is enforced by pinning
         *     `account_id` to the calling account's id inside the handler;
         *     the SQL filter rejects `account_id IS NULL` rows by default
         *     (a customer never sees anonymous rows).
         */
        get: operations["listAuditLog"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/audit-log/all": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Operator view of every audit-log entry (cross-account).
         * @description Admin-only read of the FK-free `audit_log` table. Reads
         *     across accounts by default; pass `?account_id=<uuid>` to
         *     restrict to one account. `?include_anonymous=true` surfaces
         *     the `account_id IS NULL` rows emitted by background /
         *     anonymous activity (the customer-scoped `/v1/audit-log`
         *     endpoint never returns those rows).
         *
         *     Scope: session cookie (implicitly admin) or an admin API
         *     key (`api.ScopesAdminOnly` — `{admin}`). Not MFA-gated at
         *     the handler level because admin sessions / admin keys are
         *     already MFA-gated upstream at session / key issue time.
         */
        get: operations["listAuditLogAll"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/apps/{slug}/wakes/{wake_id}/timeline": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug (lowercase, kebab-case; per-account unique). */
                slug: string;
                /**
                 * @description The per-wake correlation handle minted by the schedd
                 *     engine (UUID v4 in production). The endpoint returns
                 *     every `wake.*` events row whose `data.wake_id`
                 *     matches — the partial index `events_wake_id_idx`
                 *     (migrations/00113) serves the read in O(frames)
                 *     regardless of the events table size.
                 */
                wake_id: string;
            };
            cookie?: never;
        };
        /**
         * List the canonical wake-timeline frames for one wake.
         * @description Oldest-first (forward narrative). Returns every typed
         *     `wake.*` events row for the given wake_id: queue_accepted
         *     → admitted → boot_started → boot_completed →
         *     readiness_200 → proxy_first_byte. Build and deploy
         *     failures (`wake.build_failed`, `wake.deploy_failed`,
         *     `wake.boot_failed`) are joined in alongside the success
         *     path so a single GET shows the whole lifecycle.
         *
         *     The endpoint is a sub-resource of `/v1/apps/{slug}`;
         *     auth and rate-limit share the §12 per-app budget with
         *     logs/metrics/wake. Cross-account access 404s the
         *     same way unknown slugs do (forge-proof: every row's
         *     `data.app_id` is verified to match the resolved app).
         */
        get: operations["listWakeTimeline"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/usage": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Per-app monthly usage. */
        get: operations["getUsage"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/usage/summary": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Monthly roll-up with overage math. */
        get: operations["usageSummary"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/usage/daily": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Per-app daily rollup (informational). */
        get: operations["usageDaily"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/usage/storage": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Per-app daily storage rollup (informational). */
        get: operations["usageStorage"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/invoices": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** List the authenticated account's billing invoices. */
        get: operations["listInvoices"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/billing/portal": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get the operator-configured billing portal URL (and the card-on-file summary).
         * @description Returns the URL the customer should be sent to in order to
         *     manage their subscription (update card, view invoices,
         *     download receipts, cancel). The URL is server-rendered from
         *     the operator's `FAAS_BILLING_PORTAL_URL` template; the server
         *     does NOT call Stripe's `BillingPortal.Session` SDK on this
         *     path (issue #253 acceptance #3 partial — a follow-up PR will
         *     add the SDK call once the spec defines the contract).
         *
         *     The response also carries a `payment_method` block (issue
         *     #242) — the card-on-file summary (brand, last-4, expiry).
         *     The CLI's `faas billing payment-method` renders from this
         *     field; the dashboard's billing page does the same. The
         *     field is omitempty so no-card-on-file responses stay clean.
         */
        get: operations["getBillingPortal"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/billing/retry": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Retry the latest unpaid invoice / transaction for this account.
         * @description Stripe path: calls `Invoices.Pay` on the most recent open
         *     invoice for the customer. The Idempotency-Key header (auto
         *     UUIDv4 if absent) collapses retries on the same
         *     `acct.ID / retry / invoice.ID` key.
         *
         *     Paddle path: creates a new `Transaction` against the
         *     existing customer for the same plan + month-to-date overage.
         *     The CLI forwards the merchant-dashboard URL via the
         *     response's `provider_ref_id` extension.
         */
        post: operations["retryLatestCharge"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/billing/cancel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Set cancel_at_period_end on the active subscription; keep the account active until period end.
         * @description Stripe: `Subscriptions.Update(cancel_at_period_end=true)`.
         *     Paddle: `Customer.Update(scheduled_change=cancel)` on the
         *     customer's stored object.
         *
         *     Returns the effective cancel timestamp (`current_period_end`
         *     on Stripe; the next month-rollover instant on Paddle) in
         *     RFC 3339 so the CLI can print "your apps will stop on …".
         */
        post: operations["cancelAtPeriodEnd"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/accounts/{id}/credits": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Issue a positive-cents credit to an account (admin-only). */
        post: operations["issueAccountCredit"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/github-webhook-secrets": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Set the per-tenant GitHub App webhook secret (admin-only). */
        post: operations["setGithubWebhookSecret"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/invoices/{id}/consume-credits": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Drain active credits FIFO against an invoice's overage (admin-only, MFA-gated). */
        post: operations["consumeInvoiceCredits"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/billing-paddle-catalog": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read the cached Paddle price + product catalog (admin-only).
         * @description Returns the in-memory catalog snapshot from *paddle.Provider.
         *     synced_at is the timestamp of the most recent successful
         *     EnsurePlanProducts call; an empty string means no hydration
         *     has run yet. On a Stripe deployment the handler returns 501
         *     with code billing_op_unsupported — the type assertion at
         *     the dispatcher fails and the surface is provider-scoped.
         */
        get: operations["listPaddleCatalog"];
        put?: never;
        post?: never;
        /**
         * Signal a Paddle catalog reset (admin-only).
         * @description Paddle's catalog is durable on the platform — the in-memory
         *     reset is a no-op (returns nil immediately) and the handler
         *     renders a 200 with empty entries so the CLI can print the
         *     "delete products from the Paddle Dashboard, then call
         *     sync" message. Future work (issue #279+) may add
         *     merchant-side cleanup; this handler will then return 502
         *     on SDK failure rather than 200.
         */
        delete: operations["resetPaddleCatalog"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/billing-paddle-catalog/sync": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Force a Paddle catalog hydration (admin-only).
         * @description Idempotent: re-running on the same platform hits the
         *     Status=active filter on ListProducts, finds existing
         *     products/prices, and skips POST. Idempotency-Key middleware
         *     replays the same 200 for a flaky-network retry so the SDK
         *     round-trip is not re-issued. Returns the post-sync catalog.
         */
        post: operations["syncPaddleCatalog"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/billing-paddle-overage/preflight": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read the paddle_overage_dedupe schema probe (admin-only).
         * @description Operator-side guard for the Paddle overage pusher's
         *     per-window claim state machine (migration 00041). Returns
         *     which of the four new columns are present + the per-state
         *     row counts so an operator can verify the meterd loop will
         *     not crash on a 42703 (column missing) error before any
         *     customer-facing push is attempted.
         *
         *     Returned by the B4 pre-flight CLI subcommand. A response
         *     with table_exists=true and any of has_window_start /
         *     has_state / has_claimed_at / has_claimed_by=false means
         *     migration 00041 was not (fully) applied. A response with
         *     table_exists=false means migrations 00034 + 00041 are
         *     both unapplied (the table has never been created).
         */
        get: operations["getBillingPaddleOveragePreflight"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/billing-reconcile/{id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Run a single-account reconcile against the active billing Provider (admin-only).
         * @description Loads the account, calls billing.Provider.ReconcileUsage for
         *     a rolling 30-day window [start, end). Stripe implements
         *     this (ADR-049 §B.1); Paddle returns billing.ErrNotImplemented
         *     and the handler maps to 501. The response surfaces the
         *     SDK-returned mb_seconds total so an operator can diff
         *     against the local usage_minutes sum.
         */
        post: operations["reconcileAccount"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/admin/secrets/rekey-progress": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Read the cumulative rekey walk progress (admin-only).
         * @description Returns the latest RekeyProgress snapshot the apid rekey
         *     runner has written — either to the in-process atomic pointer
         *     (memory-only mode) or to FAAS_REKEY_PROGRESS_FILE on disk.
         *     Operators poll this endpoint to monitor the walk after a
         *     host identity rotation; the response shape mirrors
         *     rekey.RekeyProgress exactly so a future operator tool
         *     (e.g. `gregale rekey status`) can decode without a parallel
         *     type.
         *
         *     `total` is the running count of rows observed so far; it can
         *     grow as the walk paginates through (account_id, app_id, key)
         *     order. `rekeyed` + `skipped` should approach `total` once
         *     the walk drains. `failed` should stay at zero; a non-zero
         *     value means the unseal step threw for at least one row —
         *     the operationally safe recovery is `git rm
         *     migrations/*reserve_slot.sql`-style idempotent re-trigger
         *     (toggle FAAS_REKEY_ENABLED and restart apid; the seen-set
         *     inside Replayer dedupes already-done rows).
         *
         *     When the runner is disabled (FAAS_REKEY_ENABLED unset), the
         *     endpoint returns 503 with code `rekey_disabled` so an
         *     operator can distinguish "no work yet" from "feature off".
         *     If FAAS_REKEY_ENABLED=true is set but no host age identities
         *     loaded (mfaIdentities() empty — typically FAAS_HOST_AGE_IDENTITY_PATH
         *     unset), the endpoint returns 503 with the distinct code
         *     `rekey_no_identities` (PR #825 follow-up); this avoids the
         *     misleading "set FAAS_REKEY_ENABLED and restart" detail when
         *     the operator already opted in.
         */
        get: operations["getRekeyProgress"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List orgs the caller has an active membership in.
         * @description Returns the personal org + every shared org the caller
         *     belongs to. Account-scoped (no `X-Active-Org` header needed).
         *     The list is sorted by slug.
         */
        get: operations["listOrgs"];
        put?: never;
        /**
         * Create a shared org (caller becomes the first owner).
         * @description Mints a new shared (non-personal) org + the caller's owner
         *     membership in one transaction. The slug must match
         *     `^[a-z0-9][a-z0-9-]{1,30}[a-z0-9]$` (3..32 chars); name is
         *     trimmed-non-empty (1..256 chars). Personal orgs cannot be
         *     created via this endpoint — every account already has a
         *     personal org (PR 3 backfill / migration 00099).
         */
        post: operations["createOrg"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{slug}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
            };
            cookie?: never;
        };
        /**
         * Get one org by slug.
         * @description Returns the org row + the caller's role on the org. Authz:
         *     any active member of the org (`org.view`); non-members see
         *     403 `org_role_forbidden`. Unknown slugs are 404
         *     `org_not_found` (IDOR-safe — same wire shape as the
         *     pkg/authz.LoadOrg middleware).
         */
        get: operations["getOrg"];
        put?: never;
        post?: never;
        /**
         * Soft-delete the org.
         * @description Sets `status='deleted_pending'` + `deleted_pending=true`
         *     on the row (PR 5). Hard delete + GDPR purge lands in PR 8.
         *     Authz: owner-only (`org.delete`). Personal orgs are
         *     immutable.
         */
        delete: operations["deleteOrg"];
        options?: never;
        head?: never;
        /**
         * Update the org (name and/or plan).
         * @description Partial update; both fields are pointer-typed so the
         *     handler distinguishes "omitted" from "clear". Authz
         *     routing:
         *       - `name` → `org.manage_billing` (owner + billing)
         *       - `plan` → `org.change_plan` (owner only)
         *     Personal orgs are immutable (`org_personal_immutable` 409).
         */
        patch: operations["updateOrg"];
        trace?: never;
    };
    "/v1/orgs/{slug}/members": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
            };
            cookie?: never;
        };
        /**
         * List active members of the org.
         * @description Returns the active membership rows (the store returns
         *     both active + removed; the handler filters at the API
         *     boundary). Each row carries the joined `account.email`
         *     so the dashboard can render `bob@acme.com` without a
         *     second round-trip. Removed rows do NOT count toward the
         *     member cap (per ADR-061 §B).
         */
        get: operations["listOrgMembers"];
        put?: never;
        /**
         * Invite a new member (returns plaintext token ONCE).
         * @description Mints a 32-byte plaintext token, hashes it via SHA-256 for
         *     storage, and returns the plaintext ONCE in the response.
         *     The token expires after 14 days; admins can revoke earlier
         *     via `DELETE /v1/orgs/{slug}/invitations/{token}` (PR 7 owns
         *     the accept surface too — see
         *     `POST /v1/invitations/{token}/accept`). Role cannot be
         *     `owner`; transfer-ownership is the only path to owner.
         */
        post: operations["addOrgMember"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{slug}/members/{user_id}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
                /**
                 * @description Account UUID (the membership row's account_id). The
                 *     path segment is named `user_id` for backwards
                 *     compatibility with the dashboard's existing
                 *     /members/:user_id routes.
                 */
                user_id: components["parameters"]["OrgAccountID"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Remove a member.
         * @description Owner-only (`org.remove_members`). Stamps `removed_at` on
         *     the row (the row stays for audit; live-cap count drops).
         *     Self-removal is rejected at the boundary; the last-owner
         *     invariant surfaces as 409 `org_last_owner`.
         */
        delete: operations["removeOrgMember"];
        options?: never;
        head?: never;
        /**
         * Change a member's role.
         * @description Owner-only (`org.change_role`). Role cannot be `owner` on
         *     this endpoint; transfer-ownership is the only path to owner.
         *     The exactly-one-owner invariant lives in
         *     `pkg/state::UpdateOrgMemberRole`'s tx; demoting the last
         *     active owner surfaces as 409 `org_last_owner`.
         */
        patch: operations["updateOrgMemberRole"];
        trace?: never;
    };
    "/v1/orgs/{slug}/transfer_ownership": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Transfer ownership to another active member.
         * @description Atomically promotes `new_owner_account_id` to owner and
         *     demotes the caller to admin via `Store.TransferOrgOwnership`
         *     (a single PostgreSQL tx with `FOR UPDATE` locks on both
         *     rows). The exactly-one-owner invariant is enforced by the
         *     partial unique `org_memberships_one_owner_idx`
         *     (migration 00099). The new owner must already be an active
         *     member of the org.
         */
        post: operations["transferOrgOwnership"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/invitations/{token}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Plaintext or base64url-encoded invitation token. Tokens are
                 *     returned ONCE on the create call (`POST /v1/orgs/{slug}/members`)
                 *     and never re-served.
                 */
                token: string;
            };
            cookie?: never;
        };
        /**
         * Peek at a pending invitation by token (no consumption).
         * @description Read-only lookup that returns the invitation metadata
         *     (email, role, org slug, expires_at) without consuming the
         *     token. Used by the dashboard to render "you've been invited
         *     to Acme Inc. as developer" without forcing the invitee
         *     to accept yet. PR 7 added the accept surface at
         *     `POST /v1/invitations/{token}/accept`.
         */
        get: operations["peekInvitation"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/invitations/{token}/accept": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Plaintext or base64url-encoded invitation token (same
                 *     shape as the GET /v1/invitations/{token} parameter).
                 */
                token: string;
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Accept an invitation token (consume + add as member).
         * @description Consumes the invitation via `Store.ConsumeOrgInvitation` —
         *     the load-bearing tx stamps `consumed_at`, inserts the active
         *     membership, and reads the live member cap (PR 2 cap-in-tx
         *     back-stop; documents at `pkg/state/memstore.go`). Two audit
         *     rows fire post-mutation per ADR-035: `org.invitation.accepted`
         *     (invitation-side record) and `org.member.added` (member-side
         *     record). The bearer must have a valid session or API key but
         *     no `X-Active-Org` — the invitation IS how they get one. PR 8
         *     adds step-up at accept time.
         */
        post: operations["acceptInvitation"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{slug}/invitations/{token}": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
                /**
                 * @description Plaintext or base64url-encoded invitation token. The URL
                 *     is org-scoped here (vs the no-org-scoped peek at
                 *     `GET /v1/invitations/{token}`), so admin/owner gate fires
                 *     before the consume path resolves the hash.
                 */
                token: string;
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /**
         * Revoke a pending invitation.
         * @description Stamps `revoked_at` on a still-pending invitation via
         *     `Store.RevokeOrgInvitation`. Owner + admin only
         *     (`org.invite_members`, symmetric with the create-invite
         *     path). Already-consumed / already-revoked / unknown tokens
         *     collapse to a single `org_invitation_invalid` 410 (don't
         *     leak which row state was reached). Emits
         *     `org.invitation.revoked` with an 8-char token-hash prefix
         *     (never the full hash) for dashboard correlation.
         */
        delete: operations["revokeInvitation"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{slug}/seat_usage": {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
            };
            cookie?: never;
        };
        /**
         * Seat usage visibility for the active org.
         * @description Returns `{used, limit, plan}` from `Store.CountActiveOrgMembers`
         *     (the same row the cap-in-tx inside `ConsumeOrgInvitation`
         *     reads). `limit` comes from `org.Plan.OrgMembersMax()` — the
         *     `free` plan returns `0` to render "personal org only" in the
         *     dashboard rather than "0 of 0 used". Visibility-only; PR 9
         *     ships the per-seat pricing cut-over per ADR-061 §"Out of
         *     scope". Every role may read (gated by `org.view`).
         */
        get: operations["getOrgSeatUsage"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/orgs/{slug}/invitations": {
        parameters: {
            query?: {
                /**
                 * @description Opaque cursor for the row immediately preceding the
                 *     next page. Base64-url encoding of a JSON object
                 *     `{"created_at":"...","id":"..."}` (the compound key,
                 *     encoded by the server in `next_before`). Pass the
                 *     prior response's `next_before` unchanged. Omit on
                 *     the first page. Malformed cursors return 400 with
                 *     the stable code `invalid_cursor` (PR-9).
                 */
                before?: string;
                /** @description Max invitations to return per page. Silently capped at 100. */
                limit?: number;
            };
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
            };
            cookie?: never;
        };
        /**
         * List org invitations (every state).
         * @description Cursor-paginated list of every invitation minted on the
         *     org — pending, consumed, revoked, expired — in
         *     `created_at DESC` order (id tiebreak). The cursor is a
         *     opaque (base64-url-of-JSON) compound key `(created_at,
         *     id)`; `?before=<cursor>` partitions the next page so
         *     the row at the boundary is visited exactly once even
         *     under random UUIDs (PR-9 cursor upgrade). Default limit
         *     25, max 100 (per the strict-mode pagination contract at
         *     issue #393). Every role may read (gated by `org.view`,
         *     the same access model as GET /v1/orgs/{slug}/members).
         *     PR-8 ships the surface so the dashboard can render a
         *     "Pending invitations" table next to the "Members"
         *     table. Each render emits one `org.invitation.viewed`
         *     audit row (success-only, per ADR-035).
         */
        get: operations["listOrgInvitations"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/openapi.yaml": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * This spec, as YAML.
         * @description Machine-readable API description. Same bytes as
         *     `api/openapi.yaml` at the repo HEAD; embedded at build time
         *     via `//go:embed` in `pkg/apid/openapi_handler.go`. No auth.
         */
        get: operations["getOpenAPISpec"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/openapi.json": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * This spec, as JSON.
         * @description Same spec, YAML parsed and re-emitted as JSON. Preferred by
         *     SDK generators (`openapi-generator`, `oapi-codegen`).
         */
        get: operations["getOpenAPISpecJSON"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/traces/{trace_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Retrieve a stored OpenTelemetry trace.
         * @description Issue #555. Returns the full span tree for a single trace_id
         *     (32-hex, the same as the request's `wake_id`). The trace is
         *     sourced from the gatewayd-public in-memory ring (24h
         *     retention, 100k default cap). When no OTLP endpoint is set
         *     the ring is the only source — when OTLP is set, the ring
         *     still operates as the customer-facing query layer.
         *
         *     Authentication: the `X-Faas-Trace-Auth` header must carry
         *     the operator's observer token (env: `FAAS_TRACE_OBSERVER_TOKEN`).
         *     The endpoint is gated even when the dashboard session cookie is
         *     present — tracing is an operator surface, not a customer one.
         *     An empty token disables the endpoint (returns 404).
         */
        get: operations["getTrace"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** @description One entry from the closed operator runtime-configuration catalog. */
        OperatorRuntimeConfig: {
            key: string;
            label: string;
            description: string;
            category: string;
            /** @enum {string} */
            kind: "boolean" | "integer" | "duration" | "string" | "enum" | "secret_reference";
            default_value: unknown;
            desired_value: unknown;
            effective_value: unknown;
            /** @enum {string} */
            source: "default_or_environment" | "operator";
            /** @enum {string} */
            apply_mode: "hot" | "graceful" | "rolling" | "break_glass";
            mutable: boolean;
            sensitive: boolean;
            /** @enum {string} */
            status: "pending" | "applied" | "failed" | "blocked";
            last_error?: string;
            /** Format: int64 */
            version: number;
            /** Format: date-time */
            updated_at?: string;
            /** Format: date-time */
            applied_at?: string;
        };
        /** @description Durable asynchronous runtime-configuration apply request. */
        OperatorRuntimeConfigOperation: {
            /** Format: uuid */
            id: string;
            key: string;
            /** @enum {string} */
            scope: "global" | "control_plane" | "daemon" | "node";
            scope_id: string;
            /** Format: int64 */
            version: number;
            desired_value: unknown;
            effective_value: unknown;
            /** @enum {string} */
            apply_mode: "graceful" | "rolling" | "break_glass";
            /** @enum {string} */
            status: "pending" | "running" | "succeeded" | "failed" | "blocked" | "cancelled";
            phase: string;
            error?: string;
            reason: string;
            target_count: number;
            applied_count: number;
            failed_count: number;
            /** Format: date-time */
            requested_at: string;
            /** Format: date-time */
            started_at?: string;
            /** Format: date-time */
            finished_at?: string;
        };
        /** @description One append-only runtime configuration revision. */
        OperatorRuntimeConfigRevision: {
            /** Format: int64 */
            id: number;
            key: string;
            /** @enum {string} */
            scope: "global" | "control_plane" | "daemon" | "node";
            scope_id: string;
            /** Format: int64 */
            version: number;
            old_value: unknown;
            new_value: unknown;
            actor_id?: string;
            reason: string;
            /** Format: date-time */
            created_at: string;
        };
        /** @description Request to apply a historical runtime configuration revision. */
        RollbackOperatorRuntimeConfigRequest: {
            /** Format: int64 */
            version: number;
            reason: string;
            /** Format: int64 */
            expected_version?: number;
        };
        /** @description Email + password for sign-in. Email is canonicalised server-side (lowercase + trim). */
        PasswordLoginRequest: {
            /** Format: email */
            email: string;
            password: string;
        };
        /**
         * @description Successful sign-in. The session cookie is set via
         *     `Set-Cookie: faas_sid=…`; the body carries only
         *     `{account_id, plan}`. No API key is ever returned on
         *     login (issue #165, PR #2).
         */
        PasswordLoginResponse: {
            account_id: string;
            /** @enum {string} */
            plan: "free" | "hobby" | "pro" | "scale";
        };
        /**
         * @description Boot-resolved OAuth provider state for this apid host.
         *     `enabled=true` means the consent route (`/v1/auth/<name>`)
         *     will issue a 302 to the upstream consent screen;
         *     `enabled=false` means the consent route returns 503
         *     `oauth_provider_unavailable` because both the provider's
         *     `*_CLIENT_ID` and `*_CLIENT_SECRET` were unset at boot.
         */
        AuthCapabilities: {
            providers: components["schemas"]["AuthProviders"];
        };
        /**
         * @description Per-provider capability map. Closed set (google, github) —
         *     handlers MUST add a new field here when adding a new
         *     provider, not relax this to a free-form map.
         */
        AuthProviders: {
            google: components["schemas"]["OAuthProviderCapability"];
            github: components["schemas"]["OAuthProviderCapability"];
        };
        /**
         * @description One provider's capability flag. Source is
         *     `auth.SignInProvider.Enabled()` — the boot-resolved state.
         */
        OAuthProviderCapability: {
            enabled: boolean;
        };
        /** @description Email + password for signup. Same shape as PasswordLoginRequest so the create-or-claim race detection reuses the verifier. */
        PasswordSignupRequest: {
            /** Format: email */
            email: string;
            password: string;
        };
        /**
         * @description Body for the JSON-only POST /v1/auth/{signup,login} pair
         *     (issue #311). Distinct from PasswordLoginResponse: this
         *     one carries the `api_key` payload so the bearer-key CLI
         *     can use the result without a dashboard round-trip. The
         *     plaintext is returned ONCE; the caller persists it via
         *     `saveToken()` before this response is dropped.
         *
         *     Email is echoed back so the CLI's finalizeLogin step can
         *     render "Logged in as <email> (<plan> plan)" without an
         *     extra Whoami round-trip.
         */
        ProgrammaticAuthResponse: {
            /** @description Account UUID. */
            account_id: string;
            /**
             * Format: email
             * @description Email the client sent in the POST body. Echoed back so the CLI can render the success line without a Whoami round-trip.
             */
            email: string;
            /** @enum {string} */
            plan: "free" | "hobby" | "pro" | "scale";
            api_key: components["schemas"]["ProgrammaticAPIKey"];
        };
        /**
         * @description Freshly minted API key returned on the first
         *     request. The `plaintext` field is the only copy the
         *     caller will ever see — store it in `~/.config/faas/auth.json`
         *     immediately. The `id` is the row's UUID for later
         *     list/delete via `/v1/keys`.
         */
        ProgrammaticAPIKey: {
            /** @description `fp_live_<48-hex>`. Returned ONCE. */
            plaintext: string;
            /**
             * @description `fp_live_<8-hex>` — the prefix used in the auth
             *     header for greppable identification.
             */
            prefix: string;
            /** @description API key row UUID. */
            id: string;
        };
        /**
         * @description Email for the magic-link signup path. Optional; the
         *     handler accepts a missing or unparseable email and still
         *     returns 200 so the response cannot be used to enumerate
         *     accounts.
         */
        MagicLinkSignupRequest: {
            /** Format: email */
            email?: string;
        };
        /** @description Email for a password-reset request. Optional — the form-page path submits no body; the SDK sends the email. */
        PasswordResetRequest: {
            /** Format: email */
            email?: string;
        };
        /**
         * @description Token + new password for the password-reset submission.
         *     Token is the base64url-encoded 32-byte value from the
         *     email link (NOT the SHA-256 hash the server stores).
         */
        PasswordResetConfirm: {
            token: string;
            new_password: string;
        };
        /** @description New password for the authenticated account. Lets OAuth-only customers opt into password login. */
        SetPasswordRequest: {
            password: string;
        };
        /**
         * @description Body for both `POST /v1/install/repos/list` and
         *     `POST /v1/apps/{slug}/install/bind`. Carries the
         *     (installation_id, repo_full_name, production_branch) tuple
         *     the dashboard's bind picker commits. `production_branch` is
         *     optional — when omitted, githubd uses the install's
         *     `default_branch` from `/installations/{id}`.
         */
        InstallBindRequest: {
            /** Format: int64 */
            installation_id: number;
            repo_full_name: string;
            production_branch?: string;
        };
        /** @description Successful bind. `binding_id` is the deterministic `bind-<appID>-<repo>` form used in audit log entries. */
        InstallBindResponse: {
            binding_id: string;
            repo_full_name: string;
            production_branch: string;
        };
        /**
         * @description Repo visible to the user's GitHub App installation, as
         *     returned by githubd's `/user/installations/{id}/repositories`.
         *     Carries only the fields the dashboard bind picker renders —
         *     no nested owner object (the install URL already disambiguates).
         */
        RepoResponse: {
            /** Format: int64 */
            id: number;
            full_name: string;
            default_branch: string;
            private: boolean;
        };
        /** @description Account profile: id, email, plan, status, limits snapshot, current-month usage, and total app count. */
        AccountResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            /** Format: email */
            email: string;
            /**
             * @example hobby
             * @enum {string}
             */
            plan: "free" | "hobby" | "pro" | "scale";
            /**
             * @example active
             * @enum {string}
             */
            status: "active" | "past_due" | "suspended" | "deleted_pending";
            limits: components["schemas"]["AccountLimits"];
            usage_gb_hours: number;
            app_count: number;
            github_install_id?: string | null;
        };
        /** @description Plan-driven quota and resource caps: max RAM per app, concurrent wakes, total deployed apps, included GB-hours, and max app-layer bytes per build. */
        AccountLimits: {
            /**
             * @example hobby
             * @enum {string}
             */
            plan: "free" | "hobby" | "pro" | "scale";
            /** @example 256 */
            ram_mb: number;
            max_concurrency: number;
            deployed_apps: number;
            /** Format: int64 */
            included_gb_hours: number;
            app_layer_max_mb: number;
        };
        /** @description Target plan for the change. */
        ChangePlanRequest: {
            /**
             * @example hobby
             * @enum {string}
             */
            plan: "free" | "hobby" | "pro" | "scale";
        };
        /** @description GDPR export bundle: the account itself, every owned app, deployment, build, instance, usage record, domain, cron, API key, and sealed-secret envelope, plus the audit trail. */
        AccountExportResponse: {
            /**
             * Format: date-time
             * @example 2026-07-23T11:25:00Z
             */
            exported_at: string;
            account: components["schemas"]["AccountResponse"];
            apps: components["schemas"]["AppResponse"][];
            deployments: components["schemas"]["DeploymentResponse"][];
            builds: components["schemas"]["BuildExportResponse"][];
            instances: components["schemas"]["InstanceResponse"][];
            usage: components["schemas"]["UsageExportResponse"][];
            domains: components["schemas"]["CustomDomainResponse"][];
            crons: components["schemas"]["CronResponse"][];
            api_keys: components["schemas"]["APIKeyExportResponse"][];
            app_secrets: components["schemas"]["AppSecretExportResponse"][];
            audit_trail?: components["schemas"]["GdprAuditExportResponse"][];
        };
        /** @description Result of staging account deletion: status flips to `deleted_pending` and `restore_until` marks the end of the 30-day grace window. */
        AccountDeletionResponse: {
            /**
             * @example deleted_pending
             * @enum {string}
             */
            status: "deleted_pending";
            /** Format: date-time */
            scheduled_at: string;
            /** Format: date-time */
            restore_until: string;
        };
        /** @description One build record: source artifact bytes, kind (app/function), status, and lifecycle timestamps. */
        BuildExportResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            deployment_id: string;
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
            kind: string;
            /** @example active */
            status: string;
            /** Format: int64 */
            source_bytes: number;
            /** Format: date-time */
            started_at?: string | null;
            /** Format: date-time */
            finished_at?: string | null;
        };
        /** @description One usage record: app id, GB-hours consumed, started/finished timestamps for the export window. cpu_usec is the per-(app, month) cumulative host cgroup CPU-µs (issue #279 / PR-B). Informational only — billing is on mb_seconds. */
        UsageExportResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
            month: string;
            /** Format: int64 */
            mb_seconds: number;
            /** Format: int64 */
            requests: number;
            /**
             * Format: int64
             * @description Cumulative host cgroup CPU-µs consumed by the app in the export window (informational; not billed). issue #279 / PR-B.
             */
            cpu_usec?: number;
            /**
             * Format: int64
             * @description Per-(app, month) HTTP response bytes (informational; not billed). ADR-046. The gateway-side producer lands in PR-2; until then this field stays 0.
             */
            tx_bytes?: number;
            /**
             * Format: int64
             * @description Per-(app, month) byte delta on root-side vethHost.rx_bytes (informational; not billed). ADR-046. Sourced from vmmd netstats.Cache via schedd ListInstanceStats. Includes Ethernet framing — same kernel counter the per-plan tc tbf qdisc reads.
             */
            net_tx_bytes?: number;
            /**
             * Format: int64
             * @description Per-(app, month) byte delta on root-side vethHost.tx_bytes (root→guest = ingress; informational; not billed). ADR-048. Mirror of `net_tx_bytes` for the inbound direction.
             */
            net_rx_bytes?: number;
            /**
             * Format: int64
             * @description Per-(app, month) count of WAKE_RESTORE→WAKE_COLD_BOOT transitions observed (informational; not billed). ADR-048.
             */
            cold_boots?: number;
        };
        /** @description Per-app rollup for one calendar day (informational; not billed). Mirrors the `usage_daily` table populated by the meterd rollup loop (ADR-048 §5, migration 00067). */
        DailyUsageResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
            /** @example 2026-07-29 */
            day: string;
            /**
             * Format: int64
             * @description Cumulative mb_seconds for the day (informational; not billed).
             */
            mb_seconds: number;
            /**
             * Format: int64
             * @description Cumulative request count for the day (informational; not billed).
             */
            requests: number;
            /**
             * Format: int64
             * @description Cumulative host cgroup CPU-µs for the day (informational; not billed). ADR-046 / #279.
             */
            cpu_usec: number;
            /**
             * Format: int64
             * @description HTTP response bytes for the day (informational; not billed). ADR-046.
             */
            tx_bytes: number;
            /**
             * Format: int64
             * @description Cumulative net_tx_bytes for the day (informational; not billed). ADR-046.
             */
            net_tx_bytes: number;
            /**
             * Format: int64
             * @description Cumulative ingress bytes for the day (informational; not billed). ADR-048.
             */
            net_rx_bytes: number;
            /**
             * Format: int64
             * @description Per-day WAKE_RESTORE→WAKE_COLD_BOOT transition count (informational; not billed). ADR-048.
             */
            cold_boots: number;
            /**
             * Format: int64
             * @description Builder VM seconds burned by this app on this day (informational; not billed). ADR-048.
             */
            builder_seconds: number;
        };
        /** @description Page shape for GET /v1/usage/daily — wraps an array of per-app daily rollup rows. */
        DailyUsageListResponse: {
            items: components["schemas"]["DailyUsageResponse"][];
        };
        /** @description Per-app storage rollup for one calendar day (informational; not billed today). Mirrors the `snapshot_storage_daily` table populated by the meterd storage rollup loop (ADR-049 §B.3, migration 00070). */
        StorageUsageResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
            /** @example 2026-07-29 */
            day: string;
            /**
             * Format: int64
             * @description Cumulative snapshot bytes (mem_bytes + disk_bytes) for the day (informational; not billed today). ADR-049 §B.3.
             */
            snapshot_bytes: number;
            /**
             * Format: int64
             * @description Cumulative overlay-staging layer bytes for the day (informational; not billed today). ADR-049 §B.3.
             */
            layer_bytes: number;
        };
        /** @description Page shape for GET /v1/usage/storage — wraps an array of per-app storage rollup rows. */
        StorageUsageListResponse: {
            items: components["schemas"]["StorageUsageResponse"][];
        };
        /**
         * @description Cumulative rekey walk snapshot. Returned by
         *     GET /v1/admin/secrets/rekey-progress and persisted to
         *     FAAS_REKEY_PROGRESS_FILE (default
         *     /var/lib/faas/rekey-progress.json) on every batch tick.
         *     `last_id` is the (account_id, app_id, key) cursor the walk
         *     will resume from on daemon restart.
         */
        RekeyProgress: {
            /**
             * Format: int64
             * @description Running count of rows observed so far. Grows as the walk paginates through (account_id, app_id, key) order.
             */
            total: number;
            /**
             * Format: int64
             * @description Rows successfully unsealed under the previous identity and re-sealed under the current one.
             */
            rekeyed: number;
            /**
             * Format: int64
             * @description Rows already sealed under the current identity (no-op). Seen-set dedupe — does NOT mean the row is unreadable.
             */
            skipped: number;
            /**
             * Format: int64
             * @description Rows where the unseal step threw. A non-zero value warrants operator action (toggle FAAS_REKEY_ENABLED and restart apid to retry).
             */
            failed: number;
            /** @description Resume cursor in (account_id|app_id|key) form. Empty when the walk has just started or finished a clean sweep. */
            last_id?: string;
        };
        /**
         * @description One API key in export form: id, prefix (first 8 chars), label, scopes, created/last-used timestamps, and request count. Scopes is the permission set attached to the key at the moment of export (audit trail; IAM-1, ADR-034 rev2).
         * @example {
         *       "id": "0123456789abcdef0123456789abcdef",
         *       "prefix": "fp_live_ab12cd34",
         *       "label": "ci-deploy",
         *       "scopes": [
         *         "apps:read",
         *         "deploy:write"
         *       ],
         *       "created_at": "2026-07-25T13:25:00Z",
         *       "last_used_at": "2026-07-25T13:25:00Z"
         *     }
         */
        APIKeyExportResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            prefix: string;
            /** @example ci-deploy */
            label?: string | null;
            /**
             * @description Permission set attached to the key. Closed vocabulary (IAM-1, ADR-034 rev2): `admin` is the legacy full-access scope; `apps:read` covers GETs across the apps/deployments/audit/secrets-list surface; `deploy:write` covers POST/PUT/PATCH/DELETE on apps+queues; `secrets:write` covers PUT/DELETE on /apps/{slug}/secrets/{key}; `usage:read` covers GET /v1/usage*.
             * @example [
             *       "admin"
             *     ]
             */
            scopes: ("admin" | "deploy:write" | "secrets:read" | "secrets:write" | "usage:read" | "apps:read" | "env:read" | "env:write")[];
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            last_used_at?: string | null;
        };
        /** @description One row of the customer's audit trail in the GDPR export bundle. Two row kinds live here: source=`gdpr` carries a self-service action (`export`, `delete`, `restore`); source=`event` carries a security event from the events table (IAM-4, ADR-035) with a `kind` and JSON `data` payload. The two are interleaved by timestamp descending. */
        GdprAuditExportResponse: {
            /** @enum {string} */
            source: "gdpr" | "event";
            /** @enum {string} */
            action?: "export" | "delete" | "restore";
            /** Format: date-time */
            requested_at: string;
            /** Format: date-time */
            completed_at?: string | null;
            /** @description Security event kind. Populated only when `source` = `event`. Examples: `auth.login`, `key.created`, `secret.set`, `env.set`, `account.deletion_scheduled`. */
            kind?: string;
            /** @description Kind-specific JSON payload from the events row. Populated only when `source` = `event`. Plaintext values (e.g. secret VALUE) are NEVER carried in `data`. */
            data?: {
                [key: string]: unknown;
            };
        };
        /** @description One sealed secret in export form: key name, sealed ciphertext (sealed at rest per §11), version, and timestamps. Plaintext is never exported. */
        AppSecretExportResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
            /** @example DATABASE_URL */
            key: string;
            /** @description base64-encoded age-sealed envelope */
            ciphertext: string;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        /** @description An app: slug, type, runtime (for functions), RAM/cpu/idle-timeout config, current state, last-deploy pointer, per-app outbound CIDR allowlist (ADR-031 + ADR-032), and reactive scale-up trigger targets (issue #169 / #172). */
        AppResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            /** @example hello-world */
            slug: string;
            /**
             * @example app
             * @enum {string}
             */
            type: "app" | "function";
            /**
             * @description Runtime for `type: function` apps. Omit for `type: app` (the default).
             * @enum {string}
             */
            runtime?: "node22" | "python312" | "go124" | "go124-alpine" | "node24" | "python313";
            /** @example 256 */
            ram_mb: number;
            max_concurrency: number;
            /** @example 80 */
            concurrency_per_vm: number;
            idle_timeout_s?: number | null;
            min_instances: number;
            /** @example active */
            status: string;
            /** Format: uri */
            url: string;
            manifest: components["schemas"]["AppManifest"];
            /** @description Per-app outbound CIDR allowlist (ADR-031 + ADR-032). Each entry is a CIDR string — v4 (`1.2.3.0/24`) or v6 (`2001:db8::/32`). v4-mapped v6 form (`::ffff:1.2.3.0/120`) is silently canonicalised to its v4 form at write time. Empty array means no allowlist rule; the per-netns chain's default-accept policy applies. */
            egress_allowlist?: string[];
            /**
             * @description Per-instance RPS target for the reactive scale-up trigger. 0 = disabled. Hobby/Pro/Scale only. When measured per-instance RPS exceeds this value, schedd admits another instance (up to max_concurrency). See ADR-037.
             * @example 50
             */
            autoscale_target_rps: number;
            /**
             * @description Per-instance CPU% target (1..100) for the reactive scale-up trigger. 0 = disabled. Pro/Scale only. When measured per-instance CPU% exceeds this value, schedd admits another instance (up to max_concurrency). See ADR-037.
             * @example 70
             */
            autoscale_target_cpu_pct: number;
            /**
             * @description Per-app streaming flag (issue #471). Free customers always see this as false; Hobby/Pro/Scale can PATCH it. PR-B activates the streamed response path; PR-A only persists the flag.
             * @example false
             */
            streaming_enabled?: boolean;
            /**
             * @description Per-app raw-bytes Upgrade bridge flag (issue #676 / ADR-080). Default-on for Hobby/Pro/Scale; Free customers always see this as false. PATCH-true on Free is rejected by apid with 403 plan_websocket_not_allowed.
             * @example false
             */
            websocket_enabled?: boolean;
            /**
             * @description Per-app per-route observability flag (ADR-093). When true, gatewayd-internal emits gateway_request_duration_seconds{app,route,class} and serves the bounded reader at GET /v1/apps/{slug}/routes. Default-on for Hobby/Pro/Scale; Free customers always see this as false. PATCH-true on Free is rejected by apid with 403 plan_route_metrics_not_allowed.
             * @example false
             */
            route_metrics_enabled?: boolean;
            /**
             * @description Coarse per-app maintenance toggle (ADR-091 amendment). When true the gatewayd-internal hot-path short-circuits every request to this app with 503 + Retry-After (default 60 s) BEFORE auth, BEFORE wake, BEFORE any kind=maintenance edge rule. Free-tier allowed. Surfaced in the GET /v1/apps/{slug} response so dashboards can show 'maintenance on / off' alongside the streaming/WS pills.
             * @example false
             */
            maintenance_mode?: boolean;
            /** @description Per-app scaling policy (issue #462 / ADR-058). null = legacy row, project the empty-policy shape from min_instances / max_concurrency. Non-null = customer-authored policy persisted to the jsonb column `apps.scaling_policy`. */
            scaling_policy?: null | components["schemas"]["ScalingPolicy"];
            /**
             * Format: date-time
             * @description RFC 3339 timestamp of the most recent scale-out event schedd admitted for this app, or null if the app has never scaled out.
             * @example 2026-07-31T12:00:00Z
             */
            last_scale_out_at?: string | null;
            /**
             * Format: date-time
             * @description RFC 3339 timestamp of the most recent scale-in event schedd reaped for this app, or null if the app has never scaled in.
             * @example 2026-07-31T12:00:00Z
             */
            last_scale_in_at?: string | null;
            /**
             * @description Per-app cosign signature-enforcement flag (issue #472 / ADR-054). When true, OCI image deploys must carry a valid signature from a publisher in the per-app trusted_signers list. Default false.
             * @example false
             */
            require_signed?: boolean;
            /**
             * @description Per-app two-tier snapshot flag (issue #470 / ADR-055). True on Pro/Scale by default; Free/Hobby always false.
             * @example true
             */
            warm_snapshot_enabled?: boolean;
            /**
             * @description Effective per-app request-count threshold for warm-tier capture on this app (issue #470 / ADR-055). Range [1, 100].
             * @example 5
             */
            warm_snapshot_min_requests?: number;
            /**
             * @description Per-app time-since-first-ready threshold for warm-tier capture, milliseconds (issue #470 / ADR-055). Range [100, 60000].
             * @example 2000
             */
            warm_snapshot_min_ms?: number;
            /**
             * @description Per-app eviction tier (issue #475). 'best_effort' (default) keeps the pre-#475 LRU-by-last_request_at reaper behaviour; 'reserved' protects the app from cross-account RAM-pressure eviction.
             * @example best_effort
             * @enum {string}
             */
            eviction_priority?: "best_effort" | "reserved";
            /**
             * @description Per-deployment token-gate flag (issue #560). When true, gatewayd-internal demands `Authorization: Bearer <token>` on every request; cross-account tokens receive 403 insufficient_scope. Pro/Scale only — Free/Hobby PATCH-true is rejected with 403 plan_require_authn_not_allowed.
             * @example false
             */
            require_authn?: boolean;
            /** @description Most-recently parked deployment for this app, or null if never parked (issue #554 / ADR-079 follow-up). The reference surfaces the closed-set parking reason + timestamp on GET /v1/apps/{slug} so operators can answer 'why is my app evicted_cold?' without grepping the audit log. */
            parked_deployment?: components["schemas"]["ParkedDeploymentRef"] | null;
            /**
             * Format: uuid
             * @description Per-app preferred spill target for cross-node pressure rebalance (Tier A10 / ADR-088). Resolved UUID from the customer's named compute_nodes.name preference (null when unset). Consulted by Engine.RebalancePressuredApps before the A9 fallback; falls through to A9 when the target is inactive or full.
             * @example 00000000-0000-0000-0000-000000000000
             */
            overflow_node?: string | null;
            /** @example true */
            cors_default_enabled?: boolean | null;
            /**
             * @example [
             *       "https://*.example.com"
             *     ]
             */
            cors_default_origins?: string[];
            public_auth?: components["schemas"]["PublicAuthStatus"];
            /** Format: date-time */
            auth_default_flipped_at?: string | null;
        };
        /** @description App creation payload: slug, type (app|function), runtime (only for function), RAM MB, max concurrency, idle timeout, and optional manifest. */
        CreateAppRequest: {
            /** @example hello-world */
            slug: string;
            /**
             * @example app
             * @enum {string}
             */
            type?: "app" | "function";
            /** @enum {string} */
            runtime?: "node22" | "python312" | "go124" | "go124-alpine" | "node24" | "python313";
            /** @example 256 */
            ram_mb?: number;
            max_concurrency?: number;
            idle_timeout_s?: number;
            /**
             * @description Per-app streaming flag. Omitted at create-time → apid applies the plan default (issue #471).
             * @example false
             */
            streaming_enabled?: boolean;
            /**
             * @description Per-app raw-bytes Upgrade bridge flag (issue #676 / ADR-080). Omitted → apid applies the plan default; PATCH-true on Free is rejected by apid with 403 plan_websocket_not_allowed.
             * @example false
             */
            websocket_enabled?: boolean;
            /**
             * @description Per-app per-route observability flag (ADR-093). Omitted → apid applies the plan default (Free = false; Hobby/Pro/Scale = true). PATCH-true on Free is rejected by apid with 403 plan_route_metrics_not_allowed.
             * @example false
             */
            route_metrics_enabled?: boolean;
            /**
             * @description Coarse per-app maintenance toggle (ADR-091 amendment). Omitted → apid applies the default (false). Free-tier allowed; no plan gate. Flipping this on at create time pins the app for maintenance from the first request.
             * @example false
             */
            maintenance_mode?: boolean;
            /**
             * @description Per-app two-tier snapshot flag (issue #470 / ADR-055). Omitted at create-time → apid applies the plan default. Free/Hobby PATCH-true is rejected.
             * @example true
             */
            warm_snapshot_enabled?: boolean;
            /**
             * @description Optional create-time override for the warm-tier request-count threshold (issue #470 / ADR-055). Range [1, 100]. Omitted → apid applies the plan default.
             * @example 5
             */
            warm_snapshot_min_requests?: number;
            /**
             * @description Optional create-time override for the warm-tier time-since-first-ready threshold, milliseconds (issue #470 / ADR-055). Range [100, 60000]. Omitted → apid applies the plan default.
             * @example 2000
             */
            warm_snapshot_min_ms?: number;
            /**
             * @description Per-app eviction tier (issue #475). 'best_effort' (default) keeps the pre-#475 LRU-by-last_request_at reaper behaviour; 'reserved' protects the app from cross-account RAM-pressure eviction. Omitted at create-time → apid applies the schema default 'best_effort'.
             * @example best_effort
             * @enum {string}
             */
            eviction_priority?: "best_effort" | "reserved";
            /**
             * @description Per-app preferred spill target for cross-node pressure rebalance (Tier A10 / ADR-088). Wire form is compute_nodes.name (resolved server-side). Omitted → no preference; empty string at create-time is rejected with 422 invalid_overflow_node because the column starts NULL and there is no 'clear' path at create-time.
             * @example box-eu-1
             */
            overflow_node?: string;
            /**
             * @description Per-deployment token-gate flag (issue #560). Omitted at create-time → apid applies the plan default (false). Pro/Scale only.
             * @example false
             */
            require_authn?: boolean;
        };
        /** @description Partial update — every field is optional; omitted fields are unchanged. */
        UpdateAppRequest: {
            /** @example 256 */
            ram_mb?: number | null;
            idle_timeout_s?: number | null;
            max_concurrency?: number | null;
            min_instances?: number | null;
            /** @description v4 or v6 CIDR allowlist; empty array clears to chain-default-accept. */
            egress_allowlist?: string[];
            /**
             * @description Per-instance RPS target for the reactive scale-up trigger. 0 = disable. Hobby/Pro/Scale only. Values < 0 are 422 invalid_autoscale_target_rps.
             * @example 50
             */
            autoscale_target_rps?: number | null;
            /**
             * @description Per-instance CPU% target (1..100, 0 = disable) for the reactive scale-up trigger. Pro/Scale only. Values outside [1, 100] (other than 0) are 422 invalid_autoscale_target_cpu_pct.
             * @example 70
             */
            autoscale_target_cpu_pct?: number | null;
            /**
             * @description Per-app streaming flag (issue #471). Omitted → no change. Free PATCHing true is 403 plan_streaming_not_allowed.
             * @example false
             */
            streaming_enabled?: boolean | null;
            /**
             * @description Per-app raw-bytes Upgrade bridge flag (issue #676 / ADR-080). Omitted → no change. Free PATCHing true is 403 plan_websocket_not_allowed.
             * @example false
             */
            websocket_enabled?: boolean | null;
            /**
             * @description Per-app per-route observability flag (ADR-093). Omitted → no change. Free PATCHing true is 403 plan_route_metrics_not_allowed.
             * @example false
             */
            route_metrics_enabled?: boolean | null;
            /**
             * @description Coarse per-app maintenance toggle (ADR-091 amendment). Omitted → no change. PATCH true pins the app for maintenance (every request 503 + Retry-After); PATCH false restores normal handling. Free-tier allowed; no plan gate. The apps_maintenance_mode_notify trigger (migration 00237) fires pg_notify on flip.
             * @example false
             */
            maintenance_mode?: boolean | null;
            /** @description Per-app scaling policy. Omitted → no change. Non-null → atomic full-overwrite of the jsonb column. */
            scaling_policy?: null | components["schemas"]["ScalingPolicy"];
            /**
             * @description DEPRECATED on this surface. The customer PATCH /v1/apps/{slug} endpoint silently drops require_signed; the operator endpoint PATCH /v1/apps/{slug}/security is the only path that flips the flag (issue #472 / ADR-054). The field is parsed for backwards compatibility but never persisted from this endpoint.
             * @example false
             */
            require_signed?: boolean | null;
            /**
             * @description Per-app two-tier snapshot flag (issue #470 / ADR-055). Omitted → no change. PATCH-true on Free/Hobby is rejected with 403 plan_warm_snapshot_not_allowed.
             * @example true
             */
            warm_snapshot_enabled?: boolean | null;
            /**
             * @description Per-app request-count threshold for warm-tier capture (issue #470 / ADR-055). Range [1, 100]. Omitted → no change.
             * @example 5
             */
            warm_snapshot_min_requests?: number | null;
            /**
             * @description Per-app time-since-first-ready threshold for warm-tier capture, milliseconds (issue #470 / ADR-055). Range [100, 60000]. Omitted → no change.
             * @example 2000
             */
            warm_snapshot_min_ms?: number | null;
            /**
             * @description Per-app eviction tier (issue #475). 'best_effort' (default) keeps the pre-#475 LRU-by-last_request_at reaper behaviour; 'reserved' protects the app from cross-account RAM-pressure eviction (every best_effort candidate is drained before any reserved is parked). Plan-gated upstream: Free PATCH 'reserved' returns 402 plan_eviction_priority_reserved_not_allowed. Per-account cap (Hobby 1, Pro 2, Scale 4): 422 plan_eviction_priority_reserved_quota when exhausted. Omitted → no change.
             * @example best_effort
             * @enum {string|null}
             */
            eviction_priority?: "best_effort" | "reserved" | null;
            /**
             * @description Per-deployment token-gate flag (issue #560). Omitted → no change. PATCH-true on Free/Hobby is rejected with 403 plan_require_authn_not_allowed.
             * @example false
             */
            require_authn?: boolean | null;
            /** @description Per-app public-URL auth configuration (issue #477 / ADR-077). Omitted → no change. When present, mode is the closed enum {open, bearer, basic}; basic_user + basic_pass are required when mode='basic' and the apid seal step encrypts them under the APP_BASIC_AUTH secretbox namespace before persistence. */
            public_auth?: null | components["schemas"]["PublicAuthBlock"];
            /**
             * @description Per-app preferred spill target for cross-node pressure rebalance (Tier A10 / ADR-088). Wire form is the human-readable compute_nodes.name; apid resolves to UUID server-side. Tri-state: omitted → no change; empty string → clear (back to A9 fallback); non-empty → resolve name → UUID via Store.ComputeNodeByName and persist the UUID. 404 on unknown name; 422 on inactive node.
             * @example box-eu-1
             */
            overflow_node?: string | null;
            /** @example true */
            cors_default_enabled?: boolean | null;
            /**
             * @example [
             *       "https://*.example.com"
             *     ]
             */
            cors_default_origins?: string[];
        };
        /** @description Rename payload: new slug. Old slug returns 404 immediately on the next request. */
        RenameAppRequest: {
            /** @example hello-world-v2 */
            new_slug: string;
        };
        /**
         * @description Spend-cap payload (issue #561). *int64 so a missing/null
         *     field round-trips as NULL (no cap). 0 is a valid write and
         *     means "no overage allowed". Negative values are rejected at
         *     the validator before reaching the store (the migration CHECK
         *     at accounts/00049 is the storage-layer enforcement).
         */
        RaiseOverageCapRequest: {
            /** @description Cents-per-month ceiling, or null to clear. */
            overage_cap_cents: number | null;
        };
        /** @description Per-app autoscaling configuration (issue #462 / ADR-058). Mirrors the on-disk jsonb column `apps.scaling_policy`. Empty values map to the engine default (the apid gate is load-bearing for the floor / ceiling, not the encoder). PR-A persists the DTO; PR-C wires the engine; PR-D carves out the worker-class branch. */
        ScalingPolicy: {
            /**
             * @description Per-app cold-wake floor. 0 = scale to zero (default). Hobby+ unlocked at PR-A (was Pro/Scale pre-#462). Free → 403 plan_min_instances_not_allowed.
             * @example 1
             */
            min_instances?: number;
            /**
             * @description Per-app ceiling on live instances. Must be in [min_instances, plan.MaxConcurrency]. Hobby+ unlocked at PR-A. Free → 403 plan_max_instances_not_allowed. 0 = use plan max_concurrency.
             * @example 5
             */
            max_instances?: number;
            /** @description Per-instance signal the engine watches for the scale-up trigger. Closed metric set: rps | concurrent_requests | p99_latency_ms. Empty/null = engine falls back to the legacy autoscale_target_rps / autoscale_target_cpu_pct columns. Worker-class apps reject concurrent_requests with 422 scaling_target_incompatible_with_workload_class (PR-D carve-out). */
            target?: null | components["schemas"]["ScalingTarget"];
            /**
             * @description Minimum seconds between two scale-out events. Floor 1 (no 0 traps); ceiling 3600 (1 h). Out-of-range → 422 invalid_cooldown.
             * @example 5
             */
            scale_out_cooldown_s?: number;
            /**
             * @description Minimum seconds between two scale-in events. Floor 5 (matches the reaper's 5 s idle window); ceiling 86400 (1 day). Out-of-range → 422 invalid_cooldown.
             * @example 60
             */
            scale_in_cooldown_s?: number;
        };
        /** @description Per-app public-URL auth write shape (issue #477 / ADR-077). Sent on PATCH /v1/apps/{slug}; apid seals the basic_user + basic_pass into a single APP_BASIC_AUTH secretbox blob before persistence. The plaintext is never echoed on read (see PublicAuthStatus). */
        PublicAuthBlock: {
            /**
             * @description Auth mode (closed set). 'open' is the pre-#477 default (every request passes). 'bearer' requires Authorization: Bearer (Hobby+; 402 on Free). 'basic' requires HTTP Basic auth with sealed credentials (Pro+; 402 on Free/Hobby). Unknown values → 422 invalid_public_auth_mode.
             * @example basic
             * @enum {string}
             */
            mode: "open" | "bearer" | "basic";
            /**
             * @description Basic-auth username (RFC 7617 §2). Plaintext at PATCH time; sealed into apps.public_auth_basic under the APP_BASIC_AUTH secretbox namespace. Required when mode='basic'; ignored otherwise. Range [1, 128] bytes after TrimSpace.
             * @example editor
             */
            basic_user?: string;
            /**
             * Format: password
             * @description Basic-auth password (RFC 7617 §2). Plaintext at PATCH time; sealed alongside basic_user under the APP_BASIC_AUTH secretbox namespace. Required when mode='basic'; ignored otherwise. Range [1, 256] bytes.
             * @example hunter2
             */
            basic_pass?: string;
        };
        /** @description Read-only per-app public-URL auth shape on AppResponse (issue #477 / ADR-077). Mirrors the row contents without the plaintext credentials. The redaction posture is a load-bearing invariant — see ADR-077 §Decision 're-redaction invariant': neither basic_user nor basic_pass is EVER returned on the wire, even when mode='basic'. To rotate credentials, the customer PATCHes a fresh public_auth block. */
        PublicAuthStatus: {
            /**
             * @description Active auth mode. One of 'open', 'bearer', 'basic'. Matches apps.public_auth_mode on disk; a PATCH 'open' cleared any prior sealed blob so a stale secretbox row never reaches a fresh request.
             * @example basic
             * @enum {string}
             */
            mode: "open" | "bearer" | "basic";
            /**
             * @description True iff the row carries a non-null apps.public_auth_basic blob (i.e. mode='basic' with credentials). A mode='basic' row without creds would 401 every request — has_basic_creds is the operator-greppable signal that the seal succeeded.
             * @example true
             */
            has_basic_creds: boolean;
        };
        /** @description (metric, value) pair the engine watches for the scale-up trigger. The metric surface is closed; the unset state (null) is the legacy 'engine falls back to autoscale_target_rps' path. */
        ScalingTarget: {
            /**
             * @example rps
             * @enum {string}
             */
            metric?: "rps" | "concurrent_requests" | "p99_latency_ms";
            /**
             * @description Target value (units depend on Metric). Must be >= 0.
             * @example 1
             */
            value?: number;
        };
        /** @description App manifest: environment variables, build commands, working directory, healthcheck, user, and Dockerfile-as-source flag (§ux 6.3). The optional `env_secrets` field carries sealed-secret refs ("secret:NAME" strings) resolved by the host at wake time against the app_secrets table (issue #460 / ADR-053 §Decision 1). Values are NEVER sealed ciphertext — only refs. */
        AppManifest: {
            entrypoint: string[];
            env?: {
                [key: string]: string;
            };
            /** @description Env override via sealed-secret refs. Each value is "secret:NAME"; the host resolver looks up NAME against the app_secrets table at wake. */
            env_secrets?: {
                [key: string]: string;
            };
            working_dir?: string | null;
            port?: number | null;
            healthz?: string | null;
            user?: string | null;
        };
        /** @description Reference to a deployment that was parked (issue #554 / ADR-079 follow-up). Returned in AppResponse.parked_deployment when the app has at least one parked deployment. The `parked_reason` field is closed-set (liveness_exhausted | lifecycle_park | admin_park) — enforced at the schema layer via the deployments_parked_reason_check constraint from migration 00157. */
        ParkedDeploymentRef: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            /**
             * @description Closed-set parking reason from the schema-layer CHECK constraint.
             * @enum {string}
             */
            parked_reason: "liveness_exhausted" | "lifecycle_park" | "admin_park";
            /**
             * Format: date-time
             * @description Wall-clock timestamp the deployment was parked (set once, idempotent across schedd restart cycles).
             */
            parked_at: string;
        };
        /** @description One deployment: id, app, source ref, build status, commit SHA, and lifecycle timestamps. The optional `has_overrides` and `override_*` fields are the persisted echo of the create-time overrides object (issue #460 / ADR-053); they round-trip via `GET /v1/apps/{slug}/deployments/{id}` so a customer can audit what their last deploy pinned. Env values are NEVER echoed — only the keys (`override_env_keys`); env_secrets refs ARE echoed because the ref shape is non-secret by design. */
        DeploymentResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
            build_id?: string | null;
            image_digest: string;
            kind: string;
            /** @example active */
            status: string;
            error?: string | null;
            error_code?: string | null;
            /** Format: date-time */
            created_at: string;
            /** @description True when this deployment carries a non-null override_* column set. */
            has_overrides?: boolean;
            /** @description Entrypoint override echoed verbatim from the create request. nil when no override was supplied. */
            override_entrypoint?: string[];
            /** @description Cmd override echoed verbatim from the create request. */
            override_cmd?: string[];
            /** @description Sorted set of env-var keys set by the env override. VALUES ARE NEVER ECHOED (ADR-053 §Decision 4). */
            override_env_keys?: string[];
            /** @description Sorted set of env-var keys set by the env_secrets override. The parallel refs are echoed in `override_env_secret_refs` because the ref shape is non-secret by design. */
            override_env_secret_keys?: string[];
            /** @description Verbatim `secret:NAME` ref map; the customer needs to see which secret they bound to which env var to debug a misconfigured deploy. */
            override_env_secret_refs?: {
                [key: string]: string;
            };
            /** @description Listen-port override; 0 = absent (fall back to image default). */
            override_port?: number;
            /** @description Readiness-probe override. Persisted verbatim; the actual HTTP probe is a follow-up — today waitReady stays a bare TCP accept. */
            override_healthcheck?: components["schemas"]["DeploymentHealthcheck"] | null;
            /** @description Liveness-probe override echoed verbatim (issue #554 / ADR-078). nil when the deployment used the per-plan default (Hobby/Pro/Scale → 5s / 3 consecutive / 60s cooldown). Echoed on GET /v1/apps/{slug}/deployments/{id} so the customer can audit which probe the host (cmd/vmmd) is running against the VM. */
            override_liveness_probe?: components["schemas"]["DeploymentLivenessProbe"] | null;
            /** @description Per-deployment cold-wake floor override (issue #557 closure / ADR-072). 0 = inherit from parent app (default); positive value is the deployment's own floor. Effective per-instance floor = max(app.EffectiveMinInstances(), d.EffectiveMinInstances()). Validated against the parent app's plan MaxMinInstances cap on PATCH. */
            min_instances?: number;
            /** @description Per-deploy grype CVE scan surface (issue #464 / ADR-055). nil on pre-feature rows (the migration backfilled scan_status='skipped' + scan_result={reason: 'pre-feature'} on those; the apid read path returns nil so the dashboard / CLI see a clean absence — the /scan route surfaces the 'skipped' sentinel for those rows). Non-nil for post-feature rows in any of the {pending, complete, failed, skipped} states. The customer can deploy a CRITICAL-CVE image; the dashboard shows it; that is the contract (no enforcement at the deploy gate). */
            scan?: components["schemas"]["ScanResult"] | null;
            /**
             * @description Per-deployment parking reason (issue #554 / ADR-079 follow-up, migration 00157). Closed-set vocabulary enforced at the schema layer via the deployments_parked_reason_check constraint. nil for never-parked deployments — surfaced as no field on the wire via omitempty.
             * @enum {string|null}
             */
            parked_reason?: null | "liveness_exhausted" | "lifecycle_park" | "admin_park";
            /**
             * Format: date-time
             * @description Wall-clock timestamp the deployment was parked (set once, idempotent across schedd restart cycles). nil for never-parked deployments.
             */
            parked_at?: string | null;
            /**
             * @description Per-deployment traffic-split weight (issue #556 PR-A). Summed across live rows for the app = 100 by construction.
             * @example 100
             */
            traffic_percent?: number;
            /**
             * @description Per-deployment env scope (ADR-091 / PR-D). Lowercase alnum + dash, 3..40 chars, no leading/trailing dash. nil/omitted = `default`.
             * @example staging
             */
            scope?: string | null;
            /**
             * @description Per-deploy secret-scan audit row (PR-A / ADR-101). Mirrors
             *     the `scan` field shape — absent when the row has not been
             *     scanned yet (deploy mid-pipeline or pre-PR-A), present
             *     with `findings=[]` for a clean walk, present with
             *     one-or-more entries for a hit. Read by the dashboard's
             *     "secret scan" card and the CLI's `--show-secret-scan`
             *     flag. Stamped both for the imaged-side layer walk (main
             *     image + each sidecar; post-build, loud-fail on any
             *     finding) and — forward-compat — for the apid-side
             *     source-tree 422 path. Status closed-set the writer
             *     stamps: "complete" (clean) | "complete_with_redactions"
             *     (hit). The `image_digest` sub-field records which OCI
             *     digest the imaged walk ran against; null on legacy
             *     pre-PR-A rows. See `pkg/imaged/secretscan.go`.
             */
            secret_scan?: components["schemas"]["SecretScanResult"] | null;
        };
        /** @description Body for PATCH /v1/deployments/{id} (issue #557 closure / ADR-072). The only mutable field post-create is the per-deployment cold-wake floor; image / digest / overrides / sidecars stay immutable. */
        UpdateDeploymentRequest: {
            /** @description Per-deployment cold-wake floor override for PATCH /v1/deployments/{id}. 0 = inherit from parent app; positive value is the deployment's own floor. Effective per-instance floor = max(app, deployment). Validated against the parent app's plan MaxMinInstances cap. */
            min_instances: number;
        };
        /** @description Body for PATCH /v1/deployments/{id}/traffic (issue #556 PR-A). Sets the per-deployment traffic-split weight (integer [0, 100]). PR-A uses the zero-siblings rebalance form: setting row R's traffic_percent to N forces every other live row in the same app to 0, keeping Σ = 100 by construction. Pro/Scale only — Free/Hobby are rejected at 403 plan_traffic_split_not_allowed. */
        UpdateDeploymentTrafficRequest: {
            /**
             * @description Per-deployment traffic-split weight. 0 = no traffic (used during rollback). 100 = sole live deployment.
             * @example 25
             */
            traffic_percent: number;
        };
        /** @description Paginated deployment list with a `next_before` cursor for backward pagination. */
        DeploymentListResponse: {
            items: components["schemas"]["DeploymentResponse"][];
            /** Format: date-time */
            next_before?: string | null;
        };
        /**
         * @description One persisted billing-provider invoice (issue #259). Money is
         *     integer cents in the provider's currency; EUR is the platform
         *     currency today, the field is preserved per-row so multi-currency
         *     support can land without a backfill. The PDF availability flag
         *     is the only PDF surface we expose — the hosted PDF URL is
         *     provider-scoped and the customer fetches it from the
         *     Stripe/Paddle portal, not via this API. The hosted URL itself
         *     is not on the wire; the column exists in invoices.hosted_url
         *     for PR-B audit only.
         */
        Invoice: {
            /** Format: uuid */
            id: string;
            /** @enum {string} */
            provider: "stripe" | "paddle";
            /** @example in_1NxAbC2eZvKYlo2C0D4E5F6G */
            provider_invoice_id: string;
            /** @example FaaS-2026-007 */
            number?: string;
            /** @enum {string} */
            status: "draft" | "open" | "paid" | "uncollectible" | "void";
            /** Format: date-time */
            period_start: string;
            /** Format: date-time */
            period_end: string;
            /** Format: int64 */
            subtotal_cents: number;
            /** Format: int64 */
            tax_cents: number;
            /** Format: int64 */
            total_cents: number;
            /** Format: int64 */
            amount_paid_cents: number;
            /**
             * @example eur
             * @enum {string}
             */
            currency: "eur";
            pdf_available: boolean;
            /** Format: date-time */
            created_at: string;
        };
        /**
         * @description Paginated invoice list. Items is one page (period_end DESC, id
         *     DESC order). next_before is the cursor the caller passes on the
         *     next request to fetch the older page. Empty Items with 200 OK
         *     is the empty-history shape — never 404.
         */
        InvoiceListResponse: {
            items: components["schemas"]["Invoice"][];
            /** Format: date-time */
            next_before?: string | null;
        };
        /**
         * @description Operator-configured billing portal URL (issue #253) plus the
         *     card-on-file summary (issue #242). The url field is omitted
         *     when the box has FAAS_BILLING_PORTAL_URL unset — that is the
         *     "absent" sentinel; the CLI branches on it to print a friendly
         *     hint instead of opening the browser to "". The payment_method
         *     field is omitted when the account has no card on file (Free
         *     plan, or post-checkout before the first paid cycle settles).
         */
        BillingPortalResponse: {
            /**
             * Format: uri
             * @description Substituted portal URL; absent when the box has no portal configured.
             */
            url?: string | null;
            /**
             * @description Card-on-file summary. Omitted when the account has no
             *     payment method on file (Free / never-checked-out). The
             *     CLI's `faas billing payment-method` and the dashboard's
             *     billing page render from this field.
             */
            payment_method?: components["schemas"]["PaymentMethodSummary"];
        };
        /**
         * @description Card-on-file summary (issue #242). Provider-agnostic shape —
         *     the same wire shape is returned whether the operator runs
         *     Stripe or Paddle. brand is the lowercase network label
         *     ("visa", "mastercard", "amex"); last4 is the last-4 of the
         *     PAN (no full PAN, no PCI surface); exp_month / exp_year are
         *     integer card-face expiry fields.
         */
        PaymentMethodSummary: {
            /**
             * @description Card network (lowercase).
             * @example visa
             */
            brand: string;
            /**
             * @description Last 4 digits of the PAN.
             * @example 4242
             */
            last4: string;
            /**
             * @description Card expiry month (1-12).
             * @example 12
             */
            exp_month: number;
            /**
             * @description Card expiry year.
             * @example 2027
             */
            exp_year: number;
        };
        /**
         * @description Returned by POST /v1/billing/retry (issue #242). The CLI
         *     prints attempt_id and provider_ref_id so the customer can
         *     quote them to support if the charge still fails. status is
         *     "pending_provider_confirmation" — the CLI does not poll for
         *     a settlement flip; that flip arrives via the provider's
         *     webhook (EventPaymentSucceeded) and is rendered by the
         *     dashboard / dunning email pipeline.
         */
        BillingRetryResponse: {
            /** @description apId-side attempt handle (Stripe: in_…, Paddle: txn_…-related). */
            attempt_id: string;
            /** @description Provider-side handle for the new attempt (Stripe: pi_…, Paddle: tx_…). */
            provider_ref_id: string;
            /** @description Always "pending_provider_confirmation" today; reserved for future settlement states. */
            status: string;
            /**
             * Format: date-time
             * @description Next scheduled billing instant after the retry settles; null when not known yet.
             */
            next_billing_at?: string | null;
        };
        /**
         * @description Returned by POST /v1/billing/cancel (issue #242). The CLI
         *     renders effective_at as "your apps will stop on <date>".
         *     cancel_scheduled is always true on 200; the 409 path returns
         *     a Problem with a friendly "already cancelled" hint so a
         *     re-cancel idempotency click does not surface as a server error.
         */
        BillingCancelResponse: {
            /** @description Always true; the absence of an active subscription returns 409 instead. */
            cancel_scheduled: boolean;
            /**
             * Format: date-time
             * @description RFC 3339 instant at which the subscription terminates (Stripe: current_period_end; Paddle: next month-rollover).
             */
            effective_at: string;
        };
        /**
         * @description Returned by POST /v1/admin/billing-reconcile/{id} (PR-P3).
         *     mb_seconds is the integer total the provider SDK returned
         *     for the rolling 30-day window [start, end). Operators can
         *     diff the SDK-side number against the local usage_minutes
         *     sum to spot drift between the platform's billing source
         *     of truth and the provider's ledger.
         */
        BillingReconcileResponse: {
            /** Format: uuid */
            account_id: string;
            /**
             * Format: date-time
             * @description Window start (RFC 3339, UTC).
             */
            start: string;
            /**
             * Format: date-time
             * @description Window end (RFC 3339, UTC).
             */
            end: string;
            /** @description Integer mb_seconds total for the window. */
            mb_seconds: number;
        };
        /**
         * @description One row in the Paddle price + product catalog (PR-P3).
         *     Plan values are the billable api.Plan constants
         *     ("hobby", "pro", "scale") — PlanFree is intentionally
         *     absent because it carries no recurring line item.
         *     Handle is the Paddle-side id (pri_… for monthly / overage,
         *     pro_… for product). SyncedAt is RFC 3339 UTC from the
         *     catalog's lastSyncAt.
         */
        BillingCatalogEntry: {
            /** @enum {string} */
            plan: "hobby" | "pro" | "scale";
            /** @enum {string} */
            kind: "monthly" | "overage" | "product";
            /** @description Paddle price (pri_…) or product (pro_…) ID. */
            handle: string;
            /** Format: date-time */
            synced_at: string;
        };
        /**
         * @description Wire shape for GET / POST / DELETE
         *     /v1/admin/billing-paddle-catalog (PR-P3). Provider is
         *     the active billing provider's name (paddle / stripe);
         *     on a Stripe deployment the handler 501s before
         *     serializing this struct. SyncedAt is the timestamp of
         *     the most recent successful EnsurePlanProducts call;
         *     empty string when no hydration has yet completed
         *     (POST and DELETE both reset it).
         */
        BillingCatalogResponse: {
            /** @description Active provider name (paddle / stripe). */
            provider: string;
            /** @description RFC 3339 last-sync timestamp; empty string when never synced. */
            synced_at: string;
            entries: components["schemas"]["BillingCatalogEntry"][];
        };
        /**
         * @description Wire shape for GET /v1/admin/billing-paddle-overage/preflight
         *     (B4 / Tier 1 follow-up to PR #802). Operator-side guard that
         *     probes information_schema.columns for the four migration-00041
         *     columns + counts the per-state rows in a single round-trip.
         *
         *     table_exists is false when the paddle_overage_dedupe table is
         *     entirely absent (migrations 00034 + 00041 both unapplied).
         *     has_window_start / has_state / has_claimed_at / has_claimed_by
         *     each correspond to one of the four columns added by
         *     migration 00041; all four must be true for the meterd
         *     overage pusher to land. pending_rows / completed_rows are
         *     the per-state row totals (a single SQL filter pair).
         */
        BillingPaddleOveragePreflightResponse: {
            /** @description true if the paddle_overage_dedupe table is present (00034+). */
            table_exists: boolean;
            /** @description true if the migration-00041 window_start column exists. */
            has_window_start: boolean;
            /** @description true if the migration-00041 state column exists. */
            has_state: boolean;
            /** @description true if the migration-00041 claimed_at column exists. */
            has_claimed_at: boolean;
            /** @description true if the migration-00041 claimed_by column exists. */
            has_claimed_by: boolean;
            /** @description count(*) where state = pending. */
            pending_rows: number;
            /** @description count(*) where state = completed. */
            completed_rows: number;
        };
        /**
         * @description ADR-038 / Tier 3 / issue #197 B3.10-read half: the
         *     post-mortem "what ran?" record for a single successful build.
         *     Field names mirror the `build_provenance` table columns. Empty
         *     strings indicate a column the populator hasn't filled yet —
         *     `buildkit_version`, `railpack_version`, `base_digest`,
         *     `runner_digest`, and `sbom_storage_key` are populated by
         *     Phase 3 (cosign signer + syft SBOM), but the columns exist
         *     today so Phase 3 is a zero-cost schema change.
         *     `framework_version` is the language version the customer's
         *     source declares (`.nvmrc`, `package.json::engines.node`,
         *     `pyproject.toml::requires-python`, `.python-version`,
         *     `.tool-versions`, `go.mod` directive); added in issue #740 /
         *     DEPLOY-PROV-5 / ADR-087. OBSERVATIONAL ONLY — the build
         *     pipeline never reads it (the runtime is bound by the OCI
         *     base ref via `FAAS_DEPLOY_BASE_REF_<RUNTIME>`).
         */
        BuildProvenanceResponse: {
            id: string;
            build_id: string;
            buildkit_version?: string;
            railpack_version?: string;
            base_digest?: string;
            /** @description sha256 of the customer's source tarball (the cache lookup key). */
            source_sha256: string;
            source_url?: string;
            commit_sha?: string;
            /** @description free / hobby / pro / scale — copied from the account at claim time. */
            plan: string;
            runner_digest?: string;
            /** @description compute_node name (default `default-local` on the one-box). */
            builder_node_id: string;
            /** Format: date-time */
            started_at: string;
            /** Format: date-time */
            finished_at: string;
            /** @description Phase 3 populator fills this from `syft` output. Empty string when not yet populated. */
            sbom_storage_key?: string | null;
            /** @description Source-declared language version (nodes 22.11.0 / python 3.13 / go 1.24, etc.). Empty string when no version file is present or any parser fails — best-effort, never an error. Added in DEPLOY-PROV-5 / issue #740 / ADR-087. */
            framework_version?: string | null;
        };
        /**
         * @description DEPLOY-PROV-6 / ADR-089 (issue #741): the LIFECYCLE row for
         *     a single build — current status, enqueued/started/finished
         *     timestamps, failure_class, server-computed duration_seconds.
         *     Companion to BuildProvenanceResponse (post-mortem export,
         *     ADR-038) and the /sbom route (post-mortem blob, ADR-038
         *     Phase 3). The status field mirrors builds.status — a
         *     4-state enum `queued|running|succeeded|failed` per the
         *     `builds_status_check` CHECK constraint. 'cancelled' is
         *     intentionally absent (ADR-089 §1).
         *
         *     failure_class is the low-cardinality enum
         *     `oom|timeout|user_error|infra` per the
         *     `builds_failure_class_check` CHECK; present only when
         *     status='failed'.
         *
         *     duration_seconds is server-computed (FinishedAt − StartedAt)
         *     only when BOTH timestamps are populated; absent otherwise
         *     (so a queued/running build stays minimal). CI scripts can
         *     rely on its presence as "the build reached a terminal state
         *     and elapsed N wall-clock seconds." error_message is
         *     intentionally NOT in this response — it lives on
         *     deployments; clients that need the per-failure string call
         *     GET /v1/deployments/{id}.
         */
        BuildResponse: {
            id: string;
            deployment_id: string;
            /** @enum {string} */
            kind: "railpack" | "dockerfile" | "tarball" | "github";
            /** Format: int64 */
            source_bytes: number;
            /** @enum {string} */
            status: "queued" | "running" | "succeeded" | "failed";
            /** @enum {string} */
            failure_class?: "oom" | "timeout" | "user_error" | "infra";
            log_path?: string;
            /** Format: date-time */
            enqueued_at: string;
            /** Format: date-time */
            started_at?: string;
            /** Format: date-time */
            finished_at?: string;
            /** @description Server-computed FinishedAt − StartedAt in whole seconds. Absent until the build reaches a terminal state. */
            duration_seconds?: number;
        };
        /**
         * @description DEPLOY-PROV-6 follow-up / ADR-091 (issue #741 close-out):
         *     the page shape for GET /v1/builds. Items is one page of
         *     builds ordered started_at DESC NULLS LAST — queued builds
         *     (started_at IS NULL) sink to the bottom of the first page
         *     per the new index builds_deployment_started_idx. The
         *     next_before cursor is the opaque tuple
         *     `<rfc3339nano>|<id_hex>` of the LAST row on this page
         *     (post-review fix; code review surfaced issues 1+2 that
         *     the id tiebreaker resolves). The started_at segment is
         *     empty for queued-tail pages (the cursor becomes a pure
         *     id anchor in the NULL zone). An empty next_before
         *     signals end-of-list. Round-trip the cursor verbatim on
         *     subsequent requests; do NOT re-parse or re-encode.
         *
         *     Mirrors DeploymentListResponse in shape only. The new
         *     index and the sibling ListBuildsForAccountPaged method
         *     (state layer) make this O(page-size) regardless of
         *     account size; the unlimited
         *     ListBuildsForAccount(ctx, accountID) stays intact for the
         *     GDPR export at cmd/apid/handlers_account.go.
         */
        BuildListResponse: {
            items: components["schemas"]["BuildResponse"][];
            /**
             * @description Opaque cursor for the next page. Empty = end of list.
             *     Wire format: `<rfc3339nano>|<id_hex>` (pipe-separated).
             *     The id is the Build.ID of the LAST row on this page;
             *     the started_at segment is RFC3339Nano (sub-second
             *     preserved) for non-queued rows, or empty for queued
             *     rows (the cursor then keys on id alone in the NULL
             *     zone). Round-trip verbatim — server-parsed, no client
             *     re-encoding.
             */
            next_before?: string;
        };
        /**
         * @description One row in the account_credits table (issue #279). cents_remaining
         *     is the integer-cents balance still available; consumption
         *     decrements it (the consumption reducer lands in a follow-up
         *     PR). expires_at is RFC 3339 when set; absent (or null) means
         *     the credit is valid until fully consumed. reason is the
         *     operator-supplied audit text (3..500 chars).
         */
        AccountCreditResponse: {
            /** Format: uuid */
            id: string;
            /** Format: uuid */
            account_id: string;
            cents_remaining: number;
            reason: string;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            expires_at?: string | null;
        };
        /**
         * @description Response shape for POST /v1/admin/github-webhook-secrets
         *     (PR-D / ADR-012 §7 amendment). upgraded_at is the
         *     post-upsert stamp — every successful call bumps it
         *     (the audit trail; an operator re-running with the same
         *     secret is itself a rotation event worth recording).
         */
        AdminSetGithubWebhookSecretResponse: {
            /** Format: int64 */
            installation_id: number;
            /** Format: date-time */
            upgraded_at: string;
            /** @description admin:<account_id> or platform */
            upgraded_by: string;
        };
        /**
         * @description Body shape for POST /v1/admin/github-webhook-secrets
         *     (PR-D / ADR-012 §7 amendment). The CLI takes hex so the
         *     plaintext never has to be a binary argv value; the apid
         *     handler hex-decodes before the INSERT.
         */
        AdminSetGithubWebhookSecretRequest: {
            /**
             * Format: int64
             * @description GitHub App installation_id (positive bigint).
             */
            installation_id: number;
            /** @description Hex-encoded secret (16..64 bytes; 32..128 hex chars). */
            secret_hex: string;
        };
        /**
         * @description Returned by POST /v1/invoices/{id}/consume-credits (issue
         *     #279 PR-C). consumed_cents is the floored integer cents of
         *     overage drained against this invoice. remaining_credits_cents
         *     is the sum of cents_remaining across the account's active
         *     credits after the call. already_consumed_for_invoice is true
         *     on idempotent replays (the reducer returns the same
         *     consumed_cents without double-decrementing). per_credit lists
         *     FIFO-ordered credit drains with their post-decrement balance.
         *     Money is integer cents (CLAUDE.md).
         */
        ConsumeInvoiceResponse: {
            /** Format: uuid */
            invoice_id: string;
            consumed_cents: number;
            remaining_credits_cents: number;
            already_consumed_for_invoice: boolean;
            per_credit: components["schemas"]["ConsumedCreditRow"][];
        };
        /**
         * @description One FIFO drain row inside ConsumeInvoiceResponse.per_credit.
         *     delta_cents is the negative-cents decrement applied to the
         *     credit; new_balance is cents_remaining after the call.
         */
        ConsumedCreditRow: {
            /** Format: uuid */
            credit_id: string;
            /** @description Negative integer cents applied to the credit. */
            delta_cents: number;
            new_balance: number;
        };
        /** @description Two content-types accepted (see operation description): prebuilt OCI image reference, or multipart source upload. The optional `overrides` object (issue #460 / ADR-053) lets a customer redeploy the same digest-pinned image with a different entrypoint / cmd / env / env_secrets / port / healthcheck without rebuilding the image. The override field list is FROZEN — six fields, no more — and any extra field on the override object 400s the request (the handler's decoder rejects unknown keys; see ADR-053 §Decision 1). The optional `sidecars` array (issue #463 / ADR-068) attaches up to 2 stateless sidecars (1 init + 1 sidecar) per app — a one-shot DB migrator as `init`, a metrics scraper as `sidecar`. nil/omitted = no sidecars. */
        CreateDeploymentRequest: {
            /** @description registry.gregale.dev/...@sha256:... — digest-pinned OCI reference. */
            image?: string;
            /** @description Deploy-time overrides (entrypoint, cmd, env, env_secrets, port, healthcheck). nil/omitted = deploy the image as-is. */
            overrides?: components["schemas"]["CreateDeploymentOverrides"] | null;
            /**
             * @description Per-deploy signature-enforcement opt-in (issue #472 / ADR-054). nil = inherit apps.require_signed; *true is a no-op when the app flag is already on; *false is rejected with 403 deploy_signature_invalid when the app flag is on (operator policy wins).
             * @example true
             */
            require_signed?: boolean | null;
            /** @description Up to 2 stateless sidecars (1 init + 1 sidecar). nil/omitted = no sidecars. See ADR-068 for the hard 2-cap and stateless-only contract. */
            sidecars?: components["schemas"]["Sidecar"][];
            /**
             * @description Per-deployment traffic-split weight (issue #556 PR-A). nil = server default 100; explicit 0..100 = opt into canary (Pro/Scale only).
             * @example 10
             */
            traffic_percent?: number | null;
            /**
             * @description Top-level per-deployment env scope (ADR-091 / PR-D). Lowercase alnum + dash, 3..40 chars, no leading/trailing dash. nil/omitted = `default`.
             * @example prod
             */
            scope?: string | null;
        };
        /**
         * @description Fargate-shaped deploy-time override object on `POST /v1/apps/{slug}/deployments`
         *     (issue #460 / ADR-053). Field list is FROZEN — six fields, no more. Any extra
         *     field on this object 400s the request. ADR-053 §Decision 1 documents the freeze;
         *     the handler enforces it via `DisallowUnknownFields` on the JSON decoder.
         *
         *     - `entrypoint` replaces the OCI image's ENTRYPOINT/CMD argv at exec time.
         *     - `cmd` is appended to `entrypoint` (mirrors the OCI runtime contract).
         *     - `env` is plaintext; env-var keys must match `^[A-Z][A-Z0-9_]*$`; per-value
         *       byte cap = plan `EnvValueMaxBytes`.
         *     - `env_secrets` carries `secret:NAME` REFS — the runtime resolver (PR-B)
         *       strips the prefix and looks up `NAME` against the existing `app_secrets`
         *       table. The ref name must match `^[A-Z][A-Z0-9_]*$`.
         *     - `env` + `env_secrets` share the plan `EnvVarsMax` quota — no bypass by
         *       mixing the two surfaces.
         *     - `port` is per-deployment (1..65535; 0 = absent / fall back to image default).
         *     - `healthcheck` is the readiness-probe shape; the actual HTTP probe ships
         *       in a follow-up ADR.
         */
        CreateDeploymentOverrides: {
            /** @description Replaces the OCI image's ENTRYPOINT/CMD argv. Each element must be non-empty. */
            entrypoint?: string[];
            /** @description Appended to entrypoint at exec time (OCI runtime contract). Each element must be non-empty. */
            cmd?: string[];
            /** @description Plaintext env map applied at boot. Keys: `^[A-Z][A-Z0-9_]*$`. Per-value byte cap = plan EnvValueMaxBytes. */
            env?: {
                [key: string]: string;
            };
            /** @description Sealed-secret-ref env map. Each VALUE is `secret:NAME`; the runtime resolver looks up `NAME` against `app_secrets` at wake. Counts against the shared `EnvVarsMax` cap with `env`. */
            env_secrets?: {
                [key: string]: string;
            };
            /** @description Listen port; 0 = absent / fall back to image default (today 8080). */
            port?: number;
            /** @description Readiness-probe shape. Persisted today; the HTTP probe variant ships in a follow-up ADR. */
            healthcheck?: components["schemas"]["DeploymentHealthcheck"] | null;
            /**
             * @description Liveness-probe override (issue #554 / ADR-078). The host (cmd/vmmd)
             *     polls the guest's vsock 1028 STREAM on every `interval_s`; after
             *     `consecutive_failures` consecutive non-2xx (or timeout / conn-refused)
             *     responses, the VM is destroyed and schedd cold-boots it from rootfs
             *     (per ADR-005 — never snapshot-restore). 3 restarts in 300 s
             *     park the deployment.
             *
             *     Per-plan gates (issue #554): Free is locked out (Plan.LivenessAllowed()
             *     returns false; the apid handler rejects with
             *     `plan_liveness_probe_not_allowed` BEFORE the DB is touched); Hobby,
             *     Pro, Scale inherit the 5 s / 3 consecutive / 60 s cooldown / 3 in 300 s
             *     defaults. v1 is HTTP-only; gRPC health checks are deferred to v2.
             */
            liveness_probe?: components["schemas"]["DeploymentLivenessProbe"] | null;
            /**
             * @description Override-object per-deployment env scope (ADR-091 / PR-D). Lowercase alnum + dash, 3..40 chars, no leading/trailing dash. nil/omitted = inherit top-level scope or `default`.
             * @example staging
             */
            scope?: string | null;
        };
        /**
         * @description Readiness-probe shape on the deploy-time override object (issue #460 /
         *     ADR-053). Today the probe stays a bare TCP accept — `path`, `interval_s`,
         *     `timeout_s`, `retries` are persisted but not yet exercised by `vmm.waitReady`.
         *
         *     Validation rules (enforced in `pkg/api/dto.go::CreateDeploymentOverrides.Validate`):
         *     - `path` must start with `/`.
         *     - `interval_s`, `timeout_s`, `retries` must be `>= 0`.
         *     - Missing fields default to 0 (interpreted as "use image default" by the
         *       future probe implementation).
         */
        DeploymentHealthcheck: {
            /** @description Path the probe requests from the guest; must start with `/` (e.g. `/healthz`). */
            path: string;
            /** @description Probe interval in seconds; 0 = use image default. */
            interval_s?: number;
            /** @description Probe timeout in seconds; 0 = use image default. */
            timeout_s?: number;
            /** @description Consecutive failures before the instance is considered unhealthy; 0 = use image default. */
            retries?: number;
        };
        /**
         * @description Liveness-probe shape on the deploy-time override object (issue #554 / ADR-078).
         *     The probe is the Cloud-Run-parity primitive that asks "is the VM still
         *     responding?" — a wedged guest (busy-loop, leaked fd, deadlocked runner)
         *     sits resident billing RAM-hours while serving 5xx until the §13 idle
         *     reaper eventually fires. The liveness probe replaces that wait with the
         *     short, customer-visible path: 3 consecutive failures → destroy →
         *     cold-boot from rootfs (per ADR-005 — never snapshot-restore).
         *
         *     Distinct from `DeploymentHealthcheck`: readiness is "accept traffic?",
         *     liveness is "still alive?". A passing readiness but failing liveness
         *     is the canonical "wake succeeded, then the runtime died" failure mode
         *     that this primitive is designed to catch.
         *
         *     Validation rules (enforced in `pkg/api/dto.go::CreateDeploymentOverrides.Validate`):
         *     - `path` must start with `/`.
         *     - `interval_s` ∈ [0, 60] (0 = inherit per-plan default; Hobby/Pro/Scale → 5 s).
         *     - `timeout_s` ∈ [0, 5] (0 = inherit 2 s).
         *     - `consecutive_failures` ∈ [0, 10] (0 = inherit per-plan default; Hobby/Pro/Scale → 3).
         *     - `cooldown_s` ∈ [10, 600] (cooldown gate enforced by the vmmd-side
         *       probe loop after a destroy fires — see ADR-078; 0 = no cooldown, the
         *       legacy Free-plan behaviour).
         *
         *     Per-deployment overrides of `interval_s` / `timeout_s` / `consecutive_failures` / `cooldown_s`
         *     are clamped to the bounds above. The counter resets to 0 on the first 2xx
         *     and survives an intermittent 5xx across the consecutive window (AC #2:
         *     flaky app does NOT oscillate). The cooldown gate short-circuits the
         *     counter when a probe fires inside the previous destroy's window so the
         *     cold-boot replacement instance has a grace period to settle.
         */
        DeploymentLivenessProbe: {
            /** @description Path the probe requests from the guest; must start with `/` (e.g. `/healthz`). Reuses the runner's existing `:8080` listener — no runner changes (issue #554 §4). */
            path: string;
            /** @description Per-plan poll cadence in seconds; 0 = inherit per-plan default (Hobby/Pro/Scale → 5 s). Clamped to [MinLivenessPeriodSeconds=1, MaxLivenessPeriodSeconds=60]. */
            interval_s?: number;
            /** @description Per-probe HTTP timeout in seconds; 0 = inherit 2 s default (VsockLivenessTimeoutMs). A timeout is treated identically to a non-2xx response by the failure counter. */
            timeout_s?: number;
            /** @description N at which DestroyForLivenessFailure fires; 0 = inherit per-plan default (Hobby/Pro/Scale → 3). The counter is reset to 0 on the first 2xx and survives an intermittent 5xx across the consecutive window (AC #2 — flaky app does NOT oscillate). */
            consecutive_failures?: number;
            /** @description Cooldown window in seconds after a liveness-driven destroy fires. While inside the window the vmmd-side probe loop short-circuits the failure counter so the cold-boot replacement has time to settle (issue #554 closure / ADR-078). 0 = no cooldown (Free-plan / legacy behaviour); clamped to [MinLivenessCooldownSeconds=10, MaxLivenessCooldownSeconds=600] when the field is populated by a Pro/Scale deployment override. */
            cooldown_s?: number;
        };
        /**
         * @description Per-deploy grype CVE scan result (issue #464 / ADR-055). Surfaced on
         *     GET /v1/deployments/{id} (additive DeploymentResponse.Scan field) and
         *     on GET /v1/deployments/{id}/scan (the dedicated drill-down route).
         *     The dashboard renders the severity counts and the top 10 CVEs;
         *     `gregale deployment <id> --show-scan` prints the full payload.
         *     Surface, never enforce — an image with CRITICAL CVEs deploys
         *     successfully; the dashboard shows it; that is the contract.
         */
        ScanResult: {
            /**
             * @description Closed enum mirroring the deployments.scan_status column.
             *     `pending` = grype run started, not finished yet;
             *     `complete` = grype run finished, scan carries the findings;
             *     `failed` = grype run errored after the 1-retry backoff, scan carries the last error in `error`;
             *     `skipped` = pre-feature row (the migration backfilled this on every row that predates 00135).
             * @enum {string}
             */
            status: "pending" | "complete" | "failed" | "skipped";
            /**
             * Format: date-time
             * @description Wall clock the grype run completed (RFC 3339 UTC). Empty when status != "complete". Distinct from deployments.created_at — the deploy ships before the scan lands (AC
             */
            scanned_at?: string | null;
            /** @description Grype binary version that produced the scan (e.g. "grype 0.78.0"). Captured once at imaged startup via `grype version` and stamped on every ScanResult payload. */
            scanner_version?: string | null;
            /** @description OCI image digest at the time of the scan. Sourced from deployments.image_digest, not re-inspected. Empty on the pre-feature backfill (status = "skipped" with no image to stamp). */
            image_digest?: string | null;
            severity_counts: components["schemas"]["SeverityCounts"];
            /** @description Full CVE list, ordered by Grype's natural output (most-severe-first). The dashboard's "top 10" view sorts+truncates client-side. The /scan route returns the full list. */
            vulnerabilities: components["schemas"]["Vulnerability"][];
            /** @description Grype runner's last error message on a failed scan (status = "failed"). Empty on every other status. The PR-3 sink captures the message after the 1-retry backoff is exhausted. */
            error?: string | null;
        };
        /**
         * @description Per-bucket count of CVEs in Grype's closed vocabulary
         *     (CRITICAL|HIGH|MEDIUM|LOW|UNKNOWN). Negligible collapses into LOW
         *     (matches the existing pkg/imaged.grype.go::normalizeGrypeSeverity
         *     convention). All fields present without omitempty so the JSON shape
         *     is uniform — the dashboard reads counts without nil checks.
         */
        SeverityCounts: {
            critical: number;
            high: number;
            medium: number;
            low: number;
            unknown: number;
        };
        /** @description One row in the scan's CVE list. */
        Vulnerability: {
            /** @description CVE id (e.g. CVE-2024-1234). */
            id: string;
            /** @enum {string} */
            severity: "CRITICAL" | "HIGH" | "MEDIUM" | "LOW" | "UNKNOWN";
            /** @description Vulnerable package name (e.g. openssl). */
            package: string;
            /** @description Installed package version (e.g. 1.1.1k-7). */
            version: string;
            /** @description Upstream version the fix landed in. Empty when no fix is available (e.g. disputed CVE) — the dashboard renders 'no fix' for empty. */
            fixed_in?: string | null;
            /**
             * @description Per-file paths within the scanned image where the vulnerable
             *     package was located (issue #464 / extension). Sourced from
             *     grype's `artifact.locations[].path`. Many CVEs match against a
             *     package without per-file locations (deb/apt-style CVE matches);
             *     empty / null for those rows. The dashboard's "Path" column
             *     renders "—" on null.
             */
            paths?: string[];
        };
        /**
         * @description One entry in the deploy request's `sidecars` array
         *     (issue #463 / ADR-068). Up to 2 sidecars per app (1 init
         *     + 1 sidecar; the array is type-uniqueness + 2-capped at
         *     the schema layer via migration 00095's CHECK constraint).
         *     Stateless only — stateful base images (Postgres, Redis,
         *     MySQL, MongoDB, etc.) are rejected at the API gate
         *     with 403 `sidecar_stateful_denied` and again at imaged
         *     (PR-B). Image references must be digest-pinned
         *     (`repo@sha256:...`); tag references are rejected with
         *     400 `sidecar_invalid_image`. Env values are
         *     envelope-sealed at rest via secretbox (namespace
         *     `"sidecar_env"`); the wire shape is plaintext, the
         *     column is sealed ciphertext.
         *
         *     - `name` matches RFC 1123 label (lowercase alphanumeric
         *       + dash, 1..63 chars, starts with [a-z0-9]). Unique
         *       within a single request.
         *     - `image` is the digest-pinned OCI reference. Tag
         *       references rejected. State images rejected.
         *     - `type` ∈ {`init`, `sidecar`}. At most one of each per
         *       deployment.
         *     - `cmd` is the argv (image's ENTRYPOINT unchanged; CMD
         *       overridden). Every element non-empty.
         *     - `env` is plaintext on the wire, sealed at rest. Keys
         *       per `^[A-Z][A-Z0-9_]*$`; per-value byte cap = plan
         *       `EnvValueMaxBytes`. Plaintext values NEVER appear in
         *       any log, audit, or error.
         *     - `port` ∈ {0, 1..65535}. 0 = absent.
         *     - `ram_mb` ∈ {0, 32..512}. 0 = inherit plan RAM.
         *     - `essential` defaults to true. If true and the sidecar
         *       exits non-zero: type=init → fail the deploy
         *       (`failure_class=user_error`); type=sidecar → restart-
         *       loop. If false: warn + restart-cap (PR-B's runtime).
         */
        Sidecar: {
            /** @description RFC 1123 label (lowercase alphanumeric + dash, 1..63 chars, starts with [a-z0-9]). */
            name: string;
            /** @description Digest-pinned OCI reference (repo@sha256:...). Tag references rejected with 400 `sidecar_invalid_image`. */
            image: string;
            /**
             * @description `init` runs once before the main workload (DB migrator shape). `sidecar` runs alongside (metrics scraper shape).
             * @enum {string}
             */
            type: "init" | "sidecar";
            /** @description Argv. Image's ENTRYPOINT unchanged; CMD overridden. Every element non-empty. */
            cmd?: string[];
            /** @description Plaintext env map (sealed at rest). Keys `^[A-Z][A-Z0-9_]*$`; per-value byte cap = plan EnvValueMaxBytes. */
            env?: {
                [key: string]: string;
            };
            /** @description Listen port. 0 = absent / fall back to image default. */
            port?: number;
            /** @description Cgroup memory ceiling for this sidecar. 0 = inherit plan RAM; 32..512 enforced at the API. */
            ram_mb?: number;
            /** @description Defaults to true. type=init non-zero exit → fail deploy; type=sidecar non-zero exit → restart-loop. PR-B's runtime. */
            essential?: boolean;
        };
        /** @description Read-only instance view: id, deployment, state (waking/running/...), lease uid, and host-side internal endpoint (loopback only). */
        InstanceResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
            deployment_id: string;
            state: string;
            host_ip?: string | null;
            /** @example 256 */
            ram_mb: number;
            wake_id?: string;
            /** Format: date-time */
            started_at?: string | null;
            /** Format: date-time */
            last_request_at?: string | null;
            /** Format: date-time */
            parked_at?: string | null;
            min_instances_target?: number | null;
        };
        /** @description A custom domain binding: domain string, target app, verification status, and TLS provisioning state. */
        CustomDomainResponse: {
            /** @example app.example.com */
            domain: string;
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
            challenge_token?: string | null;
            verified: boolean;
            /** Format: date-time */
            verified_at?: string | null;
            txt_record?: string | null;
        };
        /** @description Bind a custom domain to an app. */
        CreateCustomDomainRequest: {
            /** @example app.example.com */
            domain: string;
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
        };
        /** @description A cron trigger: schedule (cron expression), target URL, last/next run timestamps, and enabled flag. */
        CronResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
            /** @example *\/5 * * * * */
            schedule: string;
            path: string;
            enabled: boolean;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            last_fired_at?: string | null;
        };
        /** @description One execution of a cron: when it fired, how long it ran, and how it ended. Backed by the underlying invocations row, projected so callers need not compute a duration or interpret raw state. */
        CronRun: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            /**
             * Format: date-time
             * @description When the cron fired (the invocation's created_at), not when the app began executing.
             */
            started_at: string;
            /**
             * Format: date-time
             * @description When the run reached a terminal state; null while still in flight.
             */
            completed_at?: string | null;
            /** @description completed_at - started_at in milliseconds, computed server-side. Null while the run is still in flight. */
            duration_ms?: number | null;
            /**
             * @description Normalized result. `timeout` means the dispatch exceeded its deadline; `dead_letter` means the retry budget was exhausted; `running` means the run has not reached a terminal state yet. Branch on this, never on `error`.
             * @enum {string}
             */
            outcome: "success" | "failed" | "timeout" | "dead_letter" | "running";
            /** @description Dispatch attempts for this run; greater than 1 means it was retried. */
            attempts: number;
            /** @description The instance that served the run; null if the fire never reached one. */
            instance_id?: string | null;
            /** @description Operator-facing failure text. Unstructured and unversioned — do not parse it. */
            error?: string | null;
        };
        /** @description Page of cron runs; ordered newest-first. Pass the LAST id of the returned slice as the next `?before=` to load older runs. */
        ListCronRunsResponse: {
            runs: components["schemas"]["CronRun"][];
        };
        /**
         * @description Issue #791 PR-C / ADR-090. The 202 body for `POST /v1/crons/{id}/run`.
         *     `request_id` is the durable handle on the fire-now row in
         *     `cron_fire_now_requests` (migrations/00193); poll
         *     `GET /v1/crons/{id}/runs` for the matching `cron.fired.manually`
         *     audit row, or watch the future `GET /v1/cron-fire-now-requests/{id}`
         *     for terminal status. `status` is `"pending"` at the moment of
         *     the response; transitions to `"succeeded"` or `"failed"`
         *     asynchronously as schedd claims the row.
         */
        FireCronResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            request_id: string;
            /** @example fedcba9876543210fedcba9876543210 */
            cron_id: string;
            /**
             * @example pending
             * @enum {string}
             */
            status: "pending" | "running" | "succeeded" | "failed";
        };
        /**
         * @description Issue #791 PR-D / ADR-090 §Sub-decision 7. Read shape for
         *     `GET /v1/cron-fire-now-requests/{request_id}`.
         */
        FireCronRequestResponse: {
            /** Format: uuid */
            request_id: string;
            /** Format: uuid */
            cron_id: string;
            /** @enum {string} */
            status: "pending" | "running" | "succeeded" | "failed" | "cancelled";
            /** Format: date-time */
            requested_at: string;
            /** Format: date-time */
            finished_at?: string | null;
            /** Format: uuid */
            invocation_id?: string | null;
            error?: string | null;
            /** Format: uuid */
            account_id: string;
        };
        /** @description Cron creation payload: schedule expression and target URL. */
        CreateCronRequest: {
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
            /** @example *\/5 * * * * */
            schedule: string;
            path?: string;
            enabled?: boolean | null;
        };
        /** @description Partial cron update. */
        UpdateCronRequest: {
            /** @example *\/5 * * * * */
            schedule?: string | null;
            path?: string | null;
            enabled?: boolean | null;
        };
        /** @description Multipart body for POST /v1/projects/scan (dry-run). */
        ProjectScanRequest: {
            /**
             * Format: binary
             * @description tar.gz of the repo root.
             */
            source: string;
            /** @description kebab slug; default = repo dir basename */
            project_slug?: string;
            /** @example main */
            production_branch?: string;
            /**
             * Format: int64
             * @description GitHub install id (with --repo); 0 for unbound repos
             */
            install_id?: number;
            /** @description CSV of workload names to include (others skipped) */
            only?: string;
        };
        /**
         * @description JSON body for POST /v1/apps/{slug}/deployments/source-ref
         *     (DEPLOY-PROV-4 / ADR-092, issue #739). The headless CI deploy
         *     path: `repo` resolves to an install-token-bound fetch, `ref` is
         *     the customer's chosen input (branch / tag / SHA — server
         *     resolves to a 40-char SHA before stamping the deployment row).
         */
        SourceRefDeployRequest: {
            /**
             * @description GitHub owner/name slug, e.g. `onebox-faas/hello`.
             * @example onebox-faas/hello
             */
            repo: string;
            /**
             * @description Commit ref — 40-char SHA, branch, or tag. api.github.com
             *     /repos/<repo>/commits/<ref> resolves branch / tag inputs
             *     to a pinned SHA before the tarball fetch starts. The wire
             *     shape pins to the resolved 40-char SHA (server override;
             *     caller's `ref` is preserved on the `deploy.source_ref`
             *     audit row for traceability).
             * @example main
             */
            ref: string;
            /**
             * @description Forward-compat field. PR-A only supports `tarball`.
             * @default tarball
             * @enum {string}
             */
            format: "tarball";
        };
        /**
         * @description JSON body for POST /v1/apps/{slug}/diff (PR-1). Slim
         *     purpose-built DTO — every field maps 1:1 to a
         *     [deploydiff.Pending] entry via the apid handler's adapter.
         *     Empty / absent fields mean "no change proposed" (matches
         *     the engine's pointer semantics: every nested field is
         *     optional; null = "don't touch").
         */
        DiffRequest: {
            app_config?: components["schemas"]["DiffAppConfigPatch"];
            /**
             * @description Would-write image reference. Empty = no image deploy
             *     proposed. Compared against the baseline's
             *     DeploymentResponse.ImageDigest for the immutable-diff
             *     check.
             * @example ghcr.io/me/api@sha256:abc
             */
            image?: string;
            manifest?: components["schemas"]["AppManifest"];
            /**
             * @description Per-scope env write. Full-replacement semantics per
             *     scope (ADR-090 D3). Keys are scope names ("default",
             *     "staging", ...); values are DiffEnvRow arrays.
             */
            env_by_scope?: {
                [key: string]: components["schemas"]["DiffEnvRow"][];
            };
            crons?: components["schemas"]["CreateCronRequest"][];
            edge_rules?: components["schemas"]["CreateEdgeRuleRequest"][];
        };
        /**
         * @description Per-app scalar patch. Pointer-aware: nil = "don't touch";
         *     explicit zero / explicit value = "set to this". Matches
         *     [UpdateAppRequest] semantics but exposes only the fields
         *     the engine computes against (no ScalingPolicy /
         *     PublicAuth / OverflowNode).
         */
        DiffAppConfigPatch: {
            ram_mb?: number;
            idle_timeout_s?: number;
            max_concurrency?: number;
            min_instances?: number;
            egress_allowlist?: string[];
            autoscale_target_rps?: number;
            autoscale_target_cpu_pct?: number;
            streaming_enabled?: boolean;
            websocket_enabled?: boolean;
            require_signed?: boolean;
            warm_snapshot_enabled?: boolean;
            require_authn?: boolean;
            /** @enum {string} */
            eviction_priority?: "normal" | "batch" | "latency";
        };
        /**
         * @description One would-write env var. Value carries plaintext so the
         *     quota gate's per-value byte cap can fire (the wire's list
         *     path never echoes values per ADR-053 §Decision 4).
         */
        DiffEnvRow: {
            key: string;
            value: string;
        };
        /**
         * @description One diff row. Polymorphic values (Before / After) re-emitted
         *     as JSON objects — the engine emits primitives, slices, or
         *     structs depending on the field.
         */
        DiffChange: {
            /** @description Human path: 'memory', 'concurrency', 'environment.<scope>.<key>', 'cron[<schedule> <path>]', 'edge_rule[<kind> <host><path>]' */
            field: string;
            /** @enum {string} */
            kind: "add" | "remove" | "modify";
            /** @description Primitive value (int / string / bool / []string) — JSON-encoded. Omitted on Add. */
            before?: unknown;
            /** @description Primitive value — JSON-encoded. Omitted on Remove. */
            after?: unknown;
        };
        /**
         * @description One would-ship-a-problem row. Stable RFC 7807 code
         *     (matches [api.Code*] constants) so the CLI error renders
         *     identically to what a real deploy would say.
         */
        DiffBreak: {
            /** @description Stable RFC 7807 code; matches api.Code* constants */
            code: string;
            /** @enum {string} */
            severity: "error" | "warn";
            reason: string;
            /** @description Optional scope-wide ('memory') or per-row ('environment.<scope>.<key>'). */
            field?: string;
            /** @description JSON-encoded observed value (int / string / []string / …). */
            observed?: unknown;
            /** @description JSON-encoded limit value. */
            limit?: unknown;
        };
        /**
         * @description Inner diff object the engine produces. Slug + Plan +
         *     Changes + Breaks. Wrapped by [DiffResponse] so a CI
         *     consumer reading `.diff.changes` and a CLI consumer
         *     reading the top-level keys agree.
         */
        DiffPayload: {
            slug: string;
            /**
             * @description Customer's subscription tier (echoed from acct.Plan).
             * @enum {string}
             */
            plan?: "free" | "hobby" | "pro" | "scale";
            changes: components["schemas"]["DiffChange"][];
            breaks: components["schemas"]["DiffBreak"][];
        };
        /**
         * @description Wire envelope for POST /v1/apps/{slug}/diff and
         *     `gregale deploy --diff --json`. Wraps the [DiffPayload]
         *     plus the Blocking bit so a CI consumer doesn't have to
         *     re-scan Breaks and pick the max severity.
         */
        DiffResponse: {
            diff: components["schemas"]["DiffPayload"];
            /**
             * @description True if any break has severity "error". Mirrors
             *     [pkg/deploydiff.Diff.HasBlockingBreaks]. The exit-1
             *     input for CI gates.
             */
            blocking: boolean;
            slug: string;
            /**
             * @description Echoed at top level too — kept in sync with diff.plan.
             * @enum {string}
             */
            plan?: "free" | "hobby" | "pro" | "scale";
        };
        /**
         * @description Multipart body for POST /v1/projects (apply). Same shape as
         *     ProjectScanRequest; the apply handler resolves AppIDs and
         *     inserts crons in a follow-up pass.
         */
        ProjectApplyRequest: {
            /** Format: binary */
            source: string;
            project_slug?: string;
            production_branch?: string;
            /** Format: int64 */
            install_id?: number;
            only?: string;
        };
        /** @description One discovered unit of work. Mirrors reposcan.Workload. */
        PlanWorkload: {
            /** @example api */
            name: string;
            /** @example  */
            root_dir: string;
            /** @example Dockerfile */
            dockerfile?: string;
            command: string[];
            /** @enum {string} */
            class?: "http" | "graphql" | "grpc" | "job" | "worker" | "server" | "unknown";
            /** @description cron expression when declared (CronJob, render, serverless) */
            schedule?: string;
            ports: number[];
            /** @description KEYS only — never values; spec §11 forbids logging secrets */
            env_keys?: string[];
            /** @description detector provenance, e.g. compose.yaml: api */
            source?: string;
            /** @enum {string} */
            tier?: "single" | "convention" | "workspace" | "compose" | "unknown";
        };
        /**
         * @description A service the repo declared (compose image:, render.yaml, etc.)
         *     but the platform will NOT provision. Surfaced so the customer
         *     sees them in the plan and reads a learnable env hint instead of
         *     a runtime 422 (two-drive FROM-base contract, ADR-046).
         */
        PlanManaged: {
            name: string;
            /** @example postgres */
            kind: string;
            /** @example DATABASE_URL */
            env_hint: string;
            source: string;
            image: string;
        };
        /** @description A cron expression lifted from a workload (k8s CronJob, render.yaml, serverless.yml). */
        PlanCron: {
            /** @example nightly */
            workload_name: string;
            /** @example 0 3 * * * */
            schedule: string;
            /** @example / */
            path: string;
            enabled: boolean;
        };
        /**
         * @description Limit + observed extension on a plan-quota problem. Mirrors
         *     api.Problem.WithLimit; emitted alongside any 402/403 quota
         *     response so the CLI can render "X/Y apps" without a second
         *     request.
         */
        QuotaBlock: {
            /** Format: int64 */
            limit?: number;
            /** Format: int64 */
            observed?: number;
            docs_url?: string;
        };
        /** @description Dry-run scan response. */
        PlanResponse: {
            project_slug: string;
            repo_full_name?: string;
            /** @enum {string} */
            scan_source: "compose" | "procfile" | "k8s" | "render" | "fly" | "serverless" | "workspace" | "convention" | "single" | "unknown";
            tier: string;
            workloads: components["schemas"]["PlanWorkload"][];
            managed: components["schemas"]["PlanManaged"][];
            crons: components["schemas"]["PlanCron"][];
            warnings?: string[];
            observed_apps: number;
            observed_crons: number;
            limit_apps: number;
            limit_crons: number;
            can_apply: boolean;
            crons_not_allowed?: boolean;
            /** @description base64-JSON plan token; pass back as ?plan_token= on /v1/projects to skip the second extract. */
            plan_token: string;
        };
        /** @description Apply response. Carries the inserted project_id and per-app IDs. */
        ApplyResponse: {
            project_slug: string;
            repo_full_name?: string;
            scan_source: string;
            tier?: string;
            workloads?: components["schemas"]["PlanWorkload"][];
            managed?: components["schemas"]["PlanManaged"][];
            crons?: components["schemas"]["PlanCron"][];
            warnings?: string[];
            observed_apps?: number;
            observed_crons?: number;
            limit_apps?: number;
            limit_crons?: number;
            can_apply: boolean;
            crons_not_allowed?: boolean;
            plan_token: string;
            project_id?: string;
            apps?: {
                slug: string;
                id: string;
            }[];
            /**
             * @description Per-workload build enqueue results. Populated when the apply
             *     path actually enqueued one (deployment, build) per added or
             *     changed workload (PR-A, repo decomposition Phase 5 close-
             *     the-loop). On staging or enqueue failure the per-app Error
             *     field is populated and the IDs are empty.
             */
            builds?: components["schemas"]["AppliedBuild"][];
        };
        /**
         * @description Per-workload build result from the apply-time build-enqueue loop.
         *     On success: slug + app_id + deployment_id + build_id. On failure:
         *     slug + app_id + error, no IDs.
         */
        AppliedBuild: {
            slug: string;
            app_id: string;
            deployment_id?: string;
            build_id?: string;
            /** @description Staging or enqueue error message; partial-failure rows carry this in lieu of IDs. */
            error?: string;
        };
        /**
         * @description A customer-configurable alert rule. Carries the masked webhook
         *     secret; the sealed ciphertext is server-side only.
         */
        AlertRuleResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            /**
             * @description Pinned app id. Empty string = account-wide rule.
             * @example 0123456789abcdef0123456789abcdef
             */
            app_id: string;
            /** @example p99 > 500ms */
            name: string;
            /** @example true */
            enabled: boolean;
            /** @enum {string} */
            metric: "error_rate_pct" | "latency_p50_ms" | "latency_p95_ms" | "latency_p99_ms" | "cold_start_pct" | "request_count" | "failed_invocations";
            /** @enum {string} */
            comparison: "gt" | "gte" | "lt" | "lte";
            /**
             * Format: double
             * @example 500
             */
            threshold: number;
            /** @enum {string} */
            window_spec: "5m" | "15m" | "1h" | "6h" | "24h" | "7d" | "15d";
            /**
             * @description Source dimension for failed_invocations; omit when metric is not failed_invocations (xor_chk).
             * @enum {string}
             */
            failure_source?: "any" | "cron" | "queue" | "delayed_task" | "async_invoke";
            /**
             * Format: uri
             * @example https://example.com/hook
             */
            webhook_url: string;
            /**
             * @description Literal "***" — the plaintext is never returned.
             * @example ***
             */
            webhook_secret_sealed_masked: string;
            /** @example 15 */
            cooldown_minutes: number;
            /**
             * @description Cool-down state machine.
             * @enum {string}
             */
            state: "ok" | "firing";
            /** Format: date-time */
            last_fired_at?: string;
            /** Format: date-time */
            last_evaluated_at?: string;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        /** @description Create an alert rule on an app. */
        CreateAlertRuleRequest: {
            /** @example p99 > 500ms */
            name: string;
            /** @example true */
            enabled?: boolean;
            /** @enum {string} */
            metric: "error_rate_pct" | "latency_p50_ms" | "latency_p95_ms" | "latency_p99_ms" | "cold_start_pct" | "request_count" | "failed_invocations";
            /** @enum {string} */
            comparison: "gt" | "gte" | "lt" | "lte";
            /**
             * Format: double
             * @example 500
             */
            threshold: number;
            /** @enum {string} */
            window_spec: "5m" | "15m" | "1h" | "6h" | "24h" | "7d" | "15d";
            /**
             * @description Required when metric == failed_invocations; omit otherwise (xor_chk).
             * @enum {string}
             */
            failure_source?: "any" | "cron" | "queue" | "delayed_task" | "async_invoke";
            /**
             * Format: uri
             * @example https://example.com/hook
             */
            webhook_url: string;
            /**
             * @description Plaintext HMAC secret (max 256 bytes). Sealed at rest; never echoed.
             * @example shh
             */
            webhook_secret: string;
            /** @example 15 */
            cooldown_minutes?: number;
        };
        /** @description Partial update — every field is optional. Omitted means leave alone. */
        UpdateAlertRuleRequest: {
            name?: string;
            enabled?: boolean;
            /**
             * @description Cannot cross metric families (e.g. error_rate_pct → failed_invocations) — returns 400.
             * @enum {string}
             */
            metric?: "error_rate_pct" | "latency_p50_ms" | "latency_p95_ms" | "latency_p99_ms" | "cold_start_pct" | "request_count" | "failed_invocations";
            /** @enum {string} */
            comparison?: "gt" | "gte" | "lt" | "lte";
            /** Format: double */
            threshold?: number;
            /** @enum {string} */
            window_spec?: "5m" | "15m" | "1h" | "6h" | "24h" | "7d" | "15d";
            /** Format: uri */
            webhook_url?: string;
            /** @description New plaintext HMAC secret. Omit to keep the existing secret. */
            webhook_secret?: string;
            cooldown_minutes?: number;
        };
        /** @description rotate-secret response — returns the rotated_at timestamp + the masked constant. */
        RotateAlertRuleSecretResponse: {
            /**
             * Format: date-time
             * @example 2026-07-28T12:00:00Z
             */
            rotated_at: string;
            /** @example *** */
            webhook_secret_sealed_masked: string;
        };
        /**
         * @description A customer-configurable edge rule. The `action` blob is a
         *     kind-tagged union — the shape varies by `kind`. See
         *     CreateEdgeRuleRequest.action for the per-kind shape.
         */
        EdgeRuleResponse: {
            id: string;
            account_id: string;
            app_id: string;
            /**
             * @description Host glob. `*` matches any; `*.example.com` matches any subdomain.
             * @example api.example.com
             */
            match_host: string;
            /**
             * @description Path glob. Trailing `*` matches anything beneath.
             * @example /api/*
             */
            match_path: string;
            /**
             * @description Empty array = match any method.
             * @example [
             *       "GET",
             *       "POST"
             *     ]
             */
            match_methods: string[];
            /** @default 100 */
            priority: number;
            /** @default true */
            enabled: boolean;
            /** @enum {string} */
            kind: "route" | "rewrite" | "redirect" | "headers" | "cors" | "jwt" | "ip" | "validate" | "limit" | "maintenance" | "geo" | "throttle" | "budget";
            /** @description Kind-tagged union — shape varies by `kind`. */
            action: components["schemas"]["EdgeRuleRouteAction"] | components["schemas"]["EdgeRuleRewriteAction"] | components["schemas"]["EdgeRuleRedirectAction"] | components["schemas"]["EdgeRuleHeadersAction"] | components["schemas"]["EdgeRuleCORSAction"] | components["schemas"]["EdgeRuleJWTAction"] | components["schemas"]["EdgeRuleIPAction"] | components["schemas"]["EdgeRuleValidateAction"] | components["schemas"]["EdgeRuleLimitAction"] | components["schemas"]["EdgeRuleMaintenanceAction"] | components["schemas"]["EdgeRuleGeoAction"] | components["schemas"]["EdgeRuleThrottleAction"] | components["schemas"]["EdgeRuleBudgetAction"];
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        /** @description Body shape for POST /v1/apps/{slug}/edge-rules. */
        CreateEdgeRuleRequest: {
            match_host: string;
            /** @default / */
            match_path: string;
            match_methods?: string[];
            /** @default 100 */
            priority: number;
            /** @default true */
            enabled: boolean;
            /** @enum {string} */
            kind: "route" | "rewrite" | "redirect" | "headers" | "cors" | "jwt" | "ip" | "validate" | "limit" | "maintenance" | "geo" | "throttle" | "budget";
            /** @description Kind-tagged action body — shape depends on `kind`. */
            action: components["schemas"]["EdgeRuleRouteAction"] | components["schemas"]["EdgeRuleRewriteAction"] | components["schemas"]["EdgeRuleRedirectAction"] | components["schemas"]["EdgeRuleHeadersAction"] | components["schemas"]["EdgeRuleCORSAction"] | components["schemas"]["EdgeRuleJWTAction"] | components["schemas"]["EdgeRuleIPAction"] | components["schemas"]["EdgeRuleValidateAction"] | components["schemas"]["EdgeRuleLimitAction"] | components["schemas"]["EdgeRuleMaintenanceAction"] | components["schemas"]["EdgeRuleGeoAction"] | components["schemas"]["EdgeRuleThrottleAction"] | components["schemas"]["EdgeRuleBudgetAction"];
        };
        /** @description Partial update — every field optional. Kind is not patchable. */
        UpdateEdgeRuleRequest: {
            match_host?: string;
            match_path?: string;
            match_methods?: string[];
            priority?: number;
            enabled?: boolean;
            /** @description Replaces the jsonb column whole. */
            action?: components["schemas"]["EdgeRuleRouteAction"] | components["schemas"]["EdgeRuleRewriteAction"] | components["schemas"]["EdgeRuleRedirectAction"] | components["schemas"]["EdgeRuleHeadersAction"] | components["schemas"]["EdgeRuleCORSAction"] | components["schemas"]["EdgeRuleJWTAction"] | components["schemas"]["EdgeRuleIPAction"] | components["schemas"]["EdgeRuleValidateAction"] | components["schemas"]["EdgeRuleLimitAction"] | components["schemas"]["EdgeRuleMaintenanceAction"] | components["schemas"]["EdgeRuleGeoAction"] | components["schemas"]["EdgeRuleThrottleAction"] | components["schemas"]["EdgeRuleBudgetAction"];
        };
        /** @description Re-targets the matched request to another app owned by the same account. */
        EdgeRuleRouteAction: {
            /** @example legacy-api */
            target_app_slug: string;
        };
        /** @description Mutates the request path before forwarding. */
        EdgeRuleRewriteAction: {
            /** @example /api */
            from: string;
            /** @example /v1 */
            to: string;
        };
        /** @description 3xx short-circuit. */
        EdgeRuleRedirectAction: {
            /** @enum {integer} */
            status_code: 301 | 302 | 307 | 308;
            to: string;
            /** @description Headers stamped on the redirect response. */
            headers?: {
                [key: string]: string;
            };
        };
        /** @description One header mutation. `action` ∈ {add,set,remove}. */
        EdgeRuleHeaderOp: {
            name: string;
            value?: string;
            /** @enum {string} */
            action: "add" | "set" | "remove";
        };
        /** @description Mutates request + response headers. The gateway enforces a hard-coded blacklist (Host, Content-Length, Transfer-Encoding, Connection, x-faas-*). */
        EdgeRuleHeadersAction: {
            request_headers?: components["schemas"]["EdgeRuleHeaderOp"][];
            response_headers?: components["schemas"]["EdgeRuleHeaderOp"][];
        };
        /** @description Stamps CORS headers + handles preflight in-process. */
        EdgeRuleCORSAction: {
            allow_origins: string[];
            allow_methods: string[];
            allow_headers?: string[];
            expose_headers?: string[];
            /** @default false */
            allow_credentials: boolean;
            max_age_seconds?: number;
        };
        /** @description Validates an inbound Bearer JWT against a JWKS endpoint. */
        EdgeRuleJWTAction: {
            /** @example https://idp.example.com/ */
            issuer: string;
            audience?: string[];
            /** @example https://idp.example.com/.well-known/jwks.json */
            jwks_url: string;
            algorithms: ("RS256" | "RS384" | "RS512" | "ES256" | "ES384" | "ES512" | "HS256" | "HS384" | "HS512")[];
            required_claims?: {
                [key: string]: string;
            };
        };
        /** @description CIDR allow/deny evaluator. Deny is checked AFTER allow so a single-IP deny wins. */
        EdgeRuleIPAction: {
            allow?: string[];
            deny?: string[];
        };
        /**
         * @description Customer-supplied JSON Schema (Draft 2020-12) evaluated against
         *     the inbound request body BEFORE the wake gate fires. The
         *     kind=validate rule is the platform's API-native request-
         *     validation surface: rejections return 422
         *     `request_validation_failed` with `Problem.errors` carrying
         *     per-field detail, never pay a cold-boot cost, never consume
         *     the rate-limit / wake-quota budget on malformed traffic.
         *
         *     Free-and-above (no plan gate). Schema lives inline in the
         *     `action` jsonb blob (single-table flow), capped at the
         *     platform's `MaxEdgeRuleValidateSchemaBytes` (64 KiB). The
         *     gateway re-validates at compile time as defence-in-depth so
         *     the SQL hotfix path that bypasses apid-Validate still cannot
         *     ship an external `$ref`.
         *
         *     Field-by-field:
         *       * `schema` — required JSON Schema document (Draft 2020-12).
         *         Capped at 64 KiB. External `$ref` / `$id` URLs are
         *         rejected at create-time; internal pointers (`#/definitions/Foo`)
         *         pass through.
         *       * `content_types` — optional media-type allowlist.
         *         Closed set `application/*`. Empty = match any.
         *       * `apply_while_streaming` — per-rule opt-in for the
         *         streaming response path (ADR-047). Default false mirrors
         *         the §4.1 `Accept: application/json` opt-out.
         *       * `reject_on_unknown_fields` — toggles
         *         `additionalProperties: false` on the compiled schema.
         *         Default false preserves byte-stable schemas.
         *       * `max_body_bytes` — per-rule inbound body cap. 0 =
         *         inherit `MaxRequestBodyBytes` (per-plan 25 MB buffered /
         *         100 MB streaming). Must be > 0 and <= `MaxRequestBodyBytes`.
         */
        EdgeRuleValidateAction: {
            /**
             * @description Inline JSON Schema document (Draft 2020-12). The schema
             *     is preserved byte-exact across apid↔gatewayd round-trips
             *     so the SHA-256 cache key in `pkg/edgevalidate` is stable.
             */
            schema: {
                [key: string]: unknown;
            };
            /**
             * @description Optional media-type allowlist. Every entry must start
             *     with `application/`. Empty array = match any Content-Type.
             */
            content_types?: string[];
            /**
             * @description Whether validation fires on the streaming response path
             *     (ADR-047). Default false; set true per-rule to opt the
             *     SSE / chunked response path into body validation.
             */
            apply_while_streaming?: boolean;
            /**
             * @description Toggles `additionalProperties: false` on the compiled
             *     schema. Default false so a body with stray fields does
             *     not silently fail; opt in per-rule for strict schemas.
             */
            reject_on_unknown_fields?: boolean;
            /**
             * @description Per-rule inbound body cap. 0 (default) inherits the
             *     platform cap (`api.MaxRequestBodyBytes`). When set, must
             *     be > 0 and <= the platform cap.
             */
            max_body_bytes?: number;
        };
        /**
         * @description Standalone per-route body-size cap (ADR-091 D24 / §4.1.2.13).
         *     The primitive for "POST /upload ≤ 5 MB, POST /users ≤ 1 MB,
         *     POST /webhooks ≤ 2 MB" without shipping a JSON Schema. The
         *     hot-path applier (§4.1.2.8c) installs `http.MaxBytesReader`
         *     on the inbound body and short-circuits oversize requests
         *     with 413 `request_too_large` — and, more importantly,
         *     performs a Content-Length fast-path deny so a 30 MB body on
         *     a 5 MB cap costs zero bytes of buffering (a `MaxBytesReader`
         *     alone only trips when something reads the body, and on this
         *     hot path nothing reads it until the proxy leg).
         *
         *     Rejections never reach the wake gate, the auth chain, or the
         *     rate limiter — same posture as kind=validate. Free-and-above
         *     (no plan gate).
         *
         *     Field-by-field:
         *       * `max_body_bytes` — required buffered-path cap. Must be
         *         > 0 and ≤ `MaxRequestBodyBytes` (25 MiB). A standalone
         *         limit rule with no cap is a silent no-op and is
         *         rejected at create-time with 422 — use `kind=validate`
         *         if you need a body cap alongside a JSON Schema.
         *       * `max_body_bytes_streaming` — optional streaming opt-in
         *         cap (≤ `MaxEdgeRuleLimitBodyBytesStreaming` = 100 MiB).
         *         0 (default) = no streaming carve-out; the buffered
         *         `max_body_bytes` is the cap on both paths. When set,
         *         must be ≥ `max_body_bytes` — a streaming cap tighter
         *         than the buffered cap would 413 every streaming request
         *         for a body already accepted as buffered. Runtime
         *         enforcement of this field is declared + clamped at
         *         create-time but deferred at the §4.1.2.8c slot to a
         *         follow-up PR (stated in ADR-091 D24 §6).
         */
        EdgeRuleLimitAction: {
            /**
             * @description Required per-rule buffered-path body cap. Must be > 0
             *     and ≤ `api.MaxRequestBodyBytes` (25 MiB). A standalone
             *     limit rule with no cap is rejected at create-time with
             *     422 — use `kind=validate` if you need a body cap
             *     alongside a JSON Schema.
             */
            max_body_bytes: number;
            /**
             * @description Optional streaming opt-in cap. 0 (default) = no
             *     streaming carve-out; the buffered `max_body_bytes` is
             *     the cap on both paths. Must be ≥ `max_body_bytes` when
             *     set. Runtime enforcement deferred to follow-up PR
             *     (ADR-091 D24 §6).
             */
            max_body_bytes_streaming?: number;
        };
        /**
         * @description Per-route maintenance toggle (ADR-091 amendment, §4.1.2.13).
         *     The fine-grained primitive for "this endpoint is in
         *     maintenance mode" — the hot-path applier
         *     (pkg/gateway.(*Handler).applyEdgeRuleMaintenance) short-
         *     circuits a matched (host, path, http_method) request with
         *     503 + Retry-After BEFORE auth, BEFORE wake. The coarse
         *     sibling (`apps.maintenance_mode`, PATCH /v1/apps/{slug})
         *     covers the "whole app in maintenance" case; use this kind
         *     when only specific routes need to be pinned.
         *
         *     Field-by-field:
         *       * `retry_after_seconds` — optional per-rule override for
         *         the `Retry-After` header. 0 (default) = use the
         *         platform default
         *         `api.EdgeRuleMaintenanceRetryAfterSeconds` (60 s).
         *         Must be in `[0, 86400]` (24 h, enforced by
         *         `api.MaxEdgeRuleMaintenanceRetryAfterSeconds`); a
         *         customer cannot ship a rule that asks a client to
         *         back off for a week.
         *       * `message` — optional operator-friendly string that
         *         goes into `Problem.detail`. ≤ 512 B (same payload-
         *         size budget as `EdgeRuleValidateAction.schema`).
         *         Surface this on the customer's status page or in a
         *         dashboard alert so operators see why the endpoint is
         *         dark without scraping the rule row.
         *
         *     Free-and-above (no plan gate). Mirror the validate /
         *     limit posture: rejection never reaches the wake gate, the
         *     auth chain, or the rate limiter.
         */
        EdgeRuleMaintenanceAction: {
            /**
             * @description Optional per-rule Retry-After override in seconds.
             *     0 (default) = use the platform default
             *     `api.EdgeRuleMaintenanceRetryAfterSeconds` (60 s).
             *     Must be in `[0, 86400]` (24 h); values above 86400 are
             *     rejected at create-time with 422.
             * @example 0
             */
            retry_after_seconds?: number;
            /**
             * @description Optional operator-friendly detail string that goes into
             *     `Problem.detail`. ≤ 512 B. Surface this on the
             *     customer's status page so monitoring / curl users see
             *     why the endpoint is dark.
             * @example Rolling out v2; back at 14:00 UTC.
             */
            message?: string;
        };
        /**
         * @description ISO 3166-1 alpha-2 country allow/deny evaluator (ADR-091
         *     D21 / §4.1.2.8b). Mirrors `EdgeRuleIPAction` exactly: `allow`
         *     and `deny` are parallel ISO 3166-1 alpha-2 country-code
         *     lists; the matcher walks deny AFTER allow so a single-country
         *     deny wins even when the allow list is broad.
         *
         *     The match port is `gatewayd-internal` which consults a
         *     DB-IP Lite `.mmdb` file at request time to translate the
         *     trusted XFF client IP into a country code. Plan-tier quota
         *     is enforced at apid-create time via `Limits.EdgeRulesGeoPerApp`
         *     (Free=1, Hobby=5, Pro=25, Scale=100) inside the same apps-row
         *     FOR UPDATE lock as the general edge-rule cap. Geo is NOT in
         *     IsPaidOnly — Free customers get one rule before they upgrade.
         *
         *     Failure posture is fail-open: missing `.mmdb`, IP not in
         *     any country, RFC1918/bogon, or corrupt file → the rule does
         *     not fire → the request flows through. The
         *     `gateway_edge_rule_match_total{kind="geo",result="failed"}`
         *     counter increments and an `edge_rule.geo_failed` audit event
         *     emits.
         */
        EdgeRuleGeoAction: {
            /** @description ISO 3166-1 alpha-2 country allowlist. Empty + deny non-empty = deny-only. Empty + deny empty = no-op (create-time 422). */
            allow?: string[];
            /** @description ISO 3166-1 alpha-2 country denylist. Evaluated AFTER allow; single-country deny wins. */
            deny?: string[];
        };
        /**
         * @description Per-route token-bucket cap (ADR-091 D20.5 amendment,
         *     issue #881). Customers tighten the per-route rps/burst
         *     below their plan's `plan.RateLimitRPS` — the apid
         *     validator enforces the sub-plan ceiling; the gateway
         *     compiler enforces it again at load time.
         *
         *     Sub-plan ceiling — the load-bearing constraint. A
         *     throttle rule is STRICTLY a tightening primitive. A
         *     rule that exceeds the ceiling is rejected with 422
         *     BEFORE any DB write — a customer cannot raise their
         *     plan limit by registering a throttle rule.
         *
         *     Per-IP sub-keying is deliberately absent in v1 — see
         *     the package doc on `pkg/state.EdgeRuleThrottleAction`
         *     for the design rationale (memory-bounded limiter +
         *     attacker-controlled IP cardinality = unbounded bucket
         *     growth).
         */
        EdgeRuleThrottleAction: {
            /**
             * Format: double
             * @description Token-bucket refill rate per route. The apid
             *     validator rejects rps > plan.RateLimitRPS with a
             *     422. The gateway compiler clamps + warns on the
             *     same bound at load time.
             * @example 50
             */
            requests_per_second: number;
            /**
             * @description Token-bucket burst per route. Mirrors rps: rejected
             *     above `plan.RateLimitBurst` at create time and
             *     clamped at compile time.
             * @example 200
             */
            burst: number;
        };
        /**
         * @description Per-route end-to-end request budget (ADR-093 / §4.1.2.16).
         *     The primitive for "POST /payment → 3 s, POST /signup → 10 s"
         *     without writing timeout-propagation code in the customer's
         *     app. The hot-path applier (cmd/gatewayd-public/main.go:PR-B
         *     + pkg/gateway/handler_apply_edge_rule_budget.go) installs a
         *     per-request `Budget` onto `r.Context()` via
         *     `reqbudget.WithRemaining`; every downstream hop (DB, gRPC,
         *     HTTP) tightens itself against the budget via
         *     `reqbudget.WithOverhead` / `WithCeiling`.
         *
         *     On expiry the platform returns 504 + RFC 7807 problem
         *     envelope (`code: request_budget_exceeded`) BEFORE the
         *     customer's handler runs — the goal is bounded resource
         *     pin per request, not customer-visible timer logic. Customer
         *     code can read `reqbudget.FromContext(r.Context()).Remaining`
         *     if it wants to short-circuit its own work early.
         *
         *     Field-by-field:
         *       * `budget_ms` — required wall-clock budget. Must be > 0
         *         and ≤ the per-plan max (`Plan.RequestBudgetMaxMs`, default
         *         `RequestBudgetMax = 30 s`). A 0 or negative value is
         *         rejected at create-time with 422.
         *       * `allow_override_header` — optional HTTP request header
         *         that lets a customer set a per-request override
         *         (default `x-faas-budget-ms`). Empty (default) =
         *         no override accepted; the edge-rule value is the
         *         authoritative budget. The header is parsed as a
         *         decimal integer in `[1, math.MaxInt32]`; out-of-range
         *         values are ignored and the edge-rule value wins
         *         (silent clamp, no 400 — the budget is a quality
         *         primitive, not a security gate).
         *
         *     Rejections never reach the wake gate, the auth chain, or
         *     the rate limiter — same posture as the other kind=budget
         *     edge-rule appliers. Free-and-above (no plan gate).
         */
        EdgeRuleBudgetAction: {
            /**
             * Format: int64
             * @description Per-request wall-clock budget in milliseconds (1 ms – 30 s).
             */
            budget_ms: number;
            /** @description Optional RFC 7230 token header name for per-request override (default `x-faas-budget-ms`). Empty = no override. */
            allow_override_header?: string;
        };
        /**
         * @description API key metadata: id, prefix (first 8 chars), label, scopes, created/last-used timestamps, request count. **Plaintext is returned only on POST**. `org_id` (PR 6 / issue #190 / IAM-6 / ADR-061) is the org the key was minted against; legacy account-scoped responses stamp `org_id = caller's personal org`. See `org.create_api_key` / `org.revoke_api_key` for the new org-scoped verbs.
         * @example {
         *       "id": "0123456789abcdef0123456789abcdef",
         *       "org_id": "8b1f5e5d-273e-5a18-ae00-58fceba4fe6c",
         *       "prefix": "fp_live_ab12cd34",
         *       "label": "ci-deploy",
         *       "scopes": [
         *         "apps:read",
         *         "deploy:write"
         *       ],
         *       "created_at": "2026-08-02T11:25:00Z",
         *       "expires_at": "2027-08-02T11:25:00Z",
         *       "status": "active",
         *       "last_used_at": "2026-07-25T13:25:00Z"
         *     }
         */
        APIKeyResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            /**
             * Format: uuid
             * @description Org the key was minted against (PR 6). Always set — every `api_keys` row carries an org_id post-migration 00127. Personal-org scoped keys stamp the caller's personal org id here.
             */
            org_id: string;
            /** @description First 16 chars of the key (e.g. `fp_live_abc12345…`). */
            prefix: string;
            /** @example ci-deploy */
            label?: string | null;
            /**
             * @description Permission set attached to the key. Closed vocabulary (IAM-1, ADR-034 rev2): admin is the legacy full-access scope; apps:read covers GETs across the apps/deployments/audit/secrets-list surface; deploy:write covers POST/PUT/PATCH/DELETE on apps+queues; secrets:write covers PUT/DELETE on /apps/{slug}/secrets/{key}; usage:read covers GET /v1/usage*.
             * @example [
             *       "admin"
             *     ]
             */
            scopes: ("admin" | "deploy:write" | "secrets:read" | "secrets:write" | "usage:read" | "apps:read" | "env:read" | "env:write")[];
            /** Format: date-time */
            last_used_at?: string | null;
            /**
             * Format: date-time
             * @example 2026-07-25T13:25:00Z
             */
            created_at: string;
            /**
             * Format: date-time
             * @description When the key expires (RFC 3339). Absent on never-expiring admin keys.
             */
            expires_at?: string | null;
            /**
             * @description Status state machine. `active` = ready; `grace` = in the post-rotation window; `revoked` = terminal.
             * @enum {string}
             */
            status?: "active" | "grace" | "revoked";
            /**
             * Format: date-time
             * @description When the key was revoked (RFC 3339). Absent on active/grace keys.
             */
            revoked_at?: string | null;
            /** @description Predecessor key id when this row was minted by rotateKey. Absent on a fresh mint. */
            rotated_from_id?: string | null;
            /** @description PRESENT ONLY on POST /v1/keys and POST /v1/keys/{id}/rotate responses. Never returned again. */
            plaintext?: string | null;
        };
        /** @description API key creation payload — label and optional scopes. Plaintext is returned exactly once in the 201 response. Scopes defaults to `["admin"]` when omitted so existing callers preserve the legacy full-access behavior. See IAM-1, ADR-034 rev2. */
        CreateKeyRequest: {
            /** @example ci-deploy */
            label?: string;
            /**
             * @description Requested permission set. Server validates each entry against the closed vocabulary and rejects unknown scopes at mint time. `admin` is the legacy full-access scope; the other five cover narrower surfaces (see APIKeyResponse.scopes). See IAM-1, ADR-034 rev2.
             * @example [
             *       "apps:read",
             *       "deploy:write"
             *     ]
             */
            scopes?: ("admin" | "deploy:write" | "secrets:read" | "secrets:write" | "usage:read" | "apps:read" | "env:read" | "env:write")[];
        };
        /** @description Response body of POST /v1/keys/{id}/rotate. The new `key_plaintext` is returned exactly once; the old plaintext is NEVER returned (only the hash is stored). `old_key_expires_at` is the grace deadline applied to the predecessor — the customer's CI rotates over by then. */
        RotateKeyResponse: {
            key: components["schemas"]["APIKeyResponse"];
            /** @description New plaintext (PRESENT ONLY on this response). Capture immediately; the API never returns it again. */
            key_plaintext: string;
            /** @description Predecessor key id (matches Key.rotated_from_id). */
            old_key_id: string;
            /**
             * Format: date-time
             * @description Grace deadline applied to the old key (RFC 3339, UTC). When grace_window_days=0 the deadline is 'now' (atomic rotation).
             */
            old_key_expires_at: string;
        };
        /** @description POST /v1/orgs/{slug}/keys body. Mirrors `CreateKeyRequest` (PR 6 keeps them in lockstep) — label + optional scopes. Empty `scopes` defaults to `["admin"]` so existing callers preserve the legacy full-access behavior. See IAM-1, ADR-034 rev2. */
        CreateOrgAPIKeyRequest: {
            /** @example ci-deploy */
            label?: string;
            /**
             * @description Requested permission set for the org-scoped key. Server validates each entry against the closed vocabulary and rejects unknown scopes at mint time. `admin` is the legacy full-access scope; the other five cover narrower surfaces (see APIKeyResponse.scopes). See IAM-1, ADR-034 rev2. PR 6 keeps the legacy and org-scoped shapes in lockstep so SDK callers can swap one request body for the other.
             * @example [
             *       "apps:read",
             *       "deploy:write"
             *     ]
             */
            scopes?: ("admin" | "deploy:write" | "secrets:read" | "secrets:write" | "usage:read" | "apps:read" | "env:read" | "env:write")[];
        };
        /** @description GET /v1/orgs/{slug}/keys body. Returns every key minted against the org (active + grace + revoked). Newest first; matches the legacy `GET /v1/keys` ordering. */
        ListOrgAPIKeysResponse: {
            keys: components["schemas"]["APIKeyResponse"][];
        };
        /** @description POST /v1/orgs/{slug}/keys/{id}/rotate body. `label` overrides the new key's label (inherits from the predecessor when omitted); `grace_window_days` is the same per-account override as `PATCH /v1/account/keys/grace_window_days` — defaulting to the plan default when omitted (`api.DefaultAPIKeyGraceWindowDays = 7`). */
        RotateOrgAPIKeyRequest: {
            /** @example ci-deploy-rotated */
            label?: string;
            /** @description Days the predecessor stays valid after rotation. `0` is atomic; omitted/null falls back to the plan default. */
            grace_window_days?: number;
        };
        /** @description Response body of POST /v1/orgs/{slug}/keys/{id}/rotate. Mirrors `RotateKeyResponse`; org-scoped because the request was — both keys carry the same `org_id` in `APIKeyResponse.org_id`. The new `key_plaintext` is returned exactly once; the old plaintext is NEVER returned. */
        RotateOrgAPIKeyResponse: {
            key: components["schemas"]["APIKeyResponse"];
            /** @description New plaintext (PRESENT ONLY on this response). Capture immediately; the API never returns it again. Org-scoped mint; the plaintext belongs to the active org's id. */
            key_plaintext: string;
            /** @description Predecessor key id (matches Key.rotated_from_id). Same org as the new key (rotation is org-local, never re-bound). */
            old_key_id: string;
            /**
             * Format: date-time
             * @description Grace deadline applied to the old key (RFC 3339, UTC). When grace_window_days=0 the deadline is 'now' (atomic rotation). Mirrors the legacy `RotateKeyResponse.old_key_expires_at` contract; the org binding carries through in `APIKeyResponse.org_id`.
             */
            old_key_expires_at: string;
        };
        /** @description Body of PATCH /v1/account/keys/grace_window_days. `days` is the per-account override for the rotation grace window. `days=0` is atomic rotation; `days=null` (or omitted) clears the override and falls back to the plan default (api.DefaultAPIKeyGraceWindowDays = 7). */
        SetGraceWindowRequest: {
            /** @description Per-account override in days. 0 = atomic, null = use plan default. */
            days?: number | null;
        };
        /** @description Body of GET /v1/account/keys/grace_window_days. `days` is the customer's override (null = no override); `plan_default` is the platform default the rotation handler uses when the row is null. */
        GraceWindowResponse: {
            days: number | null;
            /** @description Platform default grace window in days (api.DefaultAPIKeyGraceWindowDays = 7). */
            plan_default: number;
        };
        /** @description Body of PATCH /v1/account/egress_allowlist_extra (issue #679 / PR-B / ADR-082). `extra` is the per-account additive budget on top of the plan's `apps.egress_allowlist` cap. `extra=0` clears the override (the plan cap is authoritative again); negative values or values above `max_extra` (1024) are rejected with `account_egress_allowlist_extra_out_of_range`. */
        SetAccountEgressAllowlistExtraRequest: {
            /** @description Requested additive budget, in CIDR count. 0 = clear the override; values above max_extra are rejected. */
            extra: number;
        };
        /** @description Body of GET /v1/account/egress_allowlist_extra and PATCH /v1/account/egress_allowlist_extra. The trio (extra, plan_cap, max_extra) lets the dashboard render the override + plan cap + global ceiling in a single round-trip. */
        AccountEgressAllowlistExtraResponse: {
            /** @description Effective additive budget currently in force. 0 = no override; the plan cap is authoritative. */
            extra: number;
            /** @description Plan cap on apps.egress_allowlist CIDR count (Pro 16, Scale 64; Free/Hobby 0 — those plans don't unlock the knob at all). */
            plan_cap: number;
            /** @description Global ceiling on the per-account override (api.MaxAccountEgressAllowlistExtra = 1024). Flat across plans; the validator rejects out-of-range values with `account_egress_allowlist_extra_out_of_range`. */
            max_extra: number;
        };
        /** @description One row of the customer's security event timeline. The `kind` taxonomy and per-kind `data` schema are documented in `docs/adr/035-auth-audit-events.md`. */
        AuditEventResponse: {
            /**
             * @description Audit event row id (bigint as string).
             * @example 42
             */
            id: string;
            /**
             * Format: date-time
             * @description When the event was recorded (RFC 3339, UTC).
             * @example 2026-07-25T13:25:00Z
             */
            at: string;
            /**
             * @description Which daemon wrote the row. `apid` for IAM-4 surface; `schedd` for state-transition events (instance wakes / parks / watchdog timeouts).
             * @example apid
             */
            actor: string;
            /**
             * @description Namespaced event kind. Common values: `auth.login`, `auth.logout`, `auth.session.created`, `auth.session.revoke`, `auth.sessions.revoke_all`, `auth.session.stolen`, `key.created`, `key.deleted`, `secret.set`, `secret.deleted`, `env.set`, `env.deleted`, `account.plan_changed`, `account.deletion_scheduled`, `account.deletion_restored`.
             * @example auth.login
             */
            kind: string;
            /**
             * @description Account id (uuid string form) the event was recorded against. Omitted when the event has no subject (e.g. system-level).
             * @example 0190f8e1-2a3b-7c4d-9e5f-6a7b8c9d0e1f
             */
            subject?: string;
            /**
             * @description Highest-severity classification for stateless.advisory rows (`high` for /data, /db, /var/lib/postgresql, /var/lib/mysql; `warn` for other closed paths; `info` for an empty batch). Omitted for non-stateless kinds and for pre-PR-427 stateless.advisory rows where the data.severity key is absent. Mega-PR B / stateless-DX work.
             * @example high
             * @enum {string}
             */
            severity?: "high" | "warn" | "info";
            /**
             * @description Kind-specific payload. Always a JSON object; the inner shape depends on `kind`. Plaintext values (e.g. secret VALUE) are NEVER carried in `data`.
             * @example {
             *       "method": "password"
             *     }
             */
            data: {
                [key: string]: unknown;
            };
        };
        /** @description Envelope for `GET /v1/audit-events`. `limit` echoes the effective limit applied by the handler so the SDK can render `showing N of M` without re-issuing the request. */
        ListAuditEventsResponse: {
            events: components["schemas"]["AuditEventResponse"][];
            /**
             * @description Effective limit applied (always 1..100).
             * @example 50
             */
            limit: number;
        };
        /**
         * @description One row of the FK-free `audit_log` table (issue #755 /
         *     PR-6). The row outlives the account it relates to so a
         *     regulator / DPO can re-derive the post-deletion state
         *     without joining back to a deleted `accounts` row. The
         *     table is append-only by spec (ISO 27001 SoA A.5.33 —
         *     retention forever); there is no UPDATE / DELETE
         *     permission in production and no Go-side path that would
         *     emit one.
         */
        AuditLogEntry: {
            /**
             * Format: uuid
             * @description Audit-log row id (uuid canonical form).
             * @example 0190f8e1-2a3b-7c4d-9e5f-6a7b8c9d0e1f
             */
            id: string;
            /**
             * @description Namespaced audit-log kind. The PR-6 surface currently
             *     emits only `account.deleted` from inside
             *     `(*PgStore).DeleteAccount` / `(*MemStore).DeleteAccount`
             *     so the regulator can replay post-deletion state. The
             *     closed vocabulary will widen in a follow-up PR if a
             *     future audit emitter reuses the table for a new
             *     evidence surface.
             * @example account.deleted
             */
            kind: string;
            /**
             * Format: uuid
             * @description Account id (uuid canonical form) the row was recorded
             *     against. Nullable in the schema (anonymous /
             *     background activity can emit rows); omitted on the
             *     wire when the column is NULL.
             * @example 0190f8e1-2a3b-7c4d-9e5f-6a7b8c9d0e1f
             */
            account_id?: string;
            /**
             * @description Email captured at copy-time inside
             *     `DeleteAccount`. The audit row is self-contained: a
             *     regulator reading a row for a UUID that no longer
             *     exists in `accounts` still sees the human identifier
             *     without joining back to a deleted accounts row.
             *     Omitted when the row was emitted by anonymous
             *     activity.
             * @example alice@example.com
             */
            account_email?: string;
            /**
             * @description Which daemon wrote the row. PR-6 emits `grace-sweep`
             *     from inside the DeleteAccount store method. Future
             *     emitters (follow-up PRs) will widen the vocabulary.
             * @example grace-sweep
             */
            actor?: string;
            /**
             * Format: date-time
             * @description When the audit-log row was recorded (RFC 3339, UTC).
             * @example 2026-08-08T12:00:00Z
             */
            received_at: string;
            /**
             * @description Kind-specific payload. Always a JSON object; the
             *     inner shape depends on `kind`. For
             *     `account.deleted`, the payload carries `source`
             *     (`grace-sweep` today), `email` (the verbatim
             *     captured email), and `actor` (`grace-sweep` today)
             *     so the dashboard can render the row without joining
             *     back to a deleted `accounts` row.
             * @example {
             *       "source": "grace-sweep",
             *       "email": "alice@example.com",
             *       "actor": "grace-sweep"
             *     }
             */
            data?: {
                [key: string]: unknown;
            };
        };
        /**
         * @description Envelope for `GET /v1/audit-log` and `GET /v1/audit-log/all`
         *     (issue #755 / PR-6). `entries` is newest-first
         *     (`received_at DESC, id DESC`) so the dashboard can render
         *     top-of-list without re-sorting. `limit` echoes the
         *     effective limit applied by the handler so the SDK can
         *     render `showing N of M` without re-issuing the request.
         */
        ListAuditLogResponse: {
            entries: components["schemas"]["AuditLogEntry"][];
            /**
             * @description Effective page size applied (always 1..100; mirrors the customer and operator /v1/audit-log routes).
             * @example 50
             */
            limit: number;
        };
        /**
         * @description One frame of the wake timeline (issue #517 / PR-C /
         *     ADR-064). The shape mirrors the typed event payloads
         *     the producers write — see `pkg/events/wake.go`. The
         *     canonical `wake.*` vocabulary is documented in
         *     `docs/adr/064-wake-timeline-canonical-vocabulary.md`
         *     (12 success-path kinds + 3 failure-path kinds: build,
         *     deploy, boot).
         */
        WakeTimelineEvent: {
            /**
             * Format: date-time
             * @description RFC 3339 UTC. Oldest-first (forward narrative).
             */
            at: string;
            /**
             * @description Canonical `wake.*` kind. See ADR-064.
             * @example wake.boot_started
             */
            kind: string;
            /**
             * @description Daemon that wrote the row (`schedd` / `vmmd` / `gatewayd` / `egress` / `builderd` / `apid`).
             * @example schedd
             */
            actor: string;
            /** @description Producer-supplied payload (json object). */
            data: {
                [key: string]: unknown;
            };
        };
        /** @description Envelope for `GET /v1/apps/{slug}/wakes/{wake_id}/timeline`. */
        WakeTimelineResponse: {
            /** @description Echo of the path-segment wake_id. */
            wake_id: string;
            /**
             * Format: uuid
             * @description Resolved app id (the slug's owning app).
             */
            app_id: string;
            events: components["schemas"]["WakeTimelineEvent"][];
            /** @description Opaque RFC 3339 cursor for the next page. Empty when this is the last page. */
            next_cursor?: string;
            /**
             * @description Effective limit applied (always 1..1000).
             * @example 200
             */
            limit: number;
        };
        /** @description Set a secret: key name and plaintext (sealed at rest immediately, plaintext discarded after seal). */
        PutAppSecretRequest: {
            /**
             * @description Plaintext. Sealed server-side; never persisted in plaintext.
             * @example sk_test_secret_value
             */
            value: string;
        };
        /**
         * @description Rotate a secret: new plaintext value (ADR-089 PR-B). Same wire
         *     shape as `PutAppSecretRequest`; the rotate verb is distinct so
         *     the server can emit the `secret.rotated` audit kind (vs
         *     `secret.set` on PUT). Byte cap is the per-plan
         *     `SecretValueMaxBytes`.
         */
        RotateAppSecretRequest: {
            /**
             * @description New plaintext. Sealed server-side; never persisted in plaintext.
             * @example sk_test_new_value
             */
            value: string;
        };
        /**
         * @description Response from `POST /v1/apps/{slug}/secrets/{key}/rotate`. The
         *     `kid` is the age-1... recipient string of the host identity that
         *     sealed the new envelope (ADR-089 D4); `rotated_at` is
         *     RFC3339Nano so two rotates in the same second produce distinct
         *     timestamps. Empty `kid` means the row was rotated but the kid
         *     was not stampable (rare — happens only if apid started without
         *     host.age.pub, which the handler 503s for instead).
         */
        RotateAppSecretResponse: {
            /** @example DATABASE_URL */
            key: string;
            /**
             * Format: date-time
             * @example 2026-08-10T11:37:52.123Z
             */
            rotated_at: string;
            /**
             * @description age-1... recipient string of the host identity that sealed this row. Empty if kid was not stampable.
             * @example age1q...
             */
            kid?: string;
        };
        /** @description A sealed secret envelope: key name, sealed ciphertext (server can't read it), version, and timestamps. */
        AppSecretResponse: {
            /** @example DATABASE_URL */
            key: string;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
            /**
             * @description age-1... recipient string of the host identity that sealed this row (ADR-089). Empty for rows sealed before migration 00166.
             * @example age1q...
             */
            kid?: string;
        };
        /** @description Paginated list of sealed-secret envelopes (no plaintext). */
        AppSecretListResponse: {
            secrets: components["schemas"]["AppSecretResponse"][];
            quota_max: number;
            count: number;
        };
        /**
         * @description Set a private-registry Basic Auth credential: normalized registry
         *     host + username + plaintext password. The password is sealed
         *     server-side under namespace `"registry_creds"` against the host
         *     X25519 recipient and never persisted in plaintext.
         */
        PutAppRegistryCredentialRequest: {
            /**
             * @description Registry host. Must include explicit `https://` prefix (schemeless + http:// are rejected per ADR-062 §https-only clarification; customer's Basic Auth never leaves the box over cleartext). Trailing slash optional; embedded path / query / fragment rejected.
             * @example https://registry.gregale.dev
             */
            registry: string;
            /**
             * @description Basic Auth username (metadata, NOT sealed).
             * @example alice
             */
            username: string;
            /**
             * Format: password
             * @description Plaintext Basic Auth password. Sealed server-side; never persisted or returned.
             * @example s3cret-basic-auth-pw
             */
            password: string;
        };
        /**
         * @description A sealed private-registry credential envelope: registry + username
         *     + timestamps. Plaintext password NEVER appears in this shape.
         */
        AppRegistryCredentialResponse: {
            /** @example registry.gregale.dev */
            registry: string;
            /** @example alice */
            username: string;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
            /**
             * Format: date-time
             * @description Timestamp of the last successful authenticated pull. Omitted when the credential has not been used yet.
             */
            last_used_at?: string;
        };
        /**
         * @description Wrapped list response: rows + quota metadata. The Free plan
         *     returns 403 on PUT and an empty list on GET.
         */
        AppRegistryCredentialListResponse: {
            credentials: components["schemas"]["AppRegistryCredentialResponse"][];
            /** @description Per-app cap from the customer's plan (Free 0, Hobby 2, Pro 5, Scale 20). */
            quota_max: number;
            count: number;
        };
        /** @description Set an env var: plaintext value (persisted verbatim in app_envs, non-sensitive by contract). */
        PutAppEnvRequest: {
            /**
             * @description Plaintext env-var value. Stored as-is; do NOT use this surface for credentials.
             * @example debug
             */
            value: string;
        };
        /** @description An env var envelope: key name + scope + timestamps. The plaintext value never appears here. */
        AppEnvResponse: {
            /** @example LOG_LEVEL */
            key: string;
            /**
             * @description Env-var scope (ADR-090). Always 'default' for the pre-PR-B flat response; varies for the nested `env_by_scope` response.
             * @example default
             */
            scope: string;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        /** @description An env var envelope scoped to one named environment (ADR-090). The plaintext value never appears here. */
        ScopedAppEnvResponse: {
            /** @example staging */
            scope: string;
            /** @example DB_URL */
            key: string;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        /**
         * @description List of env var envelopes (no plaintext values). Discriminated
         *     union (ADR-090 PR-B D3):
         *
         *       * `env` array populated, `env_by_scope` omitted: the
         *         default-scope or per-scope read (omitted or `?scope=<slug>`).
         *         `count` is the per-scope row count.
         *       * `env` empty, `env_by_scope` populated: the `?scope=__all__`
         *         read. `count` is the cross-scope row total.
         *
         *     Both arms are valid wire shapes for a GET; the `?scope=`
         *     query discriminates.
         */
        AppEnvListResponse: {
            env: components["schemas"]["AppEnvResponse"][];
            /** @description Nested per-scope map (ADR-090 PR-B D3). Populated only when `?scope=__all__` is passed; keys are scope names, values are per-scope row lists ordered by key ASC. */
            env_by_scope?: {
                [key: string]: components["schemas"]["ScopedAppEnvResponse"][];
            };
            quota_max: number;
            count: number;
        };
        /**
         * @description Upsert payload for a customer data upstream. The (kind, host, port)
         *     tuple is the deduplication key — repeating the PUT updates the
         *     existing row's `last_seen_at` and (if `FAAS_DATA_PLACEMENT=1`) the
         *     inferred-source tag. Plaintext host is never persisted; the on-disk
         *     column is `host_redacted_hash`.
         */
        PutDataUpstreamRequest: {
            /**
             * @description Closed vocabulary (ADR-098 §D1). Adding a new kind requires an ADR.
             * @enum {string}
             */
            kind: "postgres" | "redis" | "mongo" | "cassandra" | "clickhouse" | "elasticsearch" | "opensearch" | "rabbitmq" | "kafka" | "nats" | "minio" | "memcached" | "etcd" | "s3" | "https_api";
            /** @description RFC 952/1123 hostname (no IPv4). Hashed server-side; the hashed form is what's persisted. */
            host: string;
            port: number;
            /** @description ADR-090 deployment-scope filter (3..40 chars, lowercase alnum + dash). Omitted = default scope. */
            scope?: string;
        };
        /**
         * @description A single customer data upstream. The plaintext host is replaced by
         *     `host_redacted_hash` (sha256(salt||host) 8-hex prefix); the §11
         *     barrier means the wire format never carries the customer's DSN.
         */
        DataUpstreamResponse: {
            /** Format: uuid */
            id: string;
            /**
             * @description Whether the row was captured by the classifier (FAAS_DATA_PLACEMENT=1) or added via PUT (explicit).
             * @enum {string}
             */
            source: "inferred" | "explicit";
            /** @enum {string} */
            kind: "postgres" | "redis" | "mongo" | "cassandra" | "clickhouse" | "elasticsearch" | "opensearch" | "rabbitmq" | "kafka" | "nats" | "minio" | "memcached" | "etcd" | "s3" | "https_api";
            /** @description SHA-256 hex of (HostHashSalt||host). 64 lowercase hex chars, matching the schema CHECK constraint. */
            host_redacted_hash: string;
            /** @description First 8 hex chars of host_redacted_hash; safe for log/scrape correlation (8 chars = ~4B capacity). */
            host_last4?: string;
            port: number;
            /** @description ADR-090 deployment-scope filter (3..40 chars, lowercase alnum + dash). Echoes the value persisted on the row; absent when the default scope applies. */
            scope?: string;
            /** @description Region hint (nullable). Empty on capture; populated by the operator or the classify-flow follow-up. */
            declared_region?: string;
            /** @description Most recent probe RTT (ms). Omitted when no probe yet. */
            last_rtt_ms?: number;
            /**
             * Format: date-time
             * @description Timestamp of the most recent probe. Omitted when no probe yet.
             */
            last_probed_at?: string;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            last_seen_at: string;
        };
        /**
         * @description List response wrapper. `quota_max` is the per-plan cap from
         *     `pkg/api/limits.go::DataPlacementHintsPerApp`; `count` is the
         *     number of rows in `upstreams` (may be less than `quota_max`).
         */
        DataUpstreamListResponse: {
            upstreams: components["schemas"]["DataUpstreamResponse"][];
            quota_max: number;
            count: number;
        };
        /** @description Per-app usage for one month: GB-hours consumed, request count, and an informational CPU-µs field (issue #279 / PR-B). The CPU dimension is observable but not yet billed. */
        UsageResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
            /** Format: int64 */
            mb_seconds: number;
            /** Format: int64 */
            requests: number;
            /** Format: int64 */
            included_gb_hours: number;
            /**
             * Format: int64
             * @description Cumulative host cgroup CPU-µs (informational; not billed). issue #279 / PR-B.
             */
            cpu_usec?: number;
            /**
             * Format: int64
             * @description Per-app monthly HTTP response bytes the gateway forwarded (informational; not billed). ADR-046. The gateway-side producer lands in PR-2; until then this field stays 0. The future egress-billing PR picks the unit; this field reports interface bytes (includes Ethernet framing).
             */
            tx_bytes?: number;
            /**
             * Format: int64
             * @description Per-app monthly byte delta on root-side vethHost.rx_bytes (informational; not billed). ADR-046. Sourced from vmmd netstats.Cache via schedd ListInstanceStats. Includes Ethernet framing — same kernel counter the per-plan tc tbf qdisc reads, so the cap and the meter are consistent.
             */
            net_tx_bytes?: number;
            /**
             * Format: int64
             * @description Per-app monthly byte delta on root-side vethHost.tx_bytes (root→guest = ingress; informational; not billed). ADR-048. Mirror of `net_tx_bytes` for the inbound direction. Same sysfs source — `/sys/class/net/<vethHost>/statistics/tx_bytes`.
             */
            net_rx_bytes?: number;
            /**
             * Format: int64
             * @description Per-app monthly count of WAKE_RESTORE→WAKE_COLD_BOOT transitions observed (informational; not billed). ADR-048. Source: schedd instancestats.Poller → meterd Sampler.
             */
            cold_boots?: number;
        };
        /**
         * @description Per-app metrics snapshot (issue #273 / ADR-041). Latencies are
         *     milliseconds for the 2xx class only; failures surface as
         *     `error_rate_pct`. `wake_p95_ms` is the FLEET p95 — the
         *     underlying `gateway_wake_latency_seconds` histogram is
         *     unlabeled. On Prometheus failure the endpoint returns 200 with
         *     zeroed fields and `source: "degraded: <reason>"`.
         */
        AppMetricsResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
            /**
             * @description Echoed window.
             * @enum {string}
             */
            range: "5m" | "15m" | "1h" | "6h" | "24h" | "7d" | "15d";
            /** @description "prometheus" on success, "degraded: <reason>" otherwise. */
            source: string;
            /**
             * Format: date-time
             * @description RFC3339Nano UTC.
             */
            as_of: string;
            /**
             * Format: int64
             * @description Share of requests in the window. Drives the dashboard empty state.
             */
            request_count: number;
            /** @description p50 of `gateway_request_duration_seconds_bucket{class="2xx"}` over the window, in ms. */
            latency_p50_ms: number;
            /** @description p95 over 2xx traffic in the window, in ms. */
            latency_p95_ms: number;
            /** @description p99 over 2xx traffic in the window, in ms. */
            latency_p99_ms: number;
            /** @description Share of [45]xx requests in the window. */
            error_rate_pct: number;
            /**
             * @description Share of requests that triggered a cold boot (the WakeGate
             *     leader). Followers waiting on the gate see zero cold
             *     contribution; their wait is on the §12 dashboard via
             *     `gateway_wake_queue_wait_seconds`.
             */
            cold_start_pct: number;
            /** @description FLEET p95 wake latency (the unlabeled histogram). Labelled as such in the UI. */
            wake_p95_ms: number;
            /**
             * Format: int64
             * @description Per-app egress byte delta over the window (informational; not billed). ADR-046. Source: schedd_egress_net_tx_bytes_total{app} (Prom rollup of usage_minutes.net_tx_bytes — PR-2 wires the rollup; until then this field stays 0).
             */
            egress_bytes?: number;
            /**
             * Format: int64
             * @description Per-app egress byte delta over the window, gateway-side mirror (informational; not billed). ADR-046 PR-2 / issue #415 PR-2. Source: gateway_egress_tx_bytes_total{app} (Prom rollup; the gatewayd-internal-local per-instance egress ring populates the counter on each raw-stream chunk). Distinct from `egress_bytes` (the schedd-side mirror) so a divergence between the two surfaces immediately on the dashboard. Best-effort: query failure does NOT flip the response to degraded — matches the `egress_bytes` semantics.
             */
            tx_bytes?: number;
            /**
             * @description Per-route breakdown for opt-in apps (ADR-093). Absent when
             *     `route_metrics_enabled` is false on the app — the dashboard
             *     distinguishes "feature off" (field absent) from "feature
             *     on, no traffic" (empty array). Each row is the bounded
             *     detail from the gatewayd-internal in-memory reader: max
             *     50 distinct routes + the `__route_other__` wildcard-path
             *     overflow bucket. The route label is method + raw path
             *     (pre-rewrite, ADR-093 D6).
             */
            routes?: components["schemas"]["RouteRow"][];
        };
        /**
         * @description Per-route detail row (ADR-093). The `route` field is the
         *     bounded label: `"GET /users/4f8a"` for an admitted route, or
         *     `"__route_other__"` for the overflow bucket. Latency fields
         *     are milliseconds over the full request duration (status-
         *     agnostic — failures included). Same zero-on-degraded
         *     contract as `AppMetricsResponse`.
         */
        RouteRow: {
            /** @description Bounded route label (method + raw path, or `__route_other__`). */
            route: string;
            /**
             * Format: int64
             * @description Number of requests with this route in the window.
             */
            count: number;
            /** @description p50 of `gateway_request_duration_seconds_bucket{app, route, class}` over the window, in ms. */
            p50_ms: number;
            /** @description p95 over all classes in the window, in ms. */
            p95_ms: number;
            /** @description p99 over all classes in the window, in ms. */
            p99_ms: number;
            /** @description Share of [45]xx requests with this route in the window. */
            error_pct: number;
        };
        /**
         * @description Per-route label snapshot (ADR-093). The bounded route label
         *     set the gatewayd-internal control listener emits for the app.
         *     Each item is `"<METHOD> <PATH>"` (pre-edge-rule-rewrite) for
         *     an admitted route, or the reserved `"__route_other__"` overflow
         *     bucket label. Bounded at 50 distinct real routes + the reserved
         *     overflow per app (ADR-093 D2).
         *
         *     `cap_hit` (ADR-093 Tier B item #1) is true iff the app's
         *     route label set has reached `RouteMetricsPerAppCap` (50) and
         *     additional routes are collapsing into the reserved
         *     `__route_other__` overflow bucket. When `cap_hit` is true,
         *     `len(routes) == RouteMetricsPerAppCap + 2` (50 real + the
         *     reserved empty label + `__route_other__`). When false, the
         *     dashboard can render "you have N admitted routes" without
         *     having to count the array (which is ambiguous: 5 real routes
         *     + `__route_other__` is indistinguishable from 50 real routes
         *     + overflow). Omitted on the `source: unavailable` path —
         *     the gatewayd-internal dial failed, the cap state is unknown.
         */
        AppRoutesResponse: {
            slug: string;
            app_id?: string;
            routes: string[];
            /** @enum {string} */
            source: "live" | "unavailable";
            /**
             * @description True iff the route label set has hit `RouteMetricsPerAppCap`
             *     (50) and additional routes are collapsing into
             *     `__route_other__`. Omitted on `source: unavailable` paths
             *     (cap state is unknown when the gatewayd-internal dial
             *     fails).
             * @default false
             */
            cap_hit: boolean;
        };
        /**
         * @description One (route → suggested rate) row in the payload returned by
         *     `GET /v1/apps/{slug}/throttle-suggestions` (ADR-091 D20.5
         *     amendment, issue #881). The recommender is read-only — it
         *     never auto-applies — and the suggestion is always ≤ the
         *     customer's plan ceiling so a customer can act on it
         *     without a 422 from apid's sub-plan validator.
         */
        ThrottleSuggestionRow: {
            /**
             * @description The bounded label exactly as emitted on the Prometheus
             *     side: `<METHOD> <PATH>` (pre-edge-rule-rewrite), or
             *     the reserved `__route_other__` overflow bucket label.
             * @example GET /users/4f8a
             */
            route: string;
            /**
             * Format: double
             * @description The `rate()` value over the window (already per-second).
             * @example 1.5
             */
            observed_rps: number;
            /**
             * Format: double
             * @description `ceil(observed_rps * multiplier)` clamped into
             *     `[1, plan.RateLimitRPS]`. The 2× headroom is echoed
             *     on the wire so the value is auditable rather than
             *     magic.
             * @example 3
             */
            suggested_rps: number;
            /**
             * @description `ceil(suggested_rps * 1.5)` clamped into
             *     `[1, plan.RateLimitBurst]`. The 1.5× factor is a
             *     softer version of the rate headroom — burst oversize
             *     is the most common cause of customer-flapping 429s.
             * @example 5
             */
            suggested_burst: number;
            /**
             * Format: double
             * @description The headroom factor the recommender applied to
             *     `observed_rps`. Pinned on the wire so a future
             *     strategy change is distinguishable from drift.
             * @example 2
             */
            multiplier: number;
        };
        /**
         * @description Per-route throttle recommendation payload (ADR-091 D20.5
         *     amendment, issue #881). Source is `prometheus` on success
         *     or `degraded: <reason>` on Prometheus failure (response is
         *     still 200 with empty Suggestions — the dashboard's
         *     empty-state branch handles it).
         */
        ThrottleSuggestionsResponse: {
            app_id: string;
            range: string;
            /**
             * @description `prometheus` on success, `degraded: <reason>` on
             *     PromQL failure.
             */
            source: string;
            /**
             * Format: date-time
             * @description RFC 3339 UTC timestamp the response was assembled.
             */
            as_of?: string;
            /**
             * @description True when `apps.route_metrics_enabled=false` (Free
             *     plan). The response carries empty Suggestions plus
             *     this flag so the dashboard can render the upsell
             *     rather than a misleading zero.
             * @default false
             */
            route_metrics_disabled: boolean;
            /**
             * @description Count of routes that collapsed into the reserved
             *     `__route_other__` overflow bucket during the window
             *     (ADR-093 cap = 50). A non-zero value indicates the
             *     throttle is partial-coverage regardless of the
             *     configured limit.
             */
            routes_collapsed: number;
            /**
             * @description `plan.RateLimitRPS` — the sub-plan ceiling the
             *     suggestion is clamped to. 0 on unknown plans (fail
             *     OPEN — the apid sub-plan validator is the
             *     authoritative gate).
             */
            plan_ceiling_rps: number;
            /**
             * @description `plan.RateLimitBurst` — the sub-plan ceiling for
             *     burst. 0 on unknown plans.
             */
            plan_ceiling_burst: number;
            /**
             * Format: double
             * @description The headroom factor the recommender applied to every
             *     route (`Multiplier` constant). Echoed on the wire
             *     so the strategy is auditable.
             * @example 2
             */
            multiplier: number;
            suggestions: components["schemas"]["ThrottleSuggestionRow"][];
        };
        /**
         * @description Per-app streaming classification (ADR-102 D6). The probe
         *     returns the same `Streaming-Status` enum value the
         *     gatewayd handler would stamp on the response header for a
         *     representative request to this app, plus the effective
         *     response-body cap and the per-gate flags that produced it.
         *
         *     `status` values (closed enum, see `pkg/api/limits.go`):
         *
         *       - `streaming` — request will stream. `effective_cap_bytes`
         *         is the cap `capWriter` will enforce.
         *       - `accept-json-downgrade` — pre-D3 customers with
         *         `Accept: application/json` would have been buffered.
         *         Post-D3 (this PR), the request streams regardless of
         *         Accept; the enum variant survives one cycle so pinned
         *         SDKs can grep for it.
         *       - `flag-disabled` — `streaming_enabled=false` on the app.
         *       - `plan-disallows` — plan tier forbids `streaming_enabled=true`;
         *         CreateApp (D5) already returns 403 on this combination,
         *         so this value should be unreachable on a properly-validated
         *         app.
         *       - `operator-disabled` — operator opt-in env is off; visible
         *         only on the gatewayd side, not this probe.
         *       - `upgrade-bypass` — request is an HTTP/1.1 Upgrade (e.g.
         *         WebSocket) and bypasses the streaming path.
         *
         *     `effective_cap_bytes` is the plan cap (`cap_kind="plan"`)
         *     on every probe in this PR. The per-edge-rule override lives
         *     in gatewayd state and is not part of the apid cache; a
         *     customer who needs the live cap fires a real request and
         *     reads the `Streaming-Status` response header.
         */
        AppStreamingStatus: {
            app_id: string;
            /** @enum {string} */
            status: "streaming" | "accept-json-downgrade" | "flag-disabled" | "plan-disallows" | "operator-disabled" | "upgrade-bypass";
            /** Format: int64 */
            effective_cap_bytes: number;
            /** Format: int64 */
            plan_cap_bytes: number;
            flag_enabled: boolean;
            plan_allowed: boolean;
            /**
             * @description `plan` is the only value this probe ever returns in
             *     this PR. The endpoint-rule and none values are reserved
             *     for the future gatewayd-side dial path described in
             *     `cmd/apid/handlers_streaming_cap.go`.
             * @enum {string}
             */
            cap_kind?: "plan" | "endpoint-rule" | "none";
        };
        /**
         * @description Page shape for `GET /v1/instances` (issue #393). `instances`
         *     is the page in started_at DESC, id DESC order. `next_before`
         *     is the cursor the caller passes on the next request to fetch
         *     the older page; empty when the page is the end.
         */
        ListInstancesResponse: {
            instances: components["schemas"]["InstanceResponse"][];
            /**
             * Format: uuid
             * @description Cursor (instances.id UUIDv7) for the next older page. Empty / null at the end.
             */
            next_before?: string | null;
        };
        /**
         * @description One row in `GET /v1/secrets` — a sealed envelope on a specific
         *     app (issue #393). Plaintext NEVER appears here: only the
         *     age-sealed envelope (base64). `app_id` and `app_slug` let the
         *     dashboard render "foo-app / DATABASE_URL" without a parallel
         *     `/v1/apps` round-trip.
         */
        AccountAppSecretResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
            app_slug: string;
            key: string;
            /** @description base64 age-sealed envelope. Plaintext NEVER appears on this wire. */
            ciphertext: string;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        /**
         * @description Page shape for `GET /v1/secrets` (issue #393). `secrets` is
         *     the page in (app_slug ASC, key ASC) order. `next_before` is
         *     the cursor the caller passes on the next request — encoded as
         *     `<slug>|<key>`. Empty / null at the end.
         */
        ListSecretsForAccountResponse: {
            secrets: components["schemas"]["AccountAppSecretResponse"][];
            /** @description Cursor in the form `<slug>|<key>`. Empty / null at the end. */
            next_before?: string | null;
        };
        /**
         * @description Account-wide per-app metrics rollup for `GET /v1/apps/metrics?range=`
         *     (issue #393). One call replaces N per-app
         *     `/v1/apps/{slug}/metrics` calls.
         *
         *     `apps` is keyed by `app_slug` so the dashboard can render the
         *     rows without a parallel `/v1/apps` lookup. The per-app values
         *     are `AppMetricsResponse` — the same shape as the per-app
         *     endpoint emits, so the dashboard renders the rollup with one
         *     code path.
         *
         *     `wake_p95_ms` per app is the FLEET p95 (the underlying
         *     `gateway_wake_latency_seconds` histogram is unlabeled — there
         *     is no per-app wake histogram).
         *
         *     On Prometheus failure the endpoint returns 200 with `apps:
         *     null` and `source: "degraded: <reason>"`, matching the
         *     per-app contract exactly so the dashboard has one empty-state
         *     branch across both endpoints.
         */
        AppsMetricsResponse: {
            /**
             * @description Time window that was queried for every per-app rollup row.
             * @enum {string}
             */
            range: "5m" | "15m" | "1h" | "6h" | "24h" | "7d" | "15d";
            /** @description "prometheus" on success; "degraded: <reason>" otherwise. */
            source: string;
            /**
             * Format: date-time
             * @description RFC3339Nano UTC stamp at which the rollup was assembled.
             */
            as_of: string;
            /** @description Per-app `AppMetricsResponse`, keyed by app_slug. Null when degraded. */
            apps: {
                [key: string]: components["schemas"]["AppMetricsResponse"];
            } | null;
        };
        /**
         * @description Shared latency sub-shape used by `AppSLOResponse` and
         *     `AccountSLOResponse`. Three percentiles over the SLO
         *     window (2xx class only); NaN/Inf from `histogram_quantile`
         *     on an empty window is coerced to 0 by the handler.
         */
        SLODuration: {
            /** @description p50 latency in milliseconds. */
            p50_ms: number;
            /** @description p95 latency in milliseconds. */
            p95_ms: number;
            /** @description p99 latency in milliseconds. */
            p99_ms: number;
        };
        /**
         * @description Per-app SLO panel returned by `GET /v1/apps/{slug}/slo?window=`
         *     (issue #696 / ADR-082). Distinct from `AppMetricsResponse`
         *     (issue #273 / ADR-042): the SLO surface is a fixed-window
         *     (1h/24h/7d) summary of the customer-facing SLO signals,
         *     not a 5m slice for the dashboard. The fields overlap only
         *     on latency percentiles, error rate, and cold-boot rate — the
         *     remaining fields (`wake_queue_p95_ms`, `throttled_total`,
         *     `instance_hours`, `gb_hours`) are net-new per the issue.
         *
         *     On Prometheus failure the endpoint returns 200 with
         *     zeroed fields and `source: "degraded: <reason>"`. When
         *     Postgres is down but the PromQL pass succeeded, only
         *     `instance_hours` / `gb_hours` are zeroed and `source` is
         *     `"degraded: postgres unavailable"`.
         */
        AppSLOResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
            app_slug: string;
            /**
             * @description Echoed SLO window, e.g. `24h`.
             * @enum {string}
             */
            window: "1h" | "24h" | "7d";
            /** @description "prometheus" on success; "degraded: <reason>" otherwise. Per-app shape: when Postgres fails only instance_hours/gb_hours are zeroed. */
            source: string;
            /**
             * Format: date-time
             * @description RFC3339Nano UTC stamp at which the SLO panel was assembled.
             */
            as_of: string;
            request_duration: components["schemas"]["SLODuration"];
            /** @description Share of [45]xx requests in the window for this app. */
            error_rate_pct: number;
            /** @description Share of requests that triggered a cold boot. */
            cold_boot_rate_pct: number;
            /** @description Sum of instance × minute / 60 over the window (from `usage_minutes`). */
            instance_hours: number;
            /** @description Sum of mb_seconds / 3600 / 1024 over the window (from `usage_minutes`). */
            gb_hours: number;
            /** @description FLEET wake-queue p95 (`gateway_wake_queue_wait_seconds` is unlabeled). */
            wake_queue_p95_ms: number;
            /** Format: int64 */
            requests_total: number;
            /**
             * Format: int64
             * @description Per-app rate-limit count over the window.
             */
            throttled_total: number;
        };
        /**
         * @description Grouped error summary returned by `GET /v1/apps/{slug}/errors/summary`
         *     (ADR-096 / PR-B). One row per fingerprint over the
         *     requested `[since, until]` window. Distinct from
         *     `AppSLOResponse` (ADR-082) which is the closed-set SLO
         *     summary — the errors summary uses a continuous window
         *     with explicit RFC3339Nano stamps.
         *
         *     Empty result returns 200 with `items: []` and the
         *     window echo set. `window_clamped` is true when the
         *     requested span was clamped to `AppErrorsWindowMaxHours`
         *     (168h). `next_cursor` is empty when the page is the
         *     last one.
         */
        AppErrorsSummaryResponse: {
            /**
             * Format: date-time
             * @description RFC3339Nano UTC stamp at which the summary was assembled.
             */
            generated_at: string;
            app_id: string;
            app_slug: string;
            /**
             * Format: date-time
             * @description Echoed (clamped) window start, RFC3339Nano UTC.
             */
            window_start: string;
            /**
             * Format: date-time
             * @description Echoed (clamped) window end, RFC3339Nano UTC.
             */
            window_end: string;
            /** @description True when the requested span was clamped to AppErrorsWindowMaxHours (168h). */
            window_clamped: boolean;
            items: components["schemas"]["AppErrorSummaryItem"][];
            /** @description Opaque base64 cursor for the next page. Empty when the current page is the last. */
            next_cursor?: string | null;
            /** @description Echoed limit applied (post-clamp to AppErrorsSummaryMaxLimit=100). */
            limit: number;
        };
        /** @description One row of the errors summary (ADR-096 / PR-B). */
        AppErrorSummaryItem: {
            /** @description 64-hex-char SHA-256 fingerprint identifying this error group; stable across requests with the same (route, status, error_class). */
            fingerprint: string;
            /**
             * @description Closed-vocabulary error class. 'unhandled' is collapsed under 'Other' in the UI; the DB stores the precise class.
             * @enum {string}
             */
            error_class: "db_timeout" | "stripe_timeout" | "null_pointer" | "invalid_json" | "wake_failed" | "upstream_5xx" | "unhandled" | "client_error";
            /** @description Matched route template (e.g. `/users/{id}`), NEVER the expanded URL. */
            route: string;
            /** Format: int32 */
            http_status: number;
            /**
             * Format: int64
             * @description Issue count (deduped within AppErrorsDedupeWindowSeconds=3600).
             */
            count: number;
            /**
             * Format: int64
             * @description Total distinct request rows for this fingerprint.
             */
            request_count: number;
            /** Format: date-time */
            first_seen_at: string;
            /** Format: date-time */
            last_seen_at: string;
            /** @description PII-redacted sample message (already-redacted at write time; ≤AppErrorsSampleMessageCapBytes=512 bytes). */
            sample_message: string;
        };
        /**
         * @description Drill-down page returned by `GET /v1/apps/{slug}/errors/{fingerprint}`
         *     (ADR-096 / PR-B). The header fields (fingerprint,
         *     error_class, route, http_status) are denormalised from the
         *     parent summary row so the dashboard renders the page header
         *     without a second round-trip. Does NOT include
         *     `headers_sample` or `redactions` — those are on the
         *     `/first` endpoint only.
         */
        AppErrorRequestsResponse: {
            fingerprint: string;
            error_class: string;
            route: string;
            /** Format: int32 */
            http_status: number;
            requests: components["schemas"]["AppErrorRequestItem"][];
            next_cursor?: string | null;
        };
        /** @description One row of the drill-down page (ADR-096 / PR-B). */
        AppErrorRequestItem: {
            request_id: string;
            /** Format: date-time */
            received_at: string;
            route: string;
            /** Format: int32 */
            http_status: number;
            error_class: string;
            sample_message: string;
            /** @description Nullable — the FK is ON DELETE SET NULL so an evicted deployment leaves the drill-down row intact. */
            deployment_id?: string | null;
        };
        /**
         * @description Single sample row returned by `GET /v1/apps/{slug}/errors/{fingerprint}/first`
         *     (ADR-096 / PR-B). Embeds `AppErrorRequestItem` plus the
         *     redacted `headers_sample` (jsonb-decoded to map) and the
         *     `redactions_applied` pattern names so the dashboard can
         *     render a "we redacted X / Y / Z" badge.
         */
        AppErrorSampleResponse: {
            request_id: string;
            /** Format: date-time */
            received_at: string;
            route: string;
            /** Format: int32 */
            http_status: number;
            error_class: string;
            sample_message: string;
            deployment_id?: string | null;
            /** @description PII-redacted request headers (≤8 keys). */
            headers_sample: {
                [key: string]: string;
            };
            /** @description Names of the redactor patterns that fired (e.g. `email`, `card`, `bearer`). */
            redactions_applied: string[];
        };
        /**
         * @description Flat account-wide SLO rollup returned by
         *     `GET /v1/account/slo?window=` (issue #696 / ADR-082).
         *     Mirrors `AppSLOResponse` field-for-field except for
         *     `app_id` / `app_slug` (the rollup is account-wide). The
         *     fields are scalar sums/rates across the account; per-app
         *     drill-down is served by the existing `/v1/apps/metrics`
         *     endpoint.
         *
         *     `source` follows the same `"degraded: <reason>"` contract
         *     as the per-app endpoint.
         */
        AccountSLOResponse: {
            /** @enum {string} */
            window: "1h" | "24h" | "7d";
            /** @description "prometheus" on success; "degraded: <reason>" otherwise. Account-wide shape: when Postgres fails only instance_hours/gb_hours are zeroed. */
            source: string;
            /** Format: date-time */
            as_of: string;
            request_duration: components["schemas"]["SLODuration"];
            error_rate_pct: number;
            cold_boot_rate_pct: number;
            /** @description Sum of instance-minutes / 60 across all apps for the account. */
            instance_hours: number;
            /** @description Sum of mb_seconds / 3600 / 1024 across all apps for the account. */
            gb_hours: number;
            wake_queue_p95_ms: number;
            /** Format: int64 */
            requests_total: number;
            /** Format: int64 */
            throttled_total: number;
        };
        /** @description Account-level monthly roll-up: included GB-hours, used, overage math, remaining balance, and informational used_cpu_hours (issue #279 / PR-B). The CPU dimension is observable but not yet billed; the GB-hours fields drive the overage math. */
        UsageSummaryResponse: {
            /** @example 2026-07 */
            month: string;
            /** @example 1.234 */
            used_gb_hours: number;
            /** Format: int64 */
            included_gb_hours: number;
            overage_gb_hours: number;
            /**
             * Format: int64
             * @description Integer cents. Overages are €0.01/GB-h.
             */
            overage_cents: number;
            /** @description Per-month CPU-hours (informational; not billed). issue #279 / PR-B. */
            used_cpu_hours?: number;
            /** @description Per-month egress GB (informational; not billed). Σ tx_bytes + net_tx_bytes across all apps, converted to GB. ADR-046. */
            used_egress_gb?: number;
            /** @description Per-month ingress GB (informational; not billed). Σ net_rx_bytes across all apps, converted to GB. ADR-048. Mirror of `used_egress_gb` for the inbound direction. */
            used_ingress_gb?: number;
            /**
             * Format: int64
             * @description Per-month sum of WAKE_RESTORE→WAKE_COLD_BOOT transitions across every app on the account (informational; not billed). ADR-048.
             */
            cold_boots?: number;
        };
        /** @description 202-side of async-invoke. The id is the invocations row id; status_url is the well-known read endpoint. */
        AsyncInvokeResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            /** @example /v1/invocations/0123456789abcdef0123456789abcdef */
            status_url: string;
        };
        /** @description Sync-invoke result. Status is the drain-driven terminal state (`completed` | `failed` | `cancelled`). Result is the original row's payload cast to JSON (omitted while still pending). */
        InvokeResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            /** @enum {string} */
            status: "pending" | "dispatching" | "completed" | "failed" | "cancelled";
            result?: {
                [key: string]: unknown;
            };
        };
        /** @description 201 — body of a freshly-enqueued queue row. */
        QueueSendResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
        };
        /** @description 200 — a dequeued row (the long-poll hit). 204 (no body) on timeout. */
        QueueReceiveResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            payload: {
                [key: string]: unknown;
            };
            result?: {
                [key: string]: unknown;
            };
        };
        /** @description 200 — queue depth / in-flight / oldest-pending stats. Read-only. */
        QueueStateResponse: {
            app_slug: string;
            /** @enum {string} */
            plan: "free" | "hobby" | "pro" | "scale";
            /** @description MaxQueueDepth for the plan. */
            plan_cap: number;
            /** @description Pending + dispatching row count. */
            depth: number;
            /** @description Rows with a live dispatch lease (state=dispatching, lease_expires_at > now or NULL). */
            in_flight: number;
            /**
             * Format: date-time
             * @description Oldest pending row's created_at; null when the queue is empty.
             */
            oldest_pending_at?: string | null;
            /** @description Convenience field — seconds since oldest_pending_at; null when the queue is empty. */
            oldest_pending_age_seconds?: number | null;
            /** Format: date-time */
            generated_at: string;
        };
        /** @description One pending row. No lease was acquired and `attempts` was not incremented. */
        QueuePeekMessage: {
            id: string;
            /** Format: date-time */
            created_at: string;
            attempts: number;
            /** @description Stored payload rendered as a JSON string (verbatim from the jsonb column). */
            payload: string;
            /** @description Most recent failure reason, when attempts > 0. */
            last_error?: string;
        };
        /** @description 200 — a page of pending rows ordered by created_at ASC, id ASC. Pass `next_before` as `?before=<id>` for the next page. */
        QueuePeekResponse: {
            app_slug: string;
            messages: components["schemas"]["QueuePeekMessage"][];
            /** @description Omitted when the page is the final one. */
            next_before?: string;
        };
        /** @description One row that exhausted the plan's retry budget (state='dead_letter'). */
        QueueDeadLetterMessage: {
            id: string;
            /** Format: date-time */
            created_at: string;
            /**
             * Format: date-time
             * @description When the drain transitioned the row to dead_letter.
             */
            failed_at: string;
            attempts: number;
            last_error: string;
            payload: string;
        };
        /** @description 200 — a page of dead-letter rows ordered by created_at DESC, id DESC. */
        QueueDeadLetterResponse: {
            app_slug: string;
            messages: components["schemas"]["QueueDeadLetterMessage"][];
            /** @description Cursor for the previous page; absent on the final page. */
            next_before?: string;
        };
        /** @description Delayed task create/get shape. ScheduledAt is the customer-facing UTC dispatch time. State is populated on get, omitted on create (always `pending` there). */
        DelayedTaskResponse: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            /** Format: date-time */
            scheduled_at: string;
            /** @enum {string} */
            state?: "pending" | "dispatching" | "completed" | "failed" | "cancelled";
        };
        /** @description A single invocations row. Account-scoped; cross-account reads return 404. */
        Invocation: {
            /** @example 0123456789abcdef0123456789abcdef */
            id: string;
            /** @example 0123456789abcdef0123456789abcdef */
            app_id: string;
            /** @example 0123456789abcdef0123456789abcdef */
            account_id: string;
            /** @enum {string} */
            source: "async_invoke" | "queue" | "delayed_task" | "cron" | "replay";
            /** @enum {string} */
            state: "pending" | "dispatching" | "completed" | "failed" | "cancelled" | "dead_letter";
            method?: string;
            path?: string;
            payload?: {
                [key: string]: unknown;
            };
            headers?: {
                [key: string]: unknown;
            };
            /** Format: date-time */
            scheduled_at?: string | null;
            /** Format: date-time */
            due_at?: string;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            completed_at?: string | null;
            instance_id?: string | null;
            result?: {
                [key: string]: unknown;
            } | null;
            last_error?: string | null;
            /** @description Optional ack URL for queueReceive consumers; populated on queue-sourced rows. */
            ack_url?: string | null;
            /** @description Number of dispatch attempts so far; 0 on the first try. */
            attempts?: number;
            /**
             * Format: date-time
             * @description When the in-flight dispatch lease expires; null when no lease is held.
             */
            lease_expires_at?: string | null;
            /**
             * Format: date-time
             * @description When the drain first claimed the row; null until claimed.
             */
            received_at?: string | null;
        };
        /** @description Page of invocations; ordered by created_at DESC, id DESC. Pass the LAST id of the returned slice as the next `?before=` to load older. */
        ListInvocationsResponse: {
            invocations: components["schemas"]["Invocation"][];
        };
        /** @description Body for POST /v1/apps/{slug}/invoke[/async]. Method defaults to POST; path defaults to `/`. */
        InvokeRequest: {
            payload?: {
                [key: string]: unknown;
            };
            headers?: {
                [key: string]: unknown;
            };
            /** @default POST */
            method: string;
            /** @default / */
            path: string;
        };
        /** @description Body for POST /v1/apps/{slug}/queues/send. Cap-checked against MaxQueueDepth. */
        QueueSendRequest: {
            payload?: {
                [key: string]: unknown;
            };
        };
        /** @description Body for POST /v1/apps/{slug}/delayed-tasks. scheduled_at must be in the future (UTC). */
        DelayedTaskRequest: {
            payload?: {
                [key: string]: unknown;
            };
            /** Format: date-time */
            scheduled_at: string;
        };
        /**
         * @description One row per dashboard login (ADR-039 / IAM-3). The
         *     cookie envelope carries the row's `id` as `sid`;
         *     every authenticated request re-validates the row.
         *     Revoked rows are filtered out of the
         *     `GET /v1/auth/sessions` response.
         */
        SessionInfo: {
            /**
             * Format: uuid
             * @description Session id. Also stamped as `sid` in the cookie envelope.
             */
            id: string;
            /** Format: uuid */
            account_id: string;
            /**
             * @description Client IP captured at login (host portion only; IPv4 or IPv6). Empty when the request's RemoteAddr was unparseable.
             * @example 192.0.2.10
             */
            issued_ip?: string;
            /** @description User-Agent header captured at login. May be empty when the client suppressed the header. */
            issued_ua?: string;
            /**
             * Format: date-time
             * @description When the session was minted (RFC 3339, UTC).
             */
            issued_at: string;
            /**
             * Format: date-time
             * @description Last time the cookie cross-validated this row. Updated with a 5-minute debounce; continues post-revoke (operational signal only, not authorization).
             */
            last_seen_at?: string;
            /** @description True when this row's id matches the calling cookie's sid. Exactly one row per list response has this flag set. */
            current_session: boolean;
        };
        /** @description Body for `GET /v1/auth/sessions`. */
        SessionListResponse: {
            /** @description Active sessions, newest first. Revoked rows are not returned. */
            sessions: components["schemas"]["SessionInfo"][];
        };
        /** @description Body for `POST /v1/auth/sessions/revoke_all`. */
        SessionsRevokeAllResponse: {
            /** @description Number of sibling sessions revoked. The caller's session is NOT included in this count. */
            revoked: number;
        };
        /**
         * @description Per-field entry of `Problem.errors`. The shape mirrors
         *     Cloudflare's API Shield 422 / Stripe's `card_errors` family so
         *     an SDK can iterate `errors[]` to drive form-field UI without
         *     parsing prose. `field` uses JSON Pointer notation for nested
         *     keys (`address.zip`). `expected` and `got` are short stable
         *     strings; consumers should not depend on the prose.
         */
        FieldError: {
            /** @example name */
            field: string;
            /** @example string */
            expected: string;
            /** @example integer */
            got?: string;
        };
        /**
         * @description Per-line entry of `Problem.secret_findings` AND
         *     `SecretScanResult.findings` (issue #862 + secret-scan v2,
         *     PR #873; PR-A 101 extends the surfaced shape via the new
         *     `layer` field). The shape mirrors `pkg/secretscan.Finding`
         *     but is decoupled so the wire schema can evolve
         *     independently of the scanner's internal fields. `snippet`
         *     is the pre-truncated safe representation (first 6 chars +
         *     "…" + last 4) — never the raw value, matching the snippet
         *     policy documented in `pkg/secretscan/scan.go`. Closed-set
         *     `provider` keys: stripe_live, stripe_test, github_pat,
         *     aws_access, openai, anthropic, private_key_block. The
         *     optional `layer` field (PR-A) names the per-walk source
         *     label — "app" for the main image, "sidecar-<slug>" for
         *     each sidecar, or absent for the apid source-tree
         *     rejection path (legacy).
         */
        SecretFinding: {
            /** @example .env.production */
            file: string;
            /** @example 2 */
            line: number;
            /** @example STRIPE_SECRET_KEY */
            key?: string;
            /** @example stripe_live */
            provider: string;
            /**
             * @example high
             * @enum {string}
             */
            severity: "high" | "medium";
            /** @example sk_liv…XXXX */
            snippet: string;
            /**
             * @description Per-walk source label (PR-A). "app" for findings in
             *     the main image; "sidecar-<slug>" for findings in a
             *     sidecar (e.g. "sidecar-redis"); absent or "" for
             *     findings from the legacy apid source-tree rejection
             *     path (cmd/apid/secretscan.go).
             * @example app
             */
            layer?: string;
        };
        /**
         * @description Customer-facing wire shape of one deployment's secret-scan
         *     audit row. Mirrors the `ScanResult` shape but for the
         *     secret-scan pipeline. The `status` enum is the closed set
         *     the imaged-side writer (PR-A) stamps: `complete` for a
         *     clean walk, `complete_with_redactions` for a hit (mirrors
         *     the v2 widening on `deployments_scan_status_chk` from
         *     migration 00264). `image_digest` is the OCI digest the
         *     scan ran against (PR-A) — mirrors
         *     `ScanResult.image_digest` so a side-by-side compare
         *     renders both scans against the same bytes. `findings[]`
         *     may be empty (clean walk); the field is always present
         *     for round-trip JSON stability.
         */
        SecretScanResult: {
            /** @example complete */
            status: string;
            /**
             * Format: date-time
             * @description RFC 3339 UTC. Empty when the deployment hasn't been
             *     secret-scanned yet.
             */
            scanned_at?: string;
            /**
             * @description OCI digest the scan ran against (PR-A). Mirrors
             *     `ScanResult.image_digest` for cross-pipeline
             *     comparison. Empty for pre-PR-A rows.
             * @example sha256:abc...
             */
            image_digest?: string;
            findings: components["schemas"]["SecretFinding"][];
            /**
             * @description In-band explanation when the audit row is unreadable
             *     (jsonb decode failed). Mirrors `ScanResult.error`.
             *     Empty on the success path — `status` carries the
             *     closed-set signal there.
             */
            error?: string;
        };
        /**
         * @description RFC 7807 problem+json envelope. The `code` field is the stable
         *     machine-readable identifier; clients branch on it. `limit` and
         *     `observed` are populated on quota errors. `docs_url` points the
         *     user at the next action. `billing_portal_url` is populated on
         *     `code: payment_required` so the dashboard can deep-link the
         *     customer to the Stripe-hosted billing portal (issue #142).
         *     `paddle_checkout_url` + `tx_id` are populated instead when the
         *     box is running on the Paddle billing provider
         *     (`FAAS_BILLING_PROVIDER=paddle`, ADR-025) — the customer lands
         *     on a Paddle-hosted checkout page for the target plan and the
         *     dashboard renders the transaction handle as a confirmation id.
         *     Exactly one of `billing_portal_url` or `paddle_checkout_url` is
         *     populated on a given 402 — never both.
         *
         *     `errors` carries per-field detail (Cloudflare / Stripe shape)
         *     for 422 sites that emit a list of field-level failures — used
         *     today by the kind=validate edge rule so a JSON Schema
         *     rejection renders as a form-field list the dashboard can
         *     iterate without parsing prose. Optional + omitempty so every
         *     other problem+json site keeps its existing flat shape unchanged.
         * @example {
         *       "type": "https://docs.gregale.dev/errors/validation_failed",
         *       "title": "Validation failed",
         *       "status": 422,
         *       "code": "validation_failed",
         *       "detail": "ram_mb must be one of [128, 256, 512, 1024, 2048]",
         *       "limit": null,
         *       "observed": null,
         *       "docs_url": "https://docs.gregale.dev/errors/validation_failed"
         *     }
         */
        Problem: {
            /**
             * Format: uri
             * @example https://docs.gregale.dev/errors/validation_failed
             */
            type?: string;
            /** @example Validation failed */
            title: string;
            /** @example 422 */
            status: number;
            /** @description Stable machine-readable error code. See StatusForCode in pkg/api/errors.go. */
            code: string;
            detail?: string;
            limit?: number | null;
            observed?: number | null;
            /** Format: uri */
            docs_url?: string;
            /** Format: uri */
            billing_portal_url?: string;
            /**
             * Format: uri
             * @description Paddle-hosted checkout URL on a `payment_required` 402 when
             *     the box is running on the Paddle billing provider. Mutually
             *     exclusive with `billing_portal_url`.
             */
            paddle_checkout_url?: string;
            /**
             * @description Paddle transaction handle (`txn_…`) on a `payment_required`
             *     402. Empty on the Stripe path. The dashboard renders this as
             *     a confirmation id after the customer completes checkout.
             */
            tx_id?: string;
            /**
             * @description Per-field validation detail. Populated by 422 sites that
             *     emit a list of field-level failures. Each entry is a
             *     `FieldError` (Cloudflare / Stripe shape: field + expected
             *     + got) so an SDK can drive form-field UI without parsing
             *     prose.
             */
            errors?: components["schemas"]["FieldError"][];
            /**
             * @description Per-line secret-scan detail. Populated by 422 sites with
             *     `code: secret_scan_strict` (cmd/apid/secretscan.go
             *     server-side scan rejection; cmd/gregale printErr
             *     --secret-scan=strict client-side rejection). The shape
             *     is shared with the on-disk `SecretScanResult` so a
             *     programmatic consumer can render the same UI for both
             *     rejection paths. Optional + omitempty.
             */
            secret_findings?: components["schemas"]["SecretFinding"][];
            /**
             * @description Customer-facing remediation nudge attached to a
             *     `code: secret_scan_strict` 422 envelope (e.g. "move
             *     detected secrets to `gregale secrets set`"). Mirrors
             *     the `FieldError` shape's prose pattern so the dashboard
             *     / SDK can render the hint as a one-line footer without
             *     parsing prose. Optional + omitempty.
             * @example move detected secrets to `gregale secrets set` (see https://docs.gregale.dev/cli/secrets)
             */
            secret_hint?: string;
        };
        /**
         * @description Plain-text body returned ONLY by the authlimiter middleware
         *     (`pkg/middleware/authlimit.go`, around line 200). All other 429s
         *     return a `Problem` with `code: plan_limit_concurrency` or
         *     `code: quota_exhausted`.
         */
        RateLimitPlain: string;
        /**
         * @description Empty body — the customer brings only their session cookie.
         *     Kept as a real struct (not `additionalProperties: false`) so
         *     `{}` decodes cleanly without a "no JSON object" parse error.
         */
        MFAEnrollRequest: Record<string, never>;
        /** @description Action-bound CSRF token for browser clients. */
        CSRFTokenResponse: {
            /** @description Opaque token to echo in the mutation's JSON body. */
            csrf_token: string;
        };
        /**
         * @description One-shot enrollment payload. Returned exactly once on
         *     /enroll. The server persists `mfa_secret_encrypted` (sealed)
         *     and `mfa_recovery_codes_hash` (SHA-256d); subsequent calls
         *     to /enroll overwrite the secret + codes but do NOT
         *     re-surface the plaintexts to the dashboard.
         */
        MFAEnrollResponse: {
            /**
             * @description Standard `otpauth://totp/...` URL with the issuer = FaaS.
             *     The customer's authenticator app ingests this on its own;
             *     the dashboard also embeds the QR for camera-based setup.
             * @example otpauth://totp/FaaS:alice@example.com?secret=JBSWY3DPEHPK3PXP&issuer=FaaS
             */
            otpauth_url: string;
            /**
             * @description Base32-encoded TOTP secret, 32 chars (no padding). Same
             *     value embedded in the otpauth URL; surfaced here so the
             *     dashboard can render the secret directly for
             *     copy-paste into an authenticator app that doesn't read
             *     URLs.
             */
            secret: string;
            /**
             * Format: byte
             * @description Base64-encoded PNG bytes of the QR code (256×256). The
             *     server base64-encodes the raw PNG for JSON transport;
             *     the dashboard decodes the string back to bytes before
             *     rendering it in an `<img>` tag. The authenticator scans
             *     the decoded PNG.
             */
            qr_code_png_base64: string;
            /**
             * @description Ten single-use 10-character base32 strings. The
             *     dashboard renders them in the "save these somewhere"
             *     step. Each code is hashed (SHA-256) before storage;
             *     the plaintext never reappears.
             */
            recovery_codes: string[];
        };
        /** @description Body for /confirm — a single 6-digit TOTP code. */
        MFAConfirmRequest: {
            /** @description 6-digit rotating code from the authenticator app. */
            totp: string;
            /**
             * @description Dashboard CSRF token. The dashboard sets a
             *     `faas_csrf` cookie on every authenticated response; the
             *     same value rides in this JSON-body field so
             *     `pkg/middleware.VerifyAuthenticated` can compare them.
             *     The token is opaque to the wire (sealed session
             *     reference); size matches the cookie value.
             */
            csrf_token?: string;
        };
        /**
         * @description Empty success body. The meaningful side effect is the
         *     `Set-Cookie: faas_sid=…` header — re-issued without the
         *     `mfa_pending` flag — and the stamp of `mfa_enrolled_at`.
         */
        MFAConfirmResponse: Record<string, never>;
        /**
         * @description Body for /verify — same 6-digit code as /confirm. The
         *     account is already enrolled; the verify path does NOT re-
         *     stamp enrolled_at, only re-issues the cookie without
         *     mfa_pending.
         */
        MFAVerifyRequest: {
            totp: string;
            /**
             * @description Dashboard CSRF token (absorbed from the body for
             *     parity with /confirm; /verify is not token-gated
             *     because the TOTP code itself IS the second factor).
             */
            csrf_token?: string;
        };
        /** @description Empty success body. Same semantics as /confirm response. */
        MFAVerifyResponse: Record<string, never>;
        /**
         * @description Body for /recover — one of the 10 recovery codes the
         *     customer received on /enroll. The hash is removed from
         *     the stored set; subsequent /recover calls with the same
         *     code return 401.
         */
        MFARecoverRequest: {
            code: string;
            /** @description Dashboard CSRF token for the recover mutation. */
            csrf_token?: string;
        };
        /**
         * @description Empty success body. Side effects: the matching hash is
         *     removed from `mfa_recovery_codes_hash`; the session cookie
         *     is re-issued without `mfa_pending`.
         */
        MFARecoverResponse: Record<string, never>;
        /**
         * @description Body for /disable. Exactly one of `password` or
         *     `recovery_code` is required — both empty and both set are
         *     rejected with 400 CodeValidation. Password is verified
         *     against the existing `account_passwords.hash`; the
         *     recovery code is consumed (one-time).
         */
        MFADisableRequest: {
            password?: string;
            recovery_code?: string;
            /** @description Dashboard CSRF token for the disable mutation. */
            csrf_token?: string;
        };
        /**
         * @description Empty success body. Side effects: mfa_secret_encrypted,
         *     mfa_recovery_codes_hash, and mfa_enrolled_at are all NULL.
         *     mfa_required is left as-is so an explicit policy remains in
         *     force after disable.
         */
        MFADisableResponse: Record<string, never>;
        /**
         * @description One entry in the per-app cosign trusted-publisher list. `name` is the
         *     apid-side label (matches `app_trusted_signers.signer_name`); `public_key_pem`
         *     is the base64-encoded DER SubjectPublicKeyInfo bytes — NOT a PEM-armoured
         *     block. The wire form strips the PEM armour at upload time so the
         *     on-disk mirror (imaged reads `/etc/faas/secrets/trusted-publishers/`) is
         *     a single blob per publisher. ECDSA P-256 only (ADR-038); apid rejects
         *     other curves at PUT time with `trusted_signer_invalid`.
         */
        TrustedSigner: {
            /**
             * @description Lower-case label. Matches `app_trusted_signers.signer_name`. The label is the on-disk filename (without .pem) under /etc/faas/secrets/trusted-publishers/.
             * @example ci-bot
             */
            name: string;
            /**
             * @description Base64-encoded DER SubjectPublicKeyInfo. Bytes length must be in [64, 1024] (DB CHECK).
             * @example MFkwEwYHKoZIzj0CAQYIKoZIzj0DAQcDQgAE...
             */
            public_key_pem: string;
            /**
             * Format: date-time
             * @description RFC3339 timestamp of when the operator onboarded this publisher.
             */
            added_at: string;
            /**
             * @description Account ID of the admin who onboarded this publisher (omit when not yet in audit log).
             * @example 0123456789abcdef0123456789abcdef
             */
            added_by?: string;
        };
        /**
         * @description PUT body for `/v1/apps/{slug}/trusted_signers/{name}`. `public_key_pem` is
         *     the base64-encoded DER blob (apid side strips PEM armour). The DER must
         *     parse as an ECDSA P-256 SPKI; any other curve or non-ECDSA key returns
         *     400 `trusted_signer_invalid`. Bytes length must land in [64, 1024].
         */
        AddTrustedSignerRequest: {
            /**
             * @description Base64-encoded DER SubjectPublicKeyInfo. Length bounds match the DB CHECK.
             * @example MFkwEwYHKoZIzj0CAQYIKoZIzj0DAQcDQgAE...
             */
            public_key_pem: string;
        };
        /** @description GET /v1/apps/{slug}/trusted_signers response body. Empty list is the EXPECTED state for any app with require_signed=false. */
        AppTrustedSignerListResponse: {
            signers: components["schemas"]["TrustedSigner"][];
        };
        /**
         * @description PATCH body for `/v1/apps/{slug}/security`. `require_signed` is a
         *     pointer so the wire form can distinguish "don't touch" (nil) from
         *     "explicit true/false" — the same Set-bit convention the broader
         *     UpdateAppRequest uses (issue #471 streaming flag precedent).
         */
        AppSecurityRequest: {
            /**
             * @description Operator-only toggle. nil = no change. *true = opt in to signature enforcement (requires the trust list to be non-empty). *false = opt out.
             * @example true
             */
            require_signed?: boolean | null;
        };
        /** @description PATCH /v1/apps/{slug}/security response body — the updated flag state. */
        AppSecurityResponse: {
            /**
             * @description The current state of the require_signed flag after the patch.
             * @example true
             */
            require_signed: boolean;
        };
        /**
         * @description POST /v1/orgs body. Slug matches `OrgSlugPattern`
         *     (lowercase alphanumeric + dashes, 3..32 chars); name is
         *     trimmed-non-empty, capped at 256 chars. Personal orgs
         *     cannot be created via this endpoint — every account
         *     already owns an immutable personal org (PR 3 backfill).
         */
        CreateOrgRequest: {
            /** @example acme */
            slug: string;
            /** @example Acme Inc. */
            name: string;
        };
        /**
         * @description PATCH /v1/orgs/{slug} body. Both fields are pointer-typed
         *     so the handler distinguishes "omitted" (leave alone) from
         *     "zero" (clear/empty). Authz routing:
         *       - name → org.manage_billing (owner + billing roles)
         *       - plan → org.change_plan (owner only)
         */
        PatchOrgRequest: {
            /** @example Acme Inc. (renamed) */
            name?: string | null;
            /**
             * @example pro
             * @enum {string|null}
             */
            plan?: "free" | "hobby" | "pro" | "scale" | null;
        };
        /**
         * @description Canonical org wire shape. RFC3339 timestamps; zero-time
         *     serialises as empty string. Mirrors `pkg/api.OrgResponse`.
         */
        OrgResponse: {
            /** @description Org UUID (stable across renames). */
            id: string;
            /** @description Lowercase slug. Personal orgs use `u-<12hex>`. */
            slug: string;
            name: string;
            /** @description True iff this is the caller's personal org (every account has exactly one). */
            personal: boolean;
            /**
             * @description Plan tier. Personal orgs default to `free`.
             * @enum {string}
             */
            plan: "free" | "hobby" | "pro" | "scale";
            /** @enum {string} */
            status: "active" | "past_due" | "suspended" | "deleted_pending";
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        /** @description GET /v1/orgs response. Sorted by slug. */
        OrgListResponse: {
            orgs: components["schemas"]["OrgResponse"][];
        };
        /**
         * @description OrgResponse + the caller's role on the active org. Used by
         *     GET /v1/orgs/me (`OrgMeResponse.org`). The role field is
         *     a closed enum: owner|admin|developer|viewer|billing.
         */
        OrgWithRole: components["schemas"]["OrgResponse"] & {
            /** @enum {string} */
            role: "owner" | "admin" | "developer" | "viewer" | "billing";
        };
        /**
         * @description GET /v1/orgs/me response. `org` is null when no X-Active-Org /
         *     ?org= hint was supplied (the passthrough case every
         *     pre-PR-5 route depends on).
         */
        OrgMeResponse: {
            /** @description null when no active-org hint was supplied; otherwise the active org + caller's role. */
            org: ((string & components["schemas"]["OrgWithRole"]) | null) & components["schemas"]["OrgWithRole"];
        };
        /**
         * @description Wire shape for a single org membership row. Email is the
         *     joined account.email; the account_id is also surfaced for
         *     non-ambiguous referential purposes (e.g. role-change / remove
         *     path segments).
         */
        OrgMemberResponse: {
            account_id: string;
            /**
             * Format: email
             * @example bob@acme.com
             */
            email: string;
            /** @enum {string} */
            role: "owner" | "admin" | "developer" | "viewer" | "billing";
            /** Format: date-time */
            joined_at: string;
        };
        /** @description GET /v1/orgs/{slug}/members response. Only active members (removed rows filtered). */
        MemberListResponse: {
            members: components["schemas"]["OrgMemberResponse"][];
        };
        /**
         * @description GET /v1/orgs/{slug}/seat_usage response. Visibility-only —
         *     PR 9 ships the per-seat pricing cut-over per ADR-061
         *     §"Out of scope". `used` counts active members only (the
         *     store's `CountActiveOrgMembers` filters `removed_at IS
         *     NULL`). `limit` comes from `org.Plan.OrgMembersMax()` —
         *     the `free` plan returns `0` so the dashboard can render
         *     "personal org only" instead of "0 of 0 used".
         */
        SeatUsageResponse: {
            /** @description Active member count. */
            used: number;
            /**
             * @description Plan cap on active members (`Plan.OrgMembersMax()`).
             *     Returns `0` for the `free` plan — the fail-closed
             *     accessor shape.
             */
            limit: number;
            /** @enum {string} */
            plan: "free" | "hobby" | "pro" | "scale";
        };
        /**
         * @description PATCH /v1/orgs/{slug}/members/{user_id} body. Role cannot
         *     be `owner` on this endpoint — transfer-ownership is the
         *     only path to owner.
         */
        ChangeMemberRoleRequest: {
            /** @enum {string} */
            role: "admin" | "developer" | "viewer" | "billing";
        };
        /**
         * @description POST /v1/orgs/{slug}/members body. Role cannot be `owner`.
         *     The handler mints a 32-byte plaintext token (returned ONCE
         *     in the response) and stores only the SHA-256 hash. The
         *     token expires after 14 days.
         */
        InviteMemberRequest: {
            /** Format: email */
            email: string;
            /** @enum {string} */
            role: "admin" | "developer" | "viewer" | "billing";
        };
        /**
         * @description Wire shape for a single invitation row. Status is the
         *     runtime-derived value (pending | consumed | revoked |
         *     expired) computed from the (consumed_at, revoked_at,
         *     expires_at) tuple. Plaintext token is NEVER re-served —
         *     it's returned ONCE on the create call via
         *     InvitationWithTokenResponse.
         */
        OrgInvitationResponse: {
            /** @description 32-hex opaque invitation id (NOT canonical UUID). */
            id: string;
            org_id: string;
            org_slug: string;
            /** Format: email */
            email: string;
            /** @enum {string} */
            role: "admin" | "developer" | "viewer" | "billing";
            /** @enum {string} */
            status: "pending" | "consumed" | "revoked" | "expired";
            /** Format: date-time */
            expires_at: string;
            /** Format: date-time */
            created_at: string;
        };
        /**
         * @description POST /v1/orgs/{slug}/members response. Carries the
         *     one-time plaintext token in addition to the canonical
         *     invitation shape. Never re-served on subsequent reads —
         *     losing the token means revoking the invitation and
         *     inviting again.
         */
        InvitationWithTokenResponse: components["schemas"]["OrgInvitationResponse"] & {
            /**
             * @description 32-byte plaintext token, base64url-encoded (44 chars).
             *     Returned ONCE; never re-served. SHA-256 hash is what's
             *     stored. Treat as a secret — anyone with this token
             *     can accept the invitation.
             * @example Y3J5cHRvX3JhbmRvbV8zMl9ieXRlc19iYXNlNjR1cmw=
             */
            token: string;
        };
        /** @description GET /v1/orgs/{slug}/invitations response. Sorted by created_at DESC. */
        InvitationListResponse: {
            invitations: components["schemas"]["OrgInvitationResponse"][];
            /**
             * @description Opaque cursor — set to the **base64-url-of-JSON** of
             *     the last row's compound `(created_at, id)` key when
             *     there's a next page. Pass back as `?before=` to
             *     fetch it. PR-9 changes the encoding from the v1
             *     bare-id cursor to the compound key so the
             *     `created_at DESC, id DESC` order is preserved
             *     exactly under random UUIDs. The wire shape is
             *     opaque; clients MUST pass the value back unchanged.
             */
            next_before?: string;
        };
        /**
         * @description POST /v1/orgs/{slug}/transfer_ownership body. The new owner
         *     must already be an active member of the org; the previous
         *     owner becomes admin on success. The exactly-one-owner
         *     invariant is enforced by the partial unique index
         *     `org_memberships_one_owner_idx` (migration 00099).
         */
        TransferOwnershipRequest: {
            new_owner_account_id: string;
        };
        /**
         * @description Issue #555. A single OpenTelemetry trace — the span tree for one
         *     request (one wake). The shape mirrors the SDK's ReadOnlySpan
         *     flattened to JSON: trace_id + span_id are W3C hex, attributes
         *     are a string map (the operator's debug session is a grep, not
         *     a query). The same trace_id may host a
         *     `gateway.handler` → `gateway.route` → `sched.wake` →
         *     `vmmd.create_*` → `guest.resume` → `guest.readiness` chain
         *     (issue #555 acceptance #1).
         */
        Trace: {
            trace_id: string;
            /** Format: date-time */
            started_at?: string;
            /** Format: date-time */
            last_seen_at?: string;
            spans: components["schemas"]["TraceSpan"][];
        };
        /**
         * @description One span in a trace. Mirrors the OTel SDK's ReadOnlySpan
         *     attributes (kind, name, status, parent linkage, timing) so
         *     the customer's debug session can reconstruct the call tree.
         */
        TraceSpan: {
            trace_id: string;
            span_id: string;
            parent_span_id?: string;
            /** @example sched.wake */
            name: string;
            /** Format: date-time */
            start_time?: string;
            /** Format: date-time */
            end_time?: string;
            /** @enum {string} */
            status?: "ok" | "error";
            status_message?: string;
            attributes?: {
                [key: string]: string;
            };
        };
        /**
         * @description An outbound webhook subscription. Carries the masked HMAC secret;
         *     the sealed ciphertext is server-side only.
         * @example {
         *       "id": "0123456789abcdef0123456789abcdef",
         *       "app_id": "fedcba9876543210fedcba9876543210",
         *       "account_id": "8b1f5e5d-273e-5a18-ae00-58fceba4fe6c",
         *       "target_url": "https://example.com/hook",
         *       "webhook_secret_sealed_masked": "***",
         *       "event_filter": [
         *         "cron.fired",
         *         "app.created"
         *       ],
         *       "retry_policy": "default",
         *       "enabled": true,
         *       "created_at": "2026-08-06T10:00:00Z",
         *       "updated_at": "2026-08-06T10:00:00Z"
         *     }
         */
        AppWebhookResponse: {
            id: string;
            app_id: string;
            /** Format: uuid */
            account_id: string;
            target_url: string;
            /** @enum {string} */
            webhook_secret_sealed_masked: "***";
            event_filter: string[];
            /** @enum {string} */
            retry_policy: "default" | "aggressive" | "none";
            enabled: boolean;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        /**
         * @description Subscribe a target URL to events emitted by the app. The
         *     webhook_secret is HMAC-SHA256 sealed at rest with the host
         *     X25519 recipient (namespace `APP_WEBHOOK`); apid mints a fresh
         *     32-byte secret if omitted.
         * @example {
         *       "target_url": "https://example.com/hook",
         *       "webhook_secret": "shh",
         *       "event_filter": [
         *         "cron.fired",
         *         "app.created"
         *       ],
         *       "retry_policy": "default",
         *       "enabled": true
         *     }
         */
        CreateAppWebhookRequest: {
            target_url: string;
            webhook_secret: string;
            event_filter?: ("cron.fired" | "app.created" | "app.deleted" | "build.succeeded" | "build.failed")[];
            /**
             * @default default
             * @enum {string}
             */
            retry_policy: "default" | "aggressive" | "none";
            /** @default true */
            enabled: boolean;
        };
        /**
         * @description Partial update of an existing webhook subscription. Every
         *     field is optional — the handler merges the supplied fields
         *     onto the current row. omit a field to leave it unchanged.
         * @example {
         *       "target_url": "https://example.com/hook2",
         *       "enabled": true
         *     }
         */
        UpdateAppWebhookRequest: {
            target_url?: string;
            webhook_secret?: string;
            event_filter?: ("cron.fired" | "app.created" | "app.deleted" | "build.succeeded" | "build.failed")[];
            /** @enum {string} */
            retry_policy?: "default" | "aggressive" | "none";
            enabled?: boolean;
        };
        /**
         * @description Response from POST /v1/apps/{slug}/webhooks/{id}/rotate-secret.
         *     The plaintext secret is NEVER returned; the masked field is
         *     a constant sentinel so the dashboard renders the same shape
         *     across all secret-bearing rows (mirrors AlertRule rotate).
         * @example {
         *       "rotated_at": "2026-08-06T12:00:00Z",
         *       "webhook_secret_sealed_masked": "***"
         *     }
         */
        RotateAppWebhookSecretResponse: {
            /** Format: date-time */
            rotated_at: string;
            /** @enum {string} */
            webhook_secret_sealed_masked: "***";
        };
        /**
         * @description One row per (event × target) emission. The dispatcher mutates
         *     this row in place as attempts progress; the GET /deliveries
         *     endpoint returns this shape per row. attempt=7 + status='dead'
         *     is the DLQ state; the customer-facing retry endpoint flips a
         *     dead row back to status='pending' for one more shot.
         * @example {
         *       "id": "0123456789abcdef0123456789abcdef",
         *       "webhook_id": "fedcba9876543210fedcba9876543210",
         *       "app_id": "8b1f5e5d273e5a18ae0058fceba4fe6c",
         *       "account_id": "8b1f5e5d-273e-5a18-ae00-58fceba4fe6c",
         *       "event": "cron.fired",
         *       "payload": {
         *         "cron_name": "nightly-rollup"
         *       },
         *       "attempt": 0,
         *       "status": "pending",
         *       "next_attempt_at": "2026-08-06T10:00:00Z",
         *       "created_at": "2026-08-06T10:00:00Z",
         *       "updated_at": "2026-08-06T10:00:00Z"
         *     }
         */
        AppWebhookDeliveryResponse: {
            id: string;
            webhook_id: string;
            app_id: string;
            /** Format: uuid */
            account_id: string;
            event: string;
            /** @description The original event payload (omitted on rows past the first attempt; the customer has already seen it). */
            payload?: {
                [key: string]: unknown;
            };
            attempt: number;
            /** @enum {string} */
            status: "pending" | "in_flight" | "succeeded" | "failed" | "dead";
            last_error?: string;
            last_response_code?: number;
            /** Format: date-time */
            next_attempt_at: string;
            /** Format: date-time */
            delivered_at?: string;
            /** Format: date-time */
            created_at: string;
            /** Format: date-time */
            updated_at: string;
        };
        /**
         * @description Paged deliveries surface for the dashboard's "recent
         *     deliveries" pane. page_token is opaque + base64-encoded —
         *     treat it as a cursor; do not parse it.
         */
        AppWebhookDeliveryListResponse: {
            deliveries: components["schemas"]["AppWebhookDeliveryResponse"][];
            /** @description Cursor for the next page; empty/absent when the page is the last. */
            next_token?: string;
        };
        /**
         * @description Response from POST /v1/apps/{slug}/webhooks/{id}/deliveries/{did}/retry.
         *     Mirrors AppWebhookDeliveryResponse; the row in `delivery` is
         *     re-emitted with status='pending' and next_attempt_at=now().
         */
        AppWebhookRetryDeliveryResponse: {
            delivery: components["schemas"]["AppWebhookDeliveryResponse"];
        };
    };
    responses: {
        /** @description code: unauthorized */
        Unauthorized: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: invalid_credentials — wrong email or password. Drafted for the dashboard auth surface (issue #165 PR #2, ADR-032). */
        InvalidCredentials: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: reset_token_invalid | reset_token_expired — the password-reset token is unknown, already consumed, or expired. */
        ResetTokenInvalid: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: billing_past_due — account is suspended; pay invoice to resume. */
        PaymentRequired: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: admission_refused — the account's spend cap (accounts.overage_cap_cents) is met/exceeded by the current-month overage. Schedd refuses new wakes until the customer raises or clears the cap via POST /v1/account/overage-cap. The Limit / Observed fields carry the cap and current overage in integer cents so a script can compute "how much to raise" without parsing prose. No Retry-After: the cap is a deliberate customer budget, not back-pressure. */
        AdmissionRefused: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: not_found */
        NotFound: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: conflict */
        Conflict: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: account_deletion_confirm_required | account_deletion_pending | account_not_restorable */
        ConflictDelete: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: app_rename_failed — slug taken by another live app, or DB unique violation. */
        ConflictRename: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: forbidden — caller is authenticated but lacks the required scope, OR plan_limit_trusted_signers / plan_limit_secret / etc. when the resource count would exceed the plan cap. */
        Forbidden: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: app_not_found — slug does not exist for the authenticated account. */
        AppNotFound: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: capacity — server-side error; retry with backoff. */
        ServerError: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: no_rollback_target — there is no superseded deployment to roll back to. */
        NoRollbackTarget: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: validation_failed | source_invalid | build_undetected | handler_missing | image_required | cron_invalid | secret_invalid_key */
        ValidationFailed: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: plan_limit_apps | plan_limit_ram | plan_limit_concurrency | plan_min_instances_not_allowed | plan_limit_secrets | plan_cron_quota | app_layer_too_large | image_egress_denied */
        PlanLimit: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: plan_limit_secrets */
        PlanLimitSecrets: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: plan_limit_concurrency */
        ConcurrencyLimit: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: source_too_large */
        SourceTooLarge: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: image_egress_denied — registry is in RFC1918 / IMDS / link-local, or blocked egress range. */
        SourceEgressDenied: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: secret_value_too_large */
        SecretValueTooLarge: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: secret_not_found */
        SecretNotFound: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: plan_registry_credentials_not_allowed — Free plan cannot store private-registry credentials. */
        PlanRegistryCredentialsNotAllowed: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: plan_registry_credential_quota — per-app host cap reached (Hobby 2 / Pro 5 / Scale 20). */
        PlanRegistryCredentialQuota: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: invalid_registry_host — host missing scheme/path, uppercase, port out of range, or field-length cap exceeded. */
        InvalidRegistryHost: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: registry_credential_not_found — DELETE on a host that has no sealed credential. */
        RegistryCredentialNotFound: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /**
         * @description Generic 503 envelope. Used by the apid capacity gate (e.g.
         *     host age recipient not loaded → registry credential PUT
         *     returns 503 instead of accepting plaintext).
         */
        ServiceUnavailable: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: plan_limit_env_vars */
        PlanLimitEnvVars: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: env_value_too_large */
        EnvValueTooLarge: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: env_var_invalid_key */
        EnvVarInvalidKey: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: env_scope_invalid — ?scope= failed the EnvScopePattern check (empty, too long, or out-of-shape slug). */
        EnvScopeInvalid: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: env_var_not_found */
        EnvVarNotFound: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: cron_invalid | plan_crons_not_allowed | plan_cron_quota */
        CronInvalid: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: alert_rule_invalid | plan_alert_rules_not_allowed | plan_alert_rule_quota | image_egress_denied */
        AlertRuleInvalid: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: edge_rule_conflict — duplicate or overlapping rule state rejected. */
        EdgeRuleConflict: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: deploy_failed | image_not_found | image_manifest_invalid | build_oom | build_timeout | stateless_only_violation */
        DeployFailed: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: invalid_min_instances — must be in [0, plan max_concurrency]. */
        InvalidMinInstances: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: capacity_unavailable — no host headroom (alerting; should be near-impossible). */
        CapacityUnavailable: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: feature_not_allowed — request targets a feature the plan does not entitle (async_invoke / queues / delayed_tasks on Free). */
        PlanFeatureGated: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: plan_queue_depth — per-app queue at the plan's MaxQueueDepth. */
        PlanQueueDepth: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: plan_delayed_tasks_cap — per-app delayed-task count at MaxDelayedTasksPerApp. */
        PlanDelayedTasksCap: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: source_too_large — payload exceeds the plan's MaxSourceBytesPerInvocation. */
        PlanSourceBytes: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: long_poll_timeout — server-side long-poll budget elapsed without a terminal row. */
        LongPollTimeout: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: invalid_scheduled_at — delayed-task scheduled_at is in the past. */
        InvalidScheduledAt: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /**
         * @description 429. Two response shapes:
         *     - `application/problem+json` for code-driven 429s (`plan_limit_concurrency`, `quota_exhausted`).
         *     - `text/plain` for the authlimiter middleware (`pkg/middleware/authlimit.go`).
         */
        TooManyRequests: {
            headers: {
                /** @description Seconds until the client may retry. Emitted by the authlimiter (plain-text path). */
                "Retry-After"?: number;
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
                "text/plain": components["schemas"]["RateLimitPlain"];
            };
        };
        /** @description code: app_webhook_invalid — malformed webhook body (missing target_url, invalid retry_policy, out-of-vocabulary event, oversize secret, etc.) or invalid state transition (e.g. retry on a non-dead row). */
        AppWebhookInvalid: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: plan_webhooks_not_allowed — the plan does not include outbound webhooks (Free today). */
        PlanWebhooksNotAllowed: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
        /** @description code: plan_webhook_quota — per-app or per-account webhook limit reached. */
        PlanWebhookQuota: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/problem+json": components["schemas"]["Problem"];
            };
        };
    };
    parameters: {
        /**
         * @description Dashboard session cookie. Sealed; opaque to the client
         *     (`HttpOnly; Secure; SameSite=Lax`). 7-day fixed lifetime.
         *     The browser sets it automatically on `/login` / `/signup`;
         *     the SDK uses the device-code flow instead and never sets
         *     this cookie.
         */
        CookieSession: string;
        /**
         * @description Idempotency key for the POST. Stored for 24h. On replay the server
         *     returns the original response with `Idempotent-Replayed: true`.
         */
        IdempotencyKey: string;
        /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
        Slug: string;
        /** @description 32-hex-char opaque ID (NOT canonical UUID). */
        Id32: string;
        /** @description Secret key. Must start with a letter; A-Z, 0-9, underscore. */
        SecretKey: string;
        /**
         * @description Env-var scope (ADR-090). A domain-valid slug (3..40 chars,
         *     lowercase alnum + dash, no leading/trailing dash) — e.g.
         *     `default`, `staging`, `prod-eu`. Or the reserved sentinel
         *     `__all__` on GET only, which returns the nested
         *     `env_by_scope` response shape (every scope on the app).
         *     Omitted = `scope=default` (pre-PR-B behavior).
         */
        EnvScope: string;
        /**
         * @description Org slug. Lowercase letters, digits, hyphens; must start
         *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
         *     in `pkg/api/errors.go` exactly so the spec drift gate
         *     (`make spec-check`) stays green.
         */
        OrgSlug: string;
        /**
         * @description Account UUID (the membership row's account_id). The
         *     path segment is named `user_id` for backwards
         *     compatibility with the dashboard's existing
         *     /members/:user_id routes.
         */
        OrgAccountID: string;
        /** @description Maximum number of rows to return; capped at 200. */
        QueueCursorLimit: number;
        /** @description Cursor — the last id from the previous page (omit for the first page). */
        QueueCursorBefore: string;
    };
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    listOperatorRuntimeConfig: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Runtime configuration catalog */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        items: components["schemas"]["OperatorRuntimeConfig"][];
                        /** Format: date-time */
                        generated_at: string;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
        };
    };
    updateOperatorRuntimeConfig: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Catalog key to update. */
                key: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    value: unknown;
                    reason: string;
                    /** Format: int64 */
                    expected_version?: number;
                };
            };
        };
        responses: {
            /** @description Applied configuration entry */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OperatorRuntimeConfig"];
                };
            };
            /** @description Graceful apply operation queued */
            202: {
                headers: {
                    /** @description Polling URL for the durable apply operation. */
                    Location?: string;
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OperatorRuntimeConfigOperation"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            409: components["responses"]["Conflict"];
        };
    };
    getOperatorRuntimeConfigOperation: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Durable configuration operation id. */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Runtime configuration apply operation */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OperatorRuntimeConfigOperation"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    rollbackOperatorRuntimeConfig: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Catalog key to roll back. */
                key: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RollbackOperatorRuntimeConfigRequest"];
            };
        };
        responses: {
            /** @description Rolled-back and applied configuration entry */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OperatorRuntimeConfig"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["Conflict"];
        };
    };
    listOperatorRuntimeConfigRevisions: {
        parameters: {
            query?: {
                /** @description Maximum number of revisions to return. */
                limit?: number;
            };
            header?: never;
            path: {
                /** @description Catalog key whose history should be returned. */
                key: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Configuration revision history */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        items: components["schemas"]["OperatorRuntimeConfigRevision"][];
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
        };
    };
    getAccount: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The calling account: id, plan, limits snapshot, current-month usage, and total app count. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AccountResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PaymentRequired"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    deleteAccount: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Staged. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AccountDeletionResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            409: components["responses"]["ConflictDelete"];
        };
    };
    changePlan: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** @description Plan change payload. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["ChangePlanRequest"];
            };
        };
        responses: {
            /** @description The updated account profile after the plan change. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AccountResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PaymentRequired"];
            409: components["responses"]["Conflict"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getAccountSLO: {
        parameters: {
            query?: {
                /** @description Window for the account-wide SLO rollup. Default `24h`. */
                window?: "1h" | "24h" | "7d";
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The account-wide SLO rollup. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "window": "24h",
                     *       "source": "prometheus",
                     *       "as_of": "2026-08-07T15:30:00Z",
                     *       "request_duration": {
                     *         "p50_ms": 22.1,
                     *         "p95_ms": 91,
                     *         "p99_ms": 410
                     *       },
                     *       "error_rate_pct": 0.55,
                     *       "cold_boot_rate_pct": 4.2,
                     *       "instance_hours": 12,
                     *       "gb_hours": 3,
                     *       "wake_queue_p95_ms": 14,
                     *       "requests_total": 12000,
                     *       "throttled_total": 23
                     *     }
                     */
                    "application/json": components["schemas"]["AccountSLOResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    exportAccount: {
        parameters: {
            query?: {
                /** @description If true (default), include the sealed-secret envelopes. */
                include_secrets?: boolean;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A bundled JSON document: the account itself plus every owned app, deployment, build, instance, usage record, domain, cron, API key, sealed-secret envelope, and the audit trail. */
            200: {
                headers: {
                    /** @description attachment; filename="faas-account-<id>-<yyyymmdd>.json" */
                    "Content-Disposition"?: string;
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AccountExportResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
        };
    };
    restoreAccount: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Restored. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AccountResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            409: components["responses"]["ConflictDelete"];
        };
    };
    mfaEnroll: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Enrollment ready. Render the QR + recovery codes to the customer. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MFAEnrollResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description Already enrolled. Call /v1/account/mfa/disable first. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description MFA unavailable — host age key not loaded. */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    mfaConfirm: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MFAConfirmRequest"];
            };
        };
        responses: {
            /** @description Enrolled. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MFAConfirmResponse"];
                };
            };
            /** @description Invalid TOTP code. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description No in-flight enrollment. Call /enroll first. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    mfaVerify: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MFAVerifyRequest"];
            };
        };
        responses: {
            /** @description Stepped up. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MFAVerifyResponse"];
                };
            };
            /** @description Invalid code. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    mfaRecover: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MFARecoverRequest"];
            };
        };
        responses: {
            /** @description Recovered. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MFARecoverResponse"];
                };
            };
            /** @description No matching recovery code. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    mfaDisable: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MFADisableRequest"];
            };
        };
        responses: {
            /** @description Disabled. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MFADisableResponse"];
                };
            };
            /** @description Both or neither of password/recovery_code set. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Invalid credentials or recovery code. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    passwordLogin: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PasswordLoginRequest"];
                "application/x-www-form-urlencoded": components["schemas"]["PasswordLoginRequest"];
            };
        };
        responses: {
            /**
             * @description Signed in. The `Set-Cookie: faas_sid=…` header carries
             *     the session cookie.
             */
            200: {
                headers: {
                    /**
                     * @description `faas_sid=<sealed-session>; HttpOnly; Secure;
                     *     SameSite=Lax`. 7-day fixed lifetime.
                     */
                    "Set-Cookie"?: string;
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PasswordLoginResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["InvalidCredentials"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    passwordSignup: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PasswordSignupRequest"];
                "application/x-www-form-urlencoded": components["schemas"]["PasswordSignupRequest"];
            };
        };
        responses: {
            /** @description Account created (or reused) and signed in. */
            200: {
                headers: {
                    /** @description `faas_sid=<sealed-session>; HttpOnly; SameSite=Lax`. */
                    "Set-Cookie"?: string;
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["PasswordLoginResponse"];
                };
            };
            /** @description Password too weak (≥12 chars required). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["InvalidCredentials"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    passwordForgot: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["PasswordResetRequest"];
                "application/x-www-form-urlencoded": components["schemas"]["PasswordResetRequest"];
            };
        };
        responses: {
            /** @description Request accepted. The reset email is sent if the address is registered. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @enum {string} */
                        status: "ok";
                    };
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    programmaticSignup: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PasswordSignupRequest"];
            };
        };
        responses: {
            /**
             * @description Account created (or reused) + freshly minted API key.
             *     The plaintext is returned ONCE; the caller persists it.
             */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProgrammaticAuthResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["InvalidCredentials"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    programmaticLogin: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PasswordLoginRequest"];
            };
        };
        responses: {
            /** @description Authenticated + freshly minted API key. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProgrammaticAuthResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["InvalidCredentials"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    programmaticSignupMagicLink: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["MagicLinkSignupRequest"];
            };
        };
        responses: {
            /**
             * @description Request accepted. The signup link is mailed if the address
             *     is recognised (or could be registered).
             */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": {
                        /** @enum {string} */
                        status: "ok";
                    };
                };
            };
            400: components["responses"]["ValidationFailed"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    passwordResetForm: {
        parameters: {
            query: {
                /** @description Base64url-encoded 32-byte token from the reset email. */
                token: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Token is valid; renders the form. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "text/html": string;
                };
            };
            410: components["responses"]["ResetTokenInvalid"];
        };
    };
    passwordResetConfirm: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/x-www-form-urlencoded": components["schemas"]["PasswordResetConfirm"];
            };
        };
        responses: {
            /**
             * @description Password set. Redirects to `/dashboard/`. `Set-Cookie:
             *     faas_sid=…` is set.
             */
            302: {
                headers: {
                    Location?: string;
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description New password is too weak (≥12 chars required). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            410: components["responses"]["ResetTokenInvalid"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    googleAuthRedirect: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Redirect to Google consent. */
            302: {
                headers: {
                    Location?: string;
                    [name: string]: unknown;
                };
                content?: never;
            };
            /**
             * @description Google OAuth misconfigured at runtime. Defensive
             *     catch — boot validation (ADR-046) should make this
             *     unreachable in production.
             */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description Google sign-in not configured on this host. Operator
             *     must set both `GOOGLE_CLIENT_ID` and
             *     `GOOGLE_CLIENT_SECRET` and restart apid (ADR-046).
             *     Surfaces as `oauth_provider_unavailable`.
             */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    googleAuthCallback: {
        parameters: {
            query: {
                /**
                 * @description Authorization code returned by Google on the consent
                 *     redirect. Single-use; exchanged at
                 *     `oauth2.googleapis.com/token`.
                 */
                code: string;
                /**
                 * @description CSRF state token. Must match the `faas_google_state`
                 *     cookie set on the consent redirect. Mismatch returns
                 *     401 `csrf_mismatch`.
                 */
                state: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /**
             * @description Redirect to `WEBSITE_URL` or `/`. `Set-Cookie:
             *     faas_sid=…` is set.
             */
            302: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Google email not verified at the upstream. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
            /**
             * @description OAuth provider not configured on this host (stale
             *     cookie or direct callback hit before operator wired
             *     `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`).
             *     Surfaces as `oauth_provider_unavailable`.
             */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    githubAuthRedirect: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Redirect to GitHub consent. */
            302: {
                headers: {
                    Location?: string;
                    [name: string]: unknown;
                };
                content?: never;
            };
            /**
             * @description GitHub OAuth misconfigured at runtime. Defensive
             *     catch — boot validation (ADR-046) should make this
             *     unreachable in production.
             */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description GitHub sign-in not configured on this host. Operator
             *     must set both `GITHUB_CLIENT_ID` and
             *     `GITHUB_CLIENT_SECRET` and restart apid (ADR-046).
             *     Surfaces as `oauth_provider_unavailable`.
             */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    githubAuthCallback: {
        parameters: {
            query: {
                /**
                 * @description Authorization code returned by GitHub on the consent
                 *     redirect. Single-use; exchanged at
                 *     `github.com/login/oauth/access_token`.
                 */
                code: string;
                /**
                 * @description CSRF state token. Must match the `faas_github_state`
                 *     cookie set on the consent redirect. Mismatch returns
                 *     400 `csrf_mismatch`.
                 */
                state: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /**
             * @description Redirect to `WEBSITE_URL` or `/` after a successful
             *     GitHub OAuth handshake. `Set-Cookie: faas_sid=…` is set.
             */
            302: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /**
             * @description Malformed callback. The `code` field maps to
             *     `invalid_state` (no CSRF cookie), `csrf_mismatch`
             *     (query `state` does not match the cookie), or
             *     `missing_code` (no `code` query param). The
             *     `oauth_exchange_failed` code signals the upstream
             *     GitHub token exchange returned a non-JSON body
             *     (rarely seen post-2024; the handler sends
             *     `Accept: application/json`).
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description GitHub account has no primary && verified email. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
            /**
             * @description OAuth misconfigured server-side (`GITHUB_CLIENT_ID`
             *     or `GITHUB_CLIENT_SECRET` unset). Surfaces as
             *     `github_oauth_misconfigured`.
             */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description `github.com/login/oauth/access_token`,
             *     `api.github.com/user`, or `api.github.com/user/emails`
             *     unreachable. Surfaces as `github_unreachable`.
             */
            502: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description OAuth provider not configured on this host (stale
             *     cookie or direct callback hit before operator wired
             *     `GITHUB_CLIENT_ID`/`GITHUB_CLIENT_SECRET`).
             *     Surfaces as `oauth_provider_unavailable`.
             */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    getAuthCapabilities: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Per-provider enabled signal. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AuthCapabilities"];
                };
            };
            /** @description No valid session cookie; redirected to /login. */
            302: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
        };
    };
    postAccountLogout: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: {
                /**
                 * @description Dashboard session cookie. Sealed; opaque to the client
                 *     (`HttpOnly; Secure; SameSite=Lax`). 7-day fixed lifetime.
                 *     The browser sets it automatically on `/login` / `/signup`;
                 *     the SDK uses the device-code flow instead and never sets
                 *     this cookie.
                 */
                faas_sid?: components["parameters"]["CookieSession"];
            };
        };
        requestBody?: never;
        responses: {
            /**
             * @description Logout successful. `Set-Cookie: faas_sid=…` clears
             *     the session cookie.
             */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description CSRF check failed. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description Cookie missing, expired, tampered, or its `sid` is
             *     not backed by an active sessions row. The cookie
             *     is cleared on the wire. Surfaces as
             *     `session_expired` (pre-IAM-3 cookie / missing /
             *     revoked) or `session_invalid` (account-mismatch
             *     defensive path; AEAD-bound envelopes should not
             *     produce this).
             */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    issueBrowserCSRFToken: {
        parameters: {
            query: {
                /** @description Exact mutation action the token will authorize. */
                action: "auth.logout" | "auth.session.revoke" | "auth.sessions.revoke_all" | "mfa_confirm" | "mfa_recover" | "mfa_disable";
            };
            header?: never;
            path?: never;
            cookie?: {
                /**
                 * @description Dashboard session cookie. Sealed; opaque to the client
                 *     (`HttpOnly; Secure; SameSite=Lax`). 7-day fixed lifetime.
                 *     The browser sets it automatically on `/login` / `/signup`;
                 *     the SDK uses the device-code flow instead and never sets
                 *     this cookie.
                 */
                faas_sid?: components["parameters"]["CookieSession"];
            };
        };
        requestBody?: never;
        responses: {
            /** @description Action-bound CSRF token. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CSRFTokenResponse"];
                };
            };
            /** @description Unknown or missing action. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getAccountSessions: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: {
                /**
                 * @description Dashboard session cookie. Sealed; opaque to the client
                 *     (`HttpOnly; Secure; SameSite=Lax`). 7-day fixed lifetime.
                 *     The browser sets it automatically on `/login` / `/signup`;
                 *     the SDK uses the device-code flow instead and never sets
                 *     this cookie.
                 */
                faas_sid?: components["parameters"]["CookieSession"];
            };
        };
        requestBody?: never;
        responses: {
            /** @description Active sessions for the calling account. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionListResponse"];
                };
            };
            /** @description See `/v1/auth/logout` — same 401 surface. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    deleteAccountSession: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Session id (UUID v4) from `SessionInfo.id`. */
                id: string;
            };
            cookie?: {
                /**
                 * @description Dashboard session cookie. Sealed; opaque to the client
                 *     (`HttpOnly; Secure; SameSite=Lax`). 7-day fixed lifetime.
                 *     The browser sets it automatically on `/login` / `/signup`;
                 *     the SDK uses the device-code flow instead and never sets
                 *     this cookie.
                 */
                faas_sid?: components["parameters"]["CookieSession"];
            };
        };
        requestBody: {
            content: {
                "application/json": {
                    csrf_token: string;
                };
            };
        };
        responses: {
            /** @description Session revoked. `Set-Cookie: faas_sid=…` clears the cookie if the path id matched the current session. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /**
             * @description CSRF check failed (`csrf_mismatch`) or the path id
             *     is not a valid UUID (`validation_failed`).
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description See `/v1/auth/logout`. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description No active session matches the id on this account
             *     (does not exist, already revoked, or belongs to
             *     another account). The 404 is the same shape
             *     regardless — we never leak existence across
             *     accounts.
             */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    postAccountSessionsRevokeAll: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: {
                /**
                 * @description Dashboard session cookie. Sealed; opaque to the client
                 *     (`HttpOnly; Secure; SameSite=Lax`). 7-day fixed lifetime.
                 *     The browser sets it automatically on `/login` / `/signup`;
                 *     the SDK uses the device-code flow instead and never sets
                 *     this cookie.
                 */
                faas_sid?: components["parameters"]["CookieSession"];
            };
        };
        requestBody: {
            content: {
                "application/json": {
                    csrf_token: string;
                };
            };
        };
        responses: {
            /** @description Bulk revocation succeeded. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SessionsRevokeAllResponse"];
                };
            };
            /** @description CSRF check failed on the bulk-revoke request. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Cookie missing, expired, tampered, or its `sid` is not backed by an active sessions row. Same 401 surface as `/v1/auth/logout`. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    setPassword: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: {
                /**
                 * @description Dashboard session cookie. Sealed; opaque to the client
                 *     (`HttpOnly; Secure; SameSite=Lax`). 7-day fixed lifetime.
                 *     The browser sets it automatically on `/login` / `/signup`;
                 *     the SDK uses the device-code flow instead and never sets
                 *     this cookie.
                 */
                faas_sid?: components["parameters"]["CookieSession"];
            };
        };
        requestBody: {
            content: {
                "application/x-www-form-urlencoded": components["schemas"]["SetPasswordRequest"];
            };
        };
        responses: {
            /** @description Password set. Redirects to `/dashboard/account/`. */
            302: {
                headers: {
                    Location?: string;
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Chosen password is too weak (≥12 chars required). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
        };
    };
    listApps: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Apps on the account. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example [
                     *       {
                     *         "id": "0123456789abcdef0123456789abcdef",
                     *         "slug": "hello-world",
                     *         "type": "app",
                     *         "ram_mb": 256,
                     *         "max_concurrency": 2,
                     *         "concurrency_per_vm": 5,
                     *         "min_instances": 0,
                     *         "status": "active",
                     *         "url": "https://hello-world.example.com",
                     *         "manifest": {
                     *           "entrypoint": [
                     *             "node",
                     *             "server.js"
                     *           ]
                     *         },
                     *         "created_at": "2026-07-01T08:00:00Z",
                     *         "updated_at": "2026-07-23T11:25:00Z",
                     *         "require_authn": false
                     *       }
                     *     ]
                     */
                    "application/json": components["schemas"]["AppResponse"][];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    createApp: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        /** @description App creation payload (slug, type, runtime, RAM, …). See CreateAppRequest schema. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateAppRequest"];
            };
        };
        responses: {
            /** @description The new app. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["PlanLimit"];
            409: components["responses"]["Conflict"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getApp: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The app. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    deleteApp: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No body. The resource was removed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    updateApp: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /** @description Patch payload — every field is optional; omitted fields are unchanged. See UpdateAppRequest. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateAppRequest"];
            };
        };
        responses: {
            /** @description The updated app. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["PlanLimit"];
            404: components["responses"]["NotFound"];
            422: components["responses"]["InvalidMinInstances"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getAppMetrics: {
        parameters: {
            query?: {
                /** @description Time window. Default `5m`. */
                range?: "5m" | "15m" | "1h" | "6h" | "24h" | "7d" | "15d";
            };
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The metrics snapshot. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "app_id": "0123456789abcdef0123456789abcdef",
                     *       "range": "5m",
                     *       "source": "prometheus",
                     *       "as_of": "2026-07-28T13:00:00Z",
                     *       "request_count": 142,
                     *       "latency_p50_ms": 12.4,
                     *       "latency_p95_ms": 87.3,
                     *       "latency_p99_ms": 412.5,
                     *       "error_rate_pct": 0.7,
                     *       "cold_start_pct": 8.5,
                     *       "wake_p95_ms": 380.2
                     *     }
                     */
                    "application/json": components["schemas"]["AppMetricsResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getAppRoutes: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The per-route rows for the app. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "slug": "my-api",
                     *       "app_id": "11111111-1111-1111-1111-111111111111",
                     *       "routes": [
                     *         "GET /users",
                     *         "POST /orders",
                     *         "__route_other__"
                     *       ],
                     *       "source": "live",
                     *       "cap_hit": true
                     *     }
                     */
                    "application/json": components["schemas"]["AppRoutesResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getAppStreamingCap: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The streaming classification for the app. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "app_id": "11111111-1111-1111-1111-111111111111",
                     *       "status": "streaming",
                     *       "effective_cap_bytes": 104857600,
                     *       "plan_cap_bytes": 104857600,
                     *       "flag_enabled": true,
                     *       "plan_allowed": true,
                     *       "cap_kind": "plan"
                     *     }
                     */
                    "application/json": components["schemas"]["AppStreamingStatus"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getAppSLO: {
        parameters: {
            query?: {
                /** @description Window for the per-app SLO panel. Default `24h`. */
                window?: "1h" | "24h" | "7d";
            };
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The SLO panel. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "app_id": "0123456789abcdef0123456789abcdef",
                     *       "app_slug": "my-app",
                     *       "window": "24h",
                     *       "source": "prometheus",
                     *       "as_of": "2026-08-07T15:30:00Z",
                     *       "request_duration": {
                     *         "p50_ms": 14.2,
                     *         "p95_ms": 87,
                     *         "p99_ms": 312.5
                     *       },
                     *       "error_rate_pct": 0.41,
                     *       "cold_boot_rate_pct": 3.1,
                     *       "instance_hours": 0,
                     *       "gb_hours": 0,
                     *       "wake_queue_p95_ms": 12,
                     *       "requests_total": 4321,
                     *       "throttled_total": 0
                     *     }
                     */
                    "application/json": components["schemas"]["AppSLOResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getAppErrorsSummary: {
        parameters: {
            query?: {
                /** @description RFC3339Nano UTC window start. Defaults to `until - 24h`. */
                since?: string | null;
                /** @description RFC3339Nano UTC window end. Defaults to `now()`. */
                until?: string | null;
                /** @description Opaque pagination cursor from the previous response's `next_cursor`. Empty for the first page. */
                cursor?: string | null;
                /** @description Page size. Default `AppErrorsSummaryDefaultLimit=20`, capped at `AppErrorsSummaryMaxLimit=100`. */
                limit?: number;
            };
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The grouped error summary. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppErrorsSummaryResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listAppErrorRequests: {
        parameters: {
            query?: {
                /** @description Opaque pagination cursor (received_at, request_id compound). Empty for the first page. */
                cursor?: string | null;
                /** @description Page size. Default `AppErrorsSummaryDefaultLimit=20`. */
                limit?: number;
            };
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 64-hex-char SHA-256 fingerprint of the error group; sha256(route_template || 0x1f || status || 0x1f || error_class). */
                fingerprint: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The drill-down rows (newest-first). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppErrorRequestsResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getAppErrorSample: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 64-hex-char SHA-256 fingerprint to inspect; the oldest request row for this group is returned with its redacted headers. */
                fingerprint: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The sample row. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppErrorSampleResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    createDeployment: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /** @description Either a prebuilt OCI image reference (`application/json`) or a source tarball upload (`multipart/form-data`). See the operation description for plan size caps. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateDeploymentRequest"];
                "multipart/form-data": {
                    /**
                     * Format: binary
                     * @description Multipart file field — the source tarball or Dockerfile context.
                     */
                    source?: string;
                    dockerfile?: boolean;
                    /** @enum {string} */
                    kind?: "app" | "function";
                    /** @enum {string} */
                    runtime?: "node22" | "python312" | "go124" | "go124-alpine" | "node24" | "python313";
                };
            };
        };
        responses: {
            /** @description The deployment whose build has been accepted and queued. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeploymentResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["SourceEgressDenied"];
            413: components["responses"]["SourceTooLarge"];
            422: components["responses"]["DeployFailed"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    createDeploymentFromSourceRef: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                /**
                 * @example {
                 *       "repo": "onebox-faas/hello",
                 *       "ref": "main",
                 *       "format": "tarball"
                 *     }
                 */
                "application/json": components["schemas"]["SourceRefDeployRequest"];
            };
        };
        responses: {
            /** @description The source-ref deployment whose build has been accepted and queued. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeploymentResponse"];
                };
            };
            /**
             * @description code: invalid_ref | validation_failed. ref must be a 40-char
             *     SHA, branch, or tag.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /**
             * @description No durable GitHub install bound to the caller's account
             *     (code: github_install_not_found).
             */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            413: components["responses"]["SourceTooLarge"];
            429: components["responses"]["TooManyRequests"];
            /**
             * @description githubd unreachable or source-ref tarball fetch failed
             *     (code: source_ref_unavailable). Retry in ~30s.
             */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    diffApp: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                /**
                 * @example {
                 *       "image": "ghcr.io/me/api@sha256:abc",
                 *       "app_config": {
                 *         "ram_mb": 512,
                 *         "max_concurrency": 30
                 *       }
                 *     }
                 */
                "application/json": components["schemas"]["DiffRequest"];
            };
        };
        responses: {
            /**
             * @description DiffResponse with `blocking: true` if any break has
             *     `severity: "error"`. CI gate input — `jq '.blocking'`
             *     collapses to a single bool.
             */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "diff": {
                     *         "slug": "api",
                     *         "plan": "hobby",
                     *         "changes": [
                     *           {
                     *             "field": "memory",
                     *             "kind": "modify",
                     *             "before": 256,
                     *             "after": 512
                     *           }
                     *         ],
                     *         "breaks": [
                     *           {
                     *             "code": "plan_limit_concurrency",
                     *             "severity": "error",
                     *             "reason": "max_concurrency exceeds plan cap",
                     *             "field": "concurrency",
                     *             "observed": 30,
                     *             "limit": 20
                     *           }
                     *         ]
                     *       },
                     *       "blocking": true,
                     *       "slug": "api",
                     *       "plan": "hobby"
                     *     }
                     */
                    "application/json": components["schemas"]["DiffResponse"];
                };
            };
            /**
             * @description code: validation_failed. Body is malformed JSON, fails
             *     strict-decode, or has a plan lookup error.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /**
             * @description code: forbidden. Caller's bearer key lacks both
             *     `apps:read` and admin scope.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description code: capacity. pkg/state read failed for the baseline
             *     builder (deployment / env / crons / edge rules).
             */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    rollbackApp: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The deployment that was created by rolling back to the previous version. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeploymentResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            409: components["responses"]["NoRollbackTarget"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    parkApp: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Parked. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    wakeApp: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Wake requested. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["AdmissionRefused"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["ConcurrencyLimit"];
            503: components["responses"]["CapacityUnavailable"];
        };
    };
    raiseOverageCap: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                /**
                 * @example {
                 *       "overage_cap_cents": 10000
                 *     }
                 */
                "application/json": components["schemas"]["RaiseOverageCapRequest"];
            };
        };
        responses: {
            /** @description The updated account. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "id": "0123456789abcdef0123456789abcdef",
                     *       "email": "alice@example.com",
                     *       "plan": "pro",
                     *       "status": "active"
                     *     }
                     */
                    "application/json": components["schemas"]["AccountResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
        };
    };
    renameApp: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /** @description Rename payload — the new slug. See RenameAppRequest. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["RenameAppRequest"];
            };
        };
        responses: {
            /** @description The renamed app. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            409: components["responses"]["ConflictRename"];
        };
    };
    listInstallableRepos: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["InstallBindRequest"];
            };
        };
        responses: {
            /** @description Repos the install can see. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RepoResponse"][];
                };
            };
            /** @description code: invalid_request — missing/malformed body or installation_id. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description `forged` if the install's account.login differs from
             *     the session's github_login; `github_login_required` if
             *     the user hasn't completed /v1/auth/github. Returned for
             *     POST /v1/install/repos/list — the bind picker refuses
             *     to list repos of an install the user can't prove they own.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description code: github_unreachable — githubd could not reach api.github.com; retry in a minute. */
            502: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description code: githubd_not_ready — githubd is not wired on this host (M7.5 slices 7-8). */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    bindAppInstall: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["InstallBindRequest"];
            };
        };
        responses: {
            /** @description Bind persisted. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InstallBindResponse"];
                };
            };
            /** @description code: invalid_request — missing/malformed body, installation_id, or repo_full_name. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description `forged` if the install's account.login differs from
             *     the session's github_login; `github_login_required` if
             *     the user hasn't completed /v1/auth/github. Returned for
             *     POST /v1/apps/{slug}/install/bind — the bind refuses to
             *     persist a row for an install the user can't prove they own.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            /** @description code: github_unreachable — githubd could not reach api.github.com when persisting the bind; retry in a minute. */
            502: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    listInstances: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Instances belonging to the app. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example [
                     *       {
                     *         "id": "0123456789abcdef0123456789abcdef",
                     *         "app_id": "0123456789abcdef0123456789abcdef",
                     *         "deployment_id": "0123456789abcdef0123456789abcdef",
                     *         "state": "running",
                     *         "ram_mb": 256,
                     *         "started_at": "2026-07-23T11:25:00Z"
                     *       }
                     *     ]
                     */
                    "application/json": components["schemas"]["InstanceResponse"][];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    streamAppLogs: {
        parameters: {
            query?: {
                /** @description If 1, hold the connection open and stream new entries. */
                follow?: 0 | 1;
                /** @description Substring filter applied to each log line. */
                grep?: string;
                /** @description RFC3339 lower-bound on the line timestamp. */
                since?: string;
                /** @description Exact match on the structured `level` field (info, warn, or error). Empty = no level filter. The CLI and the apid handler both validate against the same enum (api.IsValidLogLevel in pkg/api/logs.go); an unknown value short-circuits with an SSE error frame carrying code invalid_level. */
                level?: "info" | "warn" | "error";
                /** @description If 1, serve archived logs from S3 instead of the live ring buffer. Requires `instance=<id>` and `date=YYYY-MM-DD`. Gated by `Plan.LogArchiveEnabled()` — Free plans receive 402 + `plan_log_archive_not_allowed`. The per-plan retention cap (Hobby 7d / Pro 30d / Scale 90d) refuses `date=` values outside the window. */
                archive?: 0 | 1;
                /** @description Required when `archive=1`. The Firecracker instance id to read archived logs from (matches the `instance_id` field in the live SSE frames). */
                instance?: string;
                /** @description Required when `archive=1`. The day to read in YYYY-MM-DD UTC. Must be inside the per-plan retention cap (Hobby 7d / Pro 30d / Scale 90d) — outside values return 403 + `log_archive_retention_exceeded`. Future dates are refused with the same code. */
                date?: string;
            };
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A text/event-stream of structured log lines, terminated by an empty SSE frame when the connection closes. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "text/event-stream": unknown;
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description Plan does not include log archive read-back. Free plans receive this on `?archive=1`. */
            402: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "type": "about:blank",
                     *       "title": "Log archive unavailable on this plan",
                     *       "status": 402,
                     *       "code": "plan_log_archive_not_allowed",
                     *       "detail": "the free plan does not include log archive read-back; upgrade to Hobby or above to query historical logs from object storage."
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Log archive retention cap exceeded; `?date=` is outside the per-plan window. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /** @example {"type":"about:blank","title":"","status":403,"code":"log_archive_retention_exceeded","detail":"?date=2026-07-01 is outside the per-plan window"} */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listSecrets: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Sealed-secret envelopes on the app (plaintext never returned). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppSecretListResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    setSecret: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description Secret key. Must start with a letter; A-Z, 0-9, underscore. */
                key: components["parameters"]["SecretKey"];
            };
            cookie?: never;
        };
        /** @description Secret payload — key name + plaintext. Sealed at rest; plaintext never returned. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["PutAppSecretRequest"];
            };
        };
        responses: {
            /** @description The stored sealed-secret envelope. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppSecretResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["PlanLimitSecrets"];
            413: components["responses"]["SecretValueTooLarge"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    deleteSecret: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description Secret key. Must start with a letter; A-Z, 0-9, underscore. */
                key: components["parameters"]["SecretKey"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No body. The sealed-secret envelope was removed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["SecretNotFound"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    rotateAppSecret: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description Secret key. Must start with a letter; A-Z, 0-9, underscore. */
                key: components["parameters"]["SecretKey"];
            };
            cookie?: never;
        };
        /** @description New plaintext value. Sealed at rest server-side; plaintext never returned. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["RotateAppSecretRequest"];
            };
        };
        responses: {
            /** @description The rotated sealed-secret envelope. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RotateAppSecretResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            413: components["responses"]["SecretValueTooLarge"];
            429: components["responses"]["TooManyRequests"];
            503: components["responses"]["CapacityUnavailable"];
        };
    };
    listRegistryCredentials: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description List of credential envelopes on the app (plaintext never returned). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppRegistryCredentialListResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    setRegistryCredential: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /** @description Registry host + username + plaintext password. Sealed at rest. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["PutAppRegistryCredentialRequest"];
            };
        };
        responses: {
            /** @description The stored credential envelope. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppRegistryCredentialResponse"];
                };
            };
            400: components["responses"]["InvalidRegistryHost"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["PlanRegistryCredentialsNotAllowed"];
            404: components["responses"]["NotFound"];
            413: components["responses"]["PlanRegistryCredentialQuota"];
            429: components["responses"]["TooManyRequests"];
            503: components["responses"]["ServiceUnavailable"];
        };
    };
    deleteRegistryCredential: {
        parameters: {
            query: {
                /** @description Registry host to delete (URL-encoded; matches the host stored on the row). */
                registry: string;
            };
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No body. The credential row was removed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            400: components["responses"]["RegistryCredentialNotFound"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    patchAppSecurity: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AppSecurityRequest"];
            };
        };
        responses: {
            /** @description The updated flag state. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppSecurityResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["AppNotFound"];
            500: components["responses"]["ServerError"];
        };
    };
    listAppTrustedSigners: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The trusted-publisher list (may be empty). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppTrustedSignerListResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["AppNotFound"];
            500: components["responses"]["ServerError"];
        };
    };
    putAppTrustedSigner: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description Lower-case signer label. Matches the on-disk filename (without .pem) under /etc/faas/secrets/trusted-publishers/. */
                name: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AddTrustedSignerRequest"];
            };
        };
        responses: {
            /** @description The onboarded trusted signer. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TrustedSigner"];
                };
            };
            /** @description trusted_signer_invalid (bytes out of [64,1024] range, non-ECDSA, non-P-256, etc). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description Forbidden — admin scope required, OR plan_limit_trusted_signers when the per-app count would exceed the plan cap. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["AppNotFound"];
            500: components["responses"]["ServerError"];
        };
    };
    deleteAppTrustedSigner: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description Lower-case signer label. Matches the on-disk filename (without .pem) under /etc/faas/secrets/trusted-publishers/. */
                name: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Row removed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            /** @description trusted_signer_not_found — the (slug, name) row does not exist. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            500: components["responses"]["ServerError"];
        };
    };
    listEnv: {
        parameters: {
            query?: {
                /**
                 * @description Env-var scope (ADR-090). A domain-valid slug (3..40 chars,
                 *     lowercase alnum + dash, no leading/trailing dash) — e.g.
                 *     `default`, `staging`, `prod-eu`. Or the reserved sentinel
                 *     `__all__` on GET only, which returns the nested
                 *     `env_by_scope` response shape (every scope on the app).
                 *     Omitted = `scope=default` (pre-PR-B behavior).
                 */
                scope?: components["parameters"]["EnvScope"];
            };
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Env var envelopes on the app (plaintext never returned). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppEnvListResponse"];
                };
            };
            400: components["responses"]["EnvScopeInvalid"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    setEnv: {
        parameters: {
            query?: {
                /**
                 * @description Env-var scope (ADR-090). A domain-valid slug (3..40 chars,
                 *     lowercase alnum + dash, no leading/trailing dash) — e.g.
                 *     `default`, `staging`, `prod-eu`. Or the reserved sentinel
                 *     `__all__` on GET only, which returns the nested
                 *     `env_by_scope` response shape (every scope on the app).
                 *     Omitted = `scope=default` (pre-PR-B behavior).
                 */
                scope?: components["parameters"]["EnvScope"];
            };
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description Secret key. Must start with a letter; A-Z, 0-9, underscore. */
                key: components["parameters"]["SecretKey"];
            };
            cookie?: never;
        };
        /** @description Env var payload — key name + plaintext. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["PutAppEnvRequest"];
            };
        };
        responses: {
            /** @description The stored env var envelope. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppEnvResponse"];
                };
            };
            /** @description 400 on PUT /v1/apps/{slug}/env/{key}?scope=... — any of {env_var_invalid_key, env_scope_invalid, env_scope_reserved}. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["PlanLimitEnvVars"];
            413: components["responses"]["EnvValueTooLarge"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    deleteEnv: {
        parameters: {
            query?: {
                /**
                 * @description Env-var scope (ADR-090). A domain-valid slug (3..40 chars,
                 *     lowercase alnum + dash, no leading/trailing dash) — e.g.
                 *     `default`, `staging`, `prod-eu`. Or the reserved sentinel
                 *     `__all__` on GET only, which returns the nested
                 *     `env_by_scope` response shape (every scope on the app).
                 *     Omitted = `scope=default` (pre-PR-B behavior).
                 */
                scope?: components["parameters"]["EnvScope"];
            };
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description Secret key. Must start with a letter; A-Z, 0-9, underscore. */
                key: components["parameters"]["SecretKey"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No body. The env var was removed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description 400 on DELETE /v1/apps/{slug}/env/{key}?scope=... — any of {env_var_not_found, env_scope_invalid, env_scope_reserved}. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listAppDataUpstreams: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Captured upstreams on the app (plaintext never returned). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DataUpstreamListResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    createAppDataUpstream: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PutDataUpstreamRequest"];
            };
        };
        responses: {
            /** @description The stored upstream envelope. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DataUpstreamResponse"];
                };
            };
            /** @description 400 — any of {upstream_invalid_kind, upstream_invalid_host, upstream_invalid_port}. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description 402 — plan_data_upstreams_not_allowed (Free plan). */
            402: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description 403 — plan_limit_data_upstreams (per-app cap exceeded). */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getAppDataUpstream: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description Upstream id (UUID). */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The upstream envelope. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DataUpstreamResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
        };
    };
    deleteAppDataUpstream: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description Upstream id (UUID). */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No body. The upstream was removed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            /** @description 404 — upstream_not_found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    listDeployments: {
        parameters: {
            query?: {
                /** @description Page size (1–200, default 50). */
                limit?: number;
                /** @description RFC3339Nano cursor from a previous response's `next_before`. */
                before?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A paginated list of deployments. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeploymentListResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getDeployment: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The deployment. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeploymentResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    updateDeploymentMinInstances: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    /** @description Per-deployment cold-wake floor override. 0 = inherit from parent app. */
                    min_instances: number;
                };
            };
        };
        responses: {
            /** @description The updated deployment. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeploymentResponse"];
                };
            };
            /**
             * @description `400 Bad Request` — request body failed JSON decode or
             *     `min_instances` was outside the inclusive `[0, plan_cap]`
             *     range. Stable code `min_instances_invalid`.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            /**
             * @description `422 Unprocessable Entity` — request was syntactically valid
             *     but the parent app's plan refuses the override (e.g. a
             *     Free app PATCHing `min_instances=1`). Stable code
             *     `plan_min_instances_not_allowed`.
             */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    updateDeploymentTraffic: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateDeploymentTrafficRequest"];
            };
        };
        responses: {
            /** @description The updated deployment with the new traffic_percent. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeploymentResponse"];
                };
            };
            /** @description `400 Bad Request` — request body failed JSON decode. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /**
             * @description `403 Forbidden` — the account's plan refuses traffic
             *     splitting (Free / Hobby). Stable code
             *     `plan_traffic_split_not_allowed`.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            /**
             * @description `409 Conflict` — post-write Σ invariant check tripped.
             *     Structurally unreachable with the zero-siblings rebalance
             *     form, but pinned by the test suite as a defensive
             *     backstop against future refactors.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description `422 Unprocessable Entity` — `traffic_percent` was
             *     outside the inclusive `[0, 100]` range. Stable code
             *     `invalid_traffic_percent`.
             */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    streamDeploymentLogs: {
        parameters: {
            query?: {
                /** @description Stream cursor — return only log entries with seq strictly less than this value. */
                before_seq?: number;
                /** @description Maximum number of log entries to return in the initial burst before streaming. */
                limit?: number;
                /** @description If 1, hold the connection open and stream new build log entries as they arrive. */
                follow?: 0 | 1;
            };
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A text/event-stream of build log entries, terminated by an empty SSE frame when the build finishes. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "text/event-stream": unknown;
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getDeploymentScan: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The typed scan payload. The shape mirrors the `deployments.scan_result` jsonb column (with the Status re-pinned from the authoritative `scan_status` column). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ScanResult"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description Deployment row missing, cross-account probe, or scan has not run yet. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    getDeploymentSecretScan: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The typed secret-scan payload. Shape mirrors the `deployments.secret_findings` jsonb column. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SecretScanResult"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description Deployment row missing, cross-account probe, or secret scan has not been stamped for this deploy yet (pre-PR-A rows return 404 because the `secret_findings` jsonb has never been written). */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    getBuilds: {
        parameters: {
            query?: {
                /** @description Filter to a single app slug. Cross-account slug renders 404. */
                app?: string;
                /** @description Filter to a single status. Omit for any status. */
                status?: "queued" | "running" | "succeeded" | "failed";
                /** @description Opaque cursor token from a previous response's `next_before`. Format: `<rfc3339nano>|<id_hex>` (pipe-separated). Empty started_at segment encodes a queued-tail cursor (the `<id_hex>` part alone). Round-trip verbatim — do NOT re-parse or re-encode. */
                before?: string;
                /** @description Page size (default 50, capped at 200). */
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of builds (ordered started_at DESC, nulls last). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BuildListResponse"];
                };
            };
            /**
             * @description `400 Bad Request` — bad cursor (not RFC3339), bad status
             *     filter (not one of queued|running|succeeded|failed), or
             *     bad limit (non-numeric / out of range). Stable code
             *     `validation_failed`.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /**
             * @description `404 Not Found` — only raised when ?app=<slug> is set
             *     and the slug is unknown OR belongs to another account
             *     (uniform 404 so cross-account probes can't enumerate).
             *     Stable code `app_not_found`.
             */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    getBuild: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Build lifecycle row. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BuildResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /**
             * @description `404 Not Found` — the build id does not exist OR
             *     belongs to another account. Stable code
             *     `build_not_found`. The 404 surface is uniform so
             *     cross-account probes can't enumerate.
             */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    getBuildProvenance: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The build provenance row, with every field populated. Empty strings indicate a column the populator hasn't filled yet (the schema half of Phase 2 is in this PR; cosign / SBOM populate the rest in Phase 3). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BuildProvenanceResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description Either the build row is missing, OR the build exists but the populator INSERT failed (code=build_provenance_not_found). */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    getBuildSbom: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The CycloneDX SBOM JSON document, served verbatim from the storage backend. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/vnd.cyclonedx+json": {
                        [key: string]: unknown;
                    };
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description Either the build row is missing, OR the build exists but belongs to a different account (code=not_found on every negative path so account-existence isn't probeable). */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
            /** @description The SBOM populator has not produced an artefact for this build (code=build_sbom_unavailable), or the storage backend failed (code=capacity_unavailable). */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    listDomains: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Custom-domain bindings on the account. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example [
                     *       {
                     *         "id": "0123456789abcdef0123456789abcdef",
                     *         "domain": "app.example.com",
                     *         "app_id": "0123456789abcdef0123456789abcdef",
                     *         "verified": true,
                     *         "tls_state": "active",
                     *         "created_at": "2026-07-23T11:25:00Z"
                     *       }
                     *     ]
                     */
                    "application/json": components["schemas"]["CustomDomainResponse"][];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    createDomain: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        /** @description Domain-bind payload — domain string + target app slug. See CreateCustomDomainRequest. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateCustomDomainRequest"];
            };
        };
        responses: {
            /** @description The new custom-domain binding. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CustomDomainResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            409: components["responses"]["Conflict"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    deleteDomain: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description The custom domain string (e.g. `app.example.com`). */
                domain: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No body. The binding was removed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listCrons: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Cron triggers on the account. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example [
                     *       {
                     *         "id": "0123456789abcdef0123456789abcdef",
                     *         "app_id": "0123456789abcdef0123456789abcdef",
                     *         "schedule": "*\/5 * * * *",
                     *         "path": "/cron",
                     *         "enabled": true,
                     *         "created_at": "2026-07-23T11:25:00Z",
                     *         "last_fired_at": "2026-07-23T11:20:00Z"
                     *       }
                     *     ]
                     */
                    "application/json": components["schemas"]["CronResponse"][];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    createCron: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        /** @description Cron payload — schedule expression + target URL. See CreateCronRequest. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateCronRequest"];
            };
        };
        responses: {
            /** @description The new cron trigger. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CronResponse"];
                };
            };
            400: components["responses"]["CronInvalid"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["CronInvalid"];
            403: components["responses"]["PlanLimit"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getCron: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The cron trigger. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CronResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    deleteCron: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No body. The cron trigger was removed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    updateCron: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        /** @description Cron patch — every field is optional. See UpdateCronRequest. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateCronRequest"];
            };
        };
        responses: {
            /** @description The updated cron trigger. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CronResponse"];
                };
            };
            400: components["responses"]["CronInvalid"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listCronRuns: {
        parameters: {
            query?: {
                /** @description Cursor — return runs older than this run id. Omit for the most recent page. */
                before?: string;
                /** @description Page size; 1-100, default 10. */
                limit?: number;
            };
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of runs for this cron, newest first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ListCronRunsResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    fireCron: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Replay token. A duplicate (account, key) pair returns the
                 *     stored 202 with the original request id.
                 */
                "Idempotency-Key"?: string;
            };
            path: {
                /** @description The cron id (UUID hex, no dashes). */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Fire-now enqueued. The request_id is the durable handle. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["FireCronResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            /** @description Plan tier does not include cron support (e.g. Free plan). */
            402: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            /** @description The cron is disabled. */
            410: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    getFireCronRequest: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Fire-now request identifier returned by `POST /v1/crons/{id}/run`. */
                request_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Current state of the fire-now request. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["FireCronRequestResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    scanProject: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "multipart/form-data": components["schemas"]["ProjectScanRequest"];
            };
        };
        responses: {
            /** @description The deploy plan. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "project_slug": "my-app",
                     *       "scan_source": "compose",
                     *       "tier": "compose",
                     *       "workloads": [
                     *         {
                     *           "name": "api",
                     *           "root_dir": "",
                     *           "command": [],
                     *           "ports": [
                     *             8080
                     *           ]
                     *         },
                     *         {
                     *           "name": "worker",
                     *           "root_dir": "",
                     *           "command": [],
                     *           "ports": []
                     *         }
                     *       ],
                     *       "managed": [
                     *         {
                     *           "name": "db",
                     *           "kind": "postgres",
                     *           "env_hint": "DATABASE_URL"
                     *         }
                     *       ],
                     *       "crons": [
                     *         {
                     *           "workload_name": "nightly",
                     *           "schedule": "0 3 * * *",
                     *           "path": "/",
                     *           "enabled": true
                     *         }
                     *       ],
                     *       "observed_apps": 3,
                     *       "observed_crons": 1,
                     *       "limit_apps": 5,
                     *       "limit_crons": 5,
                     *       "can_apply": true,
                     *       "plan_token": "eyJoYXMiOiI..."
                     *     }
                     */
                    "application/json": components["schemas"]["PlanResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["CronInvalid"];
            403: components["responses"]["PlanLimit"];
            413: components["responses"]["SourceTooLarge"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    applyProject: {
        parameters: {
            query?: {
                /** @description Echo of the dry-run plan_token (base64-JSON). Omit to skip the cache. */
                plan_token?: string;
            };
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "multipart/form-data": components["schemas"]["ProjectApplyRequest"];
            };
        };
        responses: {
            /** @description The applied project + apps + crons. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "project_id": "0123456789abcdef0123456789abcdef",
                     *       "apps": [
                     *         {
                     *           "slug": "api",
                     *           "id": "0123456789abcdef0123456789abcdef"
                     *         },
                     *         {
                     *           "slug": "worker",
                     *           "id": "0123456789abcdef0123456789abcdef"
                     *         }
                     *       ],
                     *       "scan_source": "compose",
                     *       "can_apply": true
                     *     }
                     */
                    "application/json": components["schemas"]["ApplyResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["CronInvalid"];
            403: components["responses"]["PlanLimit"];
            409: components["responses"]["ValidationFailed"];
            413: components["responses"]["SourceTooLarge"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listAlertRules: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Alert rules on the account, filtered to those visible at this app. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example [
                     *       {
                     *         "id": "0123456789abcdef0123456789abcdef",
                     *         "app_id": "0123456789abcdef0123456789abcdef",
                     *         "name": "p99 > 500ms",
                     *         "enabled": true,
                     *         "metric": "latency_p99_ms",
                     *         "comparison": "gt",
                     *         "threshold": 500,
                     *         "window_spec": "5m",
                     *         "webhook_url": "https://example.com/hook",
                     *         "webhook_secret_sealed_masked": "***",
                     *         "cooldown_minutes": 15,
                     *         "state": "ok",
                     *         "created_at": "2026-07-28T10:00:00Z",
                     *         "updated_at": "2026-07-28T10:00:00Z"
                     *       },
                     *       {
                     *         "id": "fedcba9876543210fedcba9876543210",
                     *         "app_id": "",
                     *         "name": "error_rate > 1%",
                     *         "enabled": true,
                     *         "metric": "error_rate_pct",
                     *         "comparison": "gt",
                     *         "threshold": 1,
                     *         "window_spec": "15m",
                     *         "webhook_url": "https://example.com/acct",
                     *         "webhook_secret_sealed_masked": "***",
                     *         "cooldown_minutes": 30,
                     *         "state": "ok",
                     *         "created_at": "2026-07-28T09:00:00Z",
                     *         "updated_at": "2026-07-28T09:00:00Z"
                     *       }
                     *     ]
                     */
                    "application/json": components["schemas"]["AlertRuleResponse"][];
                };
            };
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["AlertRuleInvalid"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    createAlertRule: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        /** @description Alert rule payload. See CreateAlertRuleRequest. */
        requestBody: {
            content: {
                /**
                 * @example {
                 *       "name": "p99 > 500ms",
                 *       "metric": "latency_p99_ms",
                 *       "comparison": "gt",
                 *       "threshold": 500,
                 *       "window_spec": "5m",
                 *       "webhook_url": "https://example.com/hook",
                 *       "webhook_secret": "shh",
                 *       "cooldown_minutes": 15
                 *     }
                 */
                "application/json": components["schemas"]["CreateAlertRuleRequest"];
            };
        };
        responses: {
            /** @description The new alert rule (carries the masked secret). */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AlertRuleResponse"];
                };
            };
            400: components["responses"]["AlertRuleInvalid"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["AlertRuleInvalid"];
            403: components["responses"]["PlanLimit"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["AlertRuleInvalid"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getAlertRule: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The alert rule. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AlertRuleResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    deleteAlertRule: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No body. The rule was removed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["AlertRuleInvalid"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    updateAlertRule: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                /**
                 * @example {
                 *       "name": "p99 > 750ms",
                 *       "cooldown_minutes": 30
                 *     }
                 */
                "application/json": components["schemas"]["UpdateAlertRuleRequest"];
            };
        };
        responses: {
            /** @description The updated alert rule. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AlertRuleResponse"];
                };
            };
            400: components["responses"]["AlertRuleInvalid"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["AlertRuleInvalid"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    rotateAlertRuleSecret: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Rotation succeeded. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "rotated_at": "2026-07-28T12:00:00Z",
                     *       "webhook_secret_sealed_masked": "***"
                     *     }
                     */
                    "application/json": components["schemas"]["RotateAlertRuleSecretResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["AlertRuleInvalid"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listEdgeRules: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Every edge rule owned by the caller. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EdgeRuleResponse"][];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listEdgeRulesForApp: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Edge rules for this app, ordered by priority ASC. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EdgeRuleResponse"][];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    createEdgeRule: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                /**
                 * @example {
                 *       "kind": "rewrite",
                 *       "match_host": "api.example.com",
                 *       "match_path": "/api/*",
                 *       "priority": 100,
                 *       "enabled": true,
                 *       "action": {
                 *         "from": "/api",
                 *         "to": "/v1"
                 *       }
                 *     }
                 */
                "application/json": components["schemas"]["CreateEdgeRuleRequest"];
            };
        };
        responses: {
            /** @description Rule created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EdgeRuleResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            /** @description Plan-kind or per-app quota rejected the rule. */
            402: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            409: components["responses"]["EdgeRuleConflict"];
            422: components["responses"]["ValidationFailed"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getEdgeRule: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The edge rule. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EdgeRuleResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    deleteEdgeRule: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    updateEdgeRule: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpdateEdgeRuleRequest"];
            };
        };
        responses: {
            /** @description Updated rule. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["EdgeRuleResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            422: components["responses"]["ValidationFailed"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getAppThrottleSuggestions: {
        parameters: {
            query?: {
                /**
                 * @description Prometheus window. Closed vocabulary (see
                 *     `pkg/appmetrics.Ranges`). Defaults to 5m.
                 */
                range?: string;
            };
            header?: never;
            path: {
                /** @description App slug (lowercase, kebab-case; per-account unique). The recommender walks the per-route rate() for this app's gatewayd-internal reader. */
                slug: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /**
             * @description Suggestion payload. Source is `prometheus` on success
             *     or `degraded: <reason>` on Prometheus failure
             *     (response is still 200 with empty Suggestions — the
             *     dashboard's empty-state branch handles it).
             */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ThrottleSuggestionsResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listAppWebhooks: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The webhooks. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppWebhookResponse"][];
                };
            };
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PlanWebhooksNotAllowed"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    createAppWebhook: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                /**
                 * @example {
                 *       "target_url": "https://example.com/hook",
                 *       "webhook_secret": "shh",
                 *       "event_filter": [
                 *         "cron.fired",
                 *         "app.created"
                 *       ],
                 *       "retry_policy": "default",
                 *       "enabled": true
                 *     }
                 */
                "application/json": components["schemas"]["CreateAppWebhookRequest"];
            };
        };
        responses: {
            /** @description Webhook created. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppWebhookResponse"];
                };
            };
            400: components["responses"]["AppWebhookInvalid"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PlanWebhooksNotAllowed"];
            403: components["responses"]["PlanWebhookQuota"];
            404: components["responses"]["NotFound"];
            409: components["responses"]["AppWebhookInvalid"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getAppWebhook: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The webhook. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppWebhookResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PlanWebhooksNotAllowed"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    deleteAppWebhook: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No body. The webhook was removed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PlanWebhooksNotAllowed"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    updateAppWebhook: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                /**
                 * @example {
                 *       "target_url": "https://example.com/hook2",
                 *       "enabled": true
                 *     }
                 */
                "application/json": components["schemas"]["UpdateAppWebhookRequest"];
            };
        };
        responses: {
            /** @description The updated webhook. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppWebhookResponse"];
                };
            };
            400: components["responses"]["AppWebhookInvalid"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PlanWebhooksNotAllowed"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    rotateAppWebhookSecret: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Webhook secret rotation succeeded. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "rotated_at": "2026-08-06T12:00:00Z",
                     *       "webhook_secret_sealed_masked": "***"
                     *     }
                     */
                    "application/json": components["schemas"]["RotateAppWebhookSecretResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PlanWebhooksNotAllowed"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listAppWebhookDeliveries: {
        parameters: {
            query?: {
                /** @description Page size (1..100; default 50). */
                page_size?: number;
                /** @description Opaque cursor from the previous page's next_token. */
                page_token?: string;
            };
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The deliveries page. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppWebhookDeliveryListResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PlanWebhooksNotAllowed"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    retryAppWebhookDelivery: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
                /** @description Delivery id (32-hex-char UUID). */
                did: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The re-armed delivery. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AppWebhookRetryDeliveryResponse"];
                };
            };
            400: components["responses"]["AppWebhookInvalid"];
            401: components["responses"]["Unauthorized"];
            402: components["responses"]["PlanWebhooksNotAllowed"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    invokeApp: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["InvokeRequest"];
            };
        };
        responses: {
            /** @description The completed invocation. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InvokeResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["PlanFeatureGated"];
            413: components["responses"]["PlanSourceBytes"];
            429: components["responses"]["TooManyRequests"];
            504: components["responses"]["LongPollTimeout"];
        };
    };
    invokeAppAsync: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["InvokeRequest"];
            };
        };
        responses: {
            /** @description The enqueued invocation. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AsyncInvokeResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["PlanFeatureGated"];
            413: components["responses"]["PlanSourceBytes"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    queueSend: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["QueueSendRequest"];
            };
        };
        responses: {
            /** @description The enqueued row. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["QueueSendResponse"];
                };
            };
            403: components["responses"]["PlanQueueDepth"];
            413: components["responses"]["PlanSourceBytes"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    queueReceive: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A dispatched row. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["QueueReceiveResponse"];
                };
            };
            /** @description No row dispatched within the budget. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["PlanFeatureGated"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    queueAck: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No body. The ack (or a re-ack) was accepted. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    queueState: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Queue stats for the app. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["QueueStateResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    queuePeek: {
        parameters: {
            query?: {
                /** @description Maximum number of rows to return; capped at 200. */
                limit?: components["parameters"]["QueueCursorLimit"];
                /** @description Cursor — the last id from the previous page (omit for the first page). */
                before?: components["parameters"]["QueueCursorBefore"];
            };
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of pending rows. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["QueuePeekResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    queueDeadLetter: {
        parameters: {
            query?: {
                /** @description Maximum number of rows to return; capped at 200. */
                limit?: components["parameters"]["QueueCursorLimit"];
                /** @description Cursor — the last id from the previous page (omit for the first page). */
                before?: components["parameters"]["QueueCursorBefore"];
            };
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description A page of dead-letter rows. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["QueueDeadLetterResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    delayedTaskCreate: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DelayedTaskRequest"];
            };
        };
        responses: {
            /** @description The newly-scheduled task. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DelayedTaskResponse"];
                };
            };
            400: components["responses"]["InvalidScheduledAt"];
            403: components["responses"]["PlanDelayedTasksCap"];
            413: components["responses"]["PlanSourceBytes"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listInvocations: {
        parameters: {
            query?: {
                /** @description Cursor — return rows whose id is strictly less than this. Omit for the most recent page. */
                before?: string;
                /** @description Page size; 1-200, default 20. */
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The page. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ListInvocationsResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listInstancesForAccount: {
        parameters: {
            query?: {
                /** @description Cursor (instances.id UUIDv7). Omit for the most recent page. */
                before?: string;
                /** @description Page size; server clamps to 1..100, returns 400 with `limit=100` and the observed value on bad input. */
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description One page of instances, newest first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "instances": [
                     *         {
                     *           "id": "0123456789abcdef0123456789abcdef",
                     *           "app_id": "0123456789abcdef0123456789abcdef",
                     *           "deployment_id": "0123456789abcdef0123456789abcdef",
                     *           "state": "running",
                     *           "ram_mb": 256,
                     *           "started_at": "2026-07-23T11:25:00Z"
                     *         }
                     *       ],
                     *       "next_before": null
                     *     }
                     */
                    "application/json": components["schemas"]["ListInstancesResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listSecretsForAccount: {
        parameters: {
            query?: {
                /** @description Cursor in the form `<slug>|<key>`. Omit for the first page. */
                before?: string;
                /** @description Page size; server clamps to 1..100, returns 400 on bad input. */
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description One page of sealed-secret envelopes, ordered by (app_slug ASC, key ASC). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "secrets": [
                     *         {
                     *           "app_id": "0123456789abcdef0123456789abcdef",
                     *           "app_slug": "foo-app",
                     *           "key": "DATABASE_URL",
                     *           "ciphertext": "YWdlLWVuY3J5cHRpb24...",
                     *           "created_at": "2026-07-23T11:25:00Z",
                     *           "updated_at": "2026-07-23T11:25:00Z"
                     *         }
                     *       ],
                     *       "next_before": null
                     *     }
                     */
                    "application/json": components["schemas"]["ListSecretsForAccountResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getAppsMetrics: {
        parameters: {
            query?: {
                /** @description Time window applied to every per-app rollup row. Default `5m`. */
                range?: "5m" | "15m" | "1h" | "6h" | "24h" | "7d" | "15d";
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The rollup. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "range": "5m",
                     *       "source": "prometheus",
                     *       "as_of": "2026-07-28T13:00:00Z",
                     *       "apps": {
                     *         "foo-app": {
                     *           "app_id": "0123456789abcdef0123456789abcdef",
                     *           "range": "5m",
                     *           "source": "prometheus",
                     *           "as_of": "2026-07-28T13:00:00Z",
                     *           "request_count": 142,
                     *           "latency_p50_ms": 12.4,
                     *           "latency_p95_ms": 87.3,
                     *           "latency_p99_ms": 412.5,
                     *           "error_rate_pct": 0.7,
                     *           "cold_start_pct": 8.5,
                     *           "wake_p95_ms": 380.2
                     *         }
                     *       }
                     *     }
                     */
                    "application/json": components["schemas"]["AppsMetricsResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getInvocation: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The row. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Invocation"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    replayInvocation: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The new invocation row was enqueued. */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AsyncInvokeResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            /**
             * @description The invocation is not in a replayable state. Returns the
             *     `invocation_not_replayable` problem code with the current
             *     state in the detail field; only `failed` and `dead_letter`
             *     invocations can be replayed.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    delayedTaskGet: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The task. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DelayedTaskResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    delayedTaskCancel: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No body. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listKeys: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description API key metadata on the account. Plaintext is never returned. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example [
                     *       {
                     *         "id": "0123456789abcdef0123456789abcdef",
                     *         "org_id": "8b1f5e5d-273e-5a18-ae00-58fceba4fe6c",
                     *         "prefix": "fp_live_ab",
                     *         "label": "ci-deploy",
                     *         "scopes": [
                     *           "apps:read",
                     *           "deploy:write"
                     *         ],
                     *         "created_at": "2026-07-23T11:25:00Z",
                     *         "last_used_at": "2026-07-23T11:25:00Z"
                     *       }
                     *     ]
                     */
                    "application/json": components["schemas"]["APIKeyResponse"][];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    createKey: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** @description Create a new API key for the authenticated account. Plaintext is returned exactly once in the 201 response and cannot be recovered later — store it immediately. See IAM-1, ADR-034 rev2 for the scope vocabulary. */
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateKeyRequest"];
            };
        };
        responses: {
            /** @description The new API key. **The plaintext is returned exactly once** — every subsequent GET returns only the prefix. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["APIKeyResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    deleteKey: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No body. The key was revoked. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    rotateKey: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description New key minted, old key now in 'grace' (or 'revoked' if grace_window_days=0). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "key": {
                     *         "id": "0123456789abcdef0123456789abcdef",
                     *         "org_id": "8b1f5e5d-273e-5a18-ae00-58fceba4fe6c",
                     *         "prefix": "fp_live_ab",
                     *         "label": "ci-deploy",
                     *         "scopes": [
                     *           "apps:read",
                     *           "deploy:write"
                     *         ],
                     *         "created_at": "2026-08-02T11:25:00Z",
                     *         "status": "active",
                     *         "rotated_from_id": "fedcba9876543210fedcba9876543210"
                     *       },
                     *       "key_plaintext": "fp_live_NEW_PLAINTEXT_HERE",
                     *       "old_key_id": "fedcba9876543210fedcba9876543210",
                     *       "old_key_expires_at": "2026-08-09T11:25:00Z"
                     *     }
                     */
                    "application/json": components["schemas"]["RotateKeyResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description No such key, or the key is already revoked. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    listOrgAPIKeys: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Org-scoped API key list. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "keys": [
                     *         {
                     *           "id": "0123456789abcdef0123456789abcdef",
                     *           "org_id": "8b1f5e5d-273e-5a18-ae00-58fceba4fe6c",
                     *           "prefix": "fp_live_ab",
                     *           "label": "ci-deploy",
                     *           "scopes": [
                     *             "apps:read",
                     *             "deploy:write"
                     *           ],
                     *           "created_at": "2026-08-02T11:25:00Z",
                     *           "status": "active"
                     *         }
                     *       ]
                     *     }
                     */
                    "application/json": components["schemas"]["ListOrgAPIKeysResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            /** @description Org slug not found, or caller has no membership. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    createOrgAPIKey: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                /**
                 * @example {
                 *       "label": "ci-deploy",
                 *       "scopes": [
                 *         "apps:read",
                 *         "deploy:write"
                 *       ]
                 *     }
                 */
                "application/json": components["schemas"]["CreateOrgAPIKeyRequest"];
            };
        };
        responses: {
            /** @description New API key minted against the org. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "id": "0123456789abcdef0123456789abcdef",
                     *       "org_id": "8b1f5e5d-273e-5a18-ae00-58fceba4fe6c",
                     *       "prefix": "fp_live_ab",
                     *       "label": "ci-deploy",
                     *       "scopes": [
                     *         "apps:read",
                     *         "deploy:write"
                     *       ],
                     *       "created_at": "2026-08-02T11:25:00Z",
                     *       "status": "active",
                     *       "plaintext": "fp_live_NEW_PLAINTEXT_HERE"
                     *     }
                     */
                    "application/json": components["schemas"]["APIKeyResponse"];
                };
            };
            /** @description Invalid body (unknown scope, label too long). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            /** @description Active-org header missing or unknown; caller has no membership on the resolved slug. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Per-account key quota (`api.Plan.KeysMax`) reached. */
            429: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    getOrgAPIKey: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Single API key. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "id": "0123456789abcdef0123456789abcdef",
                     *       "org_id": "8b1f5e5d-273e-5a18-ae00-58fceba4fe6c",
                     *       "prefix": "fp_live_ab",
                     *       "label": "ci-deploy",
                     *       "scopes": [
                     *         "apps:read",
                     *         "deploy:write"
                     *       ],
                     *       "created_at": "2026-08-02T11:25:00Z",
                     *       "status": "active"
                     *     }
                     */
                    "application/json": components["schemas"]["APIKeyResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            /** @description Key id not in org, or org slug not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    revokeOrgAPIKey: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description No body. Status flipped to revoked; the row stays for audit lineage. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            /** @description Key id not in this org, or active-org slug not found. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    rotateOrgAPIKey: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description App slug. Lowercase letters, digits, hyphens; must start and end with alnum. */
                slug: components["parameters"]["Slug"];
                /** @description 32-hex-char opaque ID (NOT canonical UUID). */
                id: components["parameters"]["Id32"];
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                /**
                 * @example {
                 *       "label": "ci-deploy-rotated",
                 *       "grace_window_days": 7
                 *     }
                 */
                "application/json": components["schemas"]["RotateOrgAPIKeyRequest"];
            };
        };
        responses: {
            /** @description New key minted, predecessor in 'grace' (or 'revoked' if grace_window_days=0). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "key": {
                     *         "id": "0123456789abcdef0123456789abcdef",
                     *         "org_id": "8b1f5e5d-273e-5a18-ae00-58fceba4fe6c",
                     *         "prefix": "fp_live_ab",
                     *         "label": "ci-deploy-rotated",
                     *         "scopes": [
                     *           "apps:read",
                     *           "deploy:write"
                     *         ],
                     *         "created_at": "2026-08-02T11:25:00Z",
                     *         "status": "active",
                     *         "rotated_from_id": "fedcba9876543210fedcba9876543210"
                     *       },
                     *       "key_plaintext": "fp_live_NEW_PLAINTEXT_HERE",
                     *       "old_key_id": "fedcba9876543210fedcba9876543210",
                     *       "old_key_expires_at": "2026-08-09T11:25:00Z"
                     *     }
                     */
                    "application/json": components["schemas"]["RotateOrgAPIKeyResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            /** @description Key id not in org, or org slug not found, or predecessor already revoked. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    getGraceWindow: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Current override + plan default. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "days": 7,
                     *       "plan_default": 7
                     *     }
                     */
                    "application/json": components["schemas"]["GraceWindowResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    setGraceWindow: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                /**
                 * @example {
                 *       "days": 14
                 *     }
                 */
                "application/json": components["schemas"]["SetGraceWindowRequest"];
            };
        };
        responses: {
            /** @description Override applied. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "days": 14,
                     *       "plan_default": 7
                     *     }
                     */
                    "application/json": components["schemas"]["GraceWindowResponse"];
                };
            };
            /** @description Invalid body (e.g. negative days). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getAccountEgressAllowlistExtra: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Current extra + plan cap + global max extra. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "extra": 0,
                     *       "plan_cap": 16,
                     *       "max_extra": 1024
                     *     }
                     */
                    "application/json": components["schemas"]["AccountEgressAllowlistExtraResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    setAccountEgressAllowlistExtra: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                /**
                 * @example {
                 *       "extra": 8
                 *     }
                 */
                "application/json": components["schemas"]["SetAccountEgressAllowlistExtraRequest"];
            };
        };
        responses: {
            /** @description Override applied; the body echoes the new value. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "extra": 8,
                     *       "plan_cap": 16,
                     *       "max_extra": 1024
                     *     }
                     */
                    "application/json": components["schemas"]["AccountEgressAllowlistExtraResponse"];
                };
            };
            /** @description Invalid body (e.g. extra < 0 or extra > max_extra). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "type": "about:blank",
                     *       "title": "egress_allowlist_extra out of range",
                     *       "code": "account_egress_allowlist_extra_out_of_range",
                     *       "status": 400,
                     *       "detail": "extra=1025 is outside [0, 1024]; clear with extra=0 to fall back to the plan cap.",
                     *       "limit": 1024,
                     *       "observed": 1025
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listAuditEvents: {
        parameters: {
            query?: {
                /** @description Only return rows with `at >= since` (RFC 3339). Omit to read from the newest row. */
                since?: string;
                /** @description Only return rows whose `kind` starts with this prefix (e.g. `key.` returns `key.created` and `key.deleted`). */
                kind_prefix?: string;
                /** @description Max rows to return. Silently capped at 100. */
                limit?: number;
                /**
                 * @description Wave 0 PR-C / ADR-047: also surface rows with `subject =
                 *     NULL`. The defensive case where the customer's app row
                 *     was deleted between wake and the stateless-advisory
                 *     audit emit. Default `false` (the customer never sees
                 *     subject=NULL rows); operators can flip to `true` via
                 *     `?include_anonymous=true` for post-mortems.
                 */
                include_anonymous?: boolean;
                /**
                 * @description Wave 0 PR-C / ADR-047: filter the overscan window to
                 *     events whose `data.app_id` matches the given uuid. The
                 *     dashboard's `app_detail.html` "Stateless advisories"
                 *     link uses this combo with `kind_prefix=stateless.advisory`
                 *     to drill into a single app's advisories. Resolved
                 *     post-SQL on the bounded overscan window; the events
                 *     table is not indexed on `data`.
                 */
                app_id?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Newest-first audit events for the caller. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "limit": 50,
                     *       "events": [
                     *         {
                     *           "id": "42",
                     *           "at": "2026-07-25T13:25:00Z",
                     *           "actor": "apid",
                     *           "kind": "auth.login",
                     *           "subject": "0190f8e1-2a3b-7c4d-9e5f-6a7b8c9d0e1f",
                     *           "data": {
                     *             "method": "password"
                     *           }
                     *         },
                     *         {
                     *           "id": "41",
                     *           "at": "2026-07-25T11:00:00Z",
                     *           "actor": "apid",
                     *           "kind": "key.created",
                     *           "subject": "0190f8e1-2a3b-7c4d-9e5f-6a7b8c9d0e1f",
                     *           "data": {
                     *             "key_id": "0123456789abcdef0123456789abcdef",
                     *             "scopes": [
                     *               "admin"
                     *             ]
                     *           }
                     *         }
                     *       ]
                     *     }
                     */
                    "application/json": components["schemas"]["ListAuditEventsResponse"];
                };
            };
            /** @description Bad request — malformed `since` or `limit`. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getAuditEvent: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Event row id (bigint as string). */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The event. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "id": "42",
                     *       "at": "2026-07-25T13:25:00Z",
                     *       "actor": "apid",
                     *       "kind": "auth.login",
                     *       "subject": "0190f8e1-2a3b-7c4d-9e5f-6a7b8c9d0e1f",
                     *       "data": {
                     *         "method": "password"
                     *       }
                     *     }
                     */
                    "application/json": components["schemas"]["AuditEventResponse"];
                };
            };
            /** @description Bad request — id is not a positive integer. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listAuditLog: {
        parameters: {
            query?: {
                /** @description Audit-log rows with `received_at >= since` (RFC 3339) are returned. Omit to read from the newest row. */
                since?: string;
                /** @description Only return rows whose `kind` starts with this prefix (e.g. `account.` returns `account.deleted`). */
                kind_prefix?: string;
                /** @description Audit-log page size. Silently capped at 100. */
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Newest-first audit-log entries for the caller. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "limit": 50,
                     *       "entries": [
                     *         {
                     *           "id": "0190f8e1-2a3b-7c4d-9e5f-6a7b8c9d0e1f",
                     *           "kind": "account.deleted",
                     *           "account_id": "0190f8e1-2a3b-7c4d-9e5f-6a7b8c9d0e1f",
                     *           "account_email": "alice@example.com",
                     *           "actor": "grace-sweep",
                     *           "received_at": "2026-08-08T12:00:00Z",
                     *           "data": {
                     *             "source": "grace-sweep",
                     *             "email": "alice@example.com",
                     *             "actor": "grace-sweep"
                     *           }
                     *         }
                     *       ]
                     *     }
                     */
                    "application/json": components["schemas"]["ListAuditLogResponse"];
                };
            };
            /** @description Bad request — malformed `since`, `limit`, or `kind_prefix` (each must be RFC 3339 / a positive integer / a non-empty string when present). */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listAuditLogAll: {
        parameters: {
            query?: {
                /** @description Restrict the result to rows with this `account_id`. Omit to read across all accounts. */
                account_id?: string;
                /** @description Operator-side audit-log rows with `received_at >= since` (RFC 3339) are returned. Omit to read from the newest row. */
                since?: string;
                /** @description Only return rows whose `kind` starts with this prefix. */
                kind_prefix?: string;
                /** @description Operator-side audit-log page size. Silently capped at 100. */
                limit?: number;
                /** @description When `true`, also surface rows with `account_id IS NULL` (anonymous / background activity). Default `false`. */
                include_anonymous?: boolean;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Newest-first audit-log entries across accounts. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "limit": 50,
                     *       "entries": [
                     *         {
                     *           "id": "0190f8e1-2a3b-7c4d-9e5f-6a7b8c9d0e1f",
                     *           "kind": "account.deleted",
                     *           "account_id": "0190f8e1-2a3b-7c4d-9e5f-6a7b8c9d0e1f",
                     *           "account_email": "alice@example.com",
                     *           "actor": "grace-sweep",
                     *           "received_at": "2026-08-08T12:00:00Z",
                     *           "data": {
                     *             "source": "grace-sweep",
                     *             "email": "alice@example.com",
                     *             "actor": "grace-sweep"
                     *           }
                     *         },
                     *         {
                     *           "id": "0190f8e1-2a3b-7c4d-9e5f-6a7b8c9d0e20",
                     *           "kind": "system.health",
                     *           "received_at": "2026-08-08T11:55:00Z",
                     *           "data": {
                     *             "source": "healthcheck",
                     *             "status": "ok"
                     *           }
                     *         }
                     *       ]
                     *     }
                     */
                    "application/json": components["schemas"]["ListAuditLogResponse"];
                };
            };
            /** @description Bad request — malformed `since`, `limit`, or `account_id`. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listWakeTimeline: {
        parameters: {
            query?: {
                /** @description Only return rows with `at >= since` (RFC 3339). */
                since?: string;
                /** @description Max frames to return. Silently capped at 1000. */
                limit?: number;
            };
            header?: never;
            path: {
                /** @description App slug (lowercase, kebab-case; per-account unique). */
                slug: string;
                /**
                 * @description The per-wake correlation handle minted by the schedd
                 *     engine (UUID v4 in production). The endpoint returns
                 *     every `wake.*` events row whose `data.wake_id`
                 *     matches — the partial index `events_wake_id_idx`
                 *     (migrations/00113) serves the read in O(frames)
                 *     regardless of the events table size.
                 */
                wake_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Wake-timeline frames. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "wake_id": "abcdef01-2345-6789-abcd-ef0123456789",
                     *       "app_id": "0123456789abcdef0123456789abcdef",
                     *       "limit": 200,
                     *       "next_cursor": "",
                     *       "events": [
                     *         {
                     *           "at": "2026-07-25T13:25:00.123Z",
                     *           "kind": "wake.queue_accepted",
                     *           "actor": "schedd",
                     *           "data": {
                     *             "wake_id": "abcdef01-2345-6789-abcd-ef0123456789",
                     *             "app_id": "0123456789abcdef0123456789abcdef",
                     *             "request_id": "r-1",
                     *             "queue_wait_ms": 12
                     *           }
                     *         },
                     *         {
                     *           "at": "2026-07-25T13:25:00.135Z",
                     *           "kind": "wake.admitted",
                     *           "actor": "schedd",
                     *           "data": {
                     *             "wake_id": "abcdef01-2345-6789-abcd-ef0123456789",
                     *             "app_id": "0123456789abcdef0123456789abcdef",
                     *             "request_id": "r-1",
                     *             "account_id": "fdecba98-7654-3210-fedc-ba9876543210",
                     *             "plan": "pro"
                     *           }
                     *         },
                     *         {
                     *           "at": "2026-07-25T13:25:00.420Z",
                     *           "kind": "wake.boot_started",
                     *           "actor": "schedd",
                     *           "data": {
                     *             "wake_id": "abcdef01-2345-6789-abcd-ef0123456789",
                     *             "app_id": "0123456789abcdef0123456789abcdef",
                     *             "instance_id": "inst-1",
                     *             "node_id": "node-1",
                     *             "method": "restore"
                     *           }
                     *         },
                     *         {
                     *           "at": "2026-07-25T13:25:01.180Z",
                     *           "kind": "wake.readiness_200",
                     *           "actor": "vmmd",
                     *           "data": {
                     *             "wake_id": "abcdef01-2345-6789-abcd-ef0123456789",
                     *             "app_id": "0123456789abcdef0123456789abcdef",
                     *             "instance_id": "inst-1",
                     *             "node_id": "node-1",
                     *             "healthcheck_path": "/healthz",
                     *             "probe_count": 1,
                     *             "elapsed_ms": 760
                     *           }
                     *         },
                     *         {
                     *           "at": "2026-07-25T13:25:01.220Z",
                     *           "kind": "wake.proxy_first_byte",
                     *           "actor": "egress",
                     *           "data": {
                     *             "wake_id": "abcdef01-2345-6789-abcd-ef0123456789",
                     *             "app_id": "0123456789abcdef0123456789abcdef",
                     *             "request_id": "r-1",
                     *             "instance_id": "inst-1",
                     *             "node_id": "node-1",
                     *             "latency_ms": 40
                     *           }
                     *         }
                     *       ]
                     *     }
                     */
                    "application/json": components["schemas"]["WakeTimelineResponse"];
                };
            };
            /** @description Malformed query parameter on the wake-timeline read — `since` not RFC 3339 or `limit` out of range. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description No such app (slug) or wake_id is unknown. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    getUsage: {
        parameters: {
            query?: {
                /** @description Billing month in `YYYY-MM` form (UTC). Defaults to the current month. */
                month?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Per-app usage rows for the month. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example [
                     *       {
                     *         "month": "2026-07",
                     *         "app_id": "0123456789abcdef0123456789abcdef",
                     *         "mb_seconds": 1234,
                     *         "requests": 42,
                     *         "included_gb_hours": 50,
                     *         "used_gb_hours": 1.234,
                     *         "next_before": null
                     *       }
                     *     ]
                     */
                    "application/json": components["schemas"]["UsageResponse"][];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    usageSummary: {
        parameters: {
            query?: {
                /** @description Billing month in `YYYY-MM` form (UTC). Defaults to the current month for the roll-up. */
                month?: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The monthly roll-up with overage math. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["UsageSummaryResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    usageDaily: {
        parameters: {
            query: {
                /** @description Calendar day in `YYYY-MM-DD` form (UTC). Required. */
                day: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Per-app daily rollup rows for the requested day. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "items": [
                     *         {
                     *           "app_id": "0123456789abcdef0123456789abcdef",
                     *           "day": "2026-07-29",
                     *           "mb_seconds": 1234,
                     *           "requests": 42,
                     *           "cpu_usec": 5678,
                     *           "tx_bytes": 12345,
                     *           "net_tx_bytes": 23456,
                     *           "net_rx_bytes": 34567,
                     *           "cold_boots": 1,
                     *           "builder_seconds": 12
                     *         }
                     *       ]
                     *     }
                     */
                    "application/json": components["schemas"]["DailyUsageListResponse"];
                };
            };
            /** @description Missing or malformed `day` query parameter. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    usageStorage: {
        parameters: {
            query: {
                /** @description Calendar day in `YYYY-MM-DD` form (UTC). Required for the storage rollup. */
                day: string;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Per-app storage rollup rows for the requested day. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "items": [
                     *         {
                     *           "app_id": "0123456789abcdef0123456789abcdef",
                     *           "day": "2026-07-29",
                     *           "snapshot_bytes": 130000000,
                     *           "layer_bytes": 256000000
                     *         }
                     *       ]
                     *     }
                     */
                    "application/json": components["schemas"]["StorageUsageListResponse"];
                };
            };
            /** @description Missing or malformed `day` query parameter on the storage rollup surface. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listInvoices: {
        parameters: {
            query?: {
                /** @description Optional `YYYY-MM` filter (UTC half-open range on period_end). */
                month?: string;
                /** @description Cursor (RFC3339Nano) for the next older page. Omit for the first page. */
                before?: string;
                /** @description Page size; server clamps to 1..100. */
                limit?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description One page of invoices, newest first. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "items": [
                     *         {
                     *           "id": "9f1d8e7c-6b5a-4d3c-2b1a-0fedcba98765",
                     *           "provider": "stripe",
                     *           "provider_invoice_id": "in_1NxAbC2eZvKYlo2C0D4E5F6G",
                     *           "number": "FaaS-2026-007",
                     *           "status": "paid",
                     *           "period_start": "2026-07-01T00:00:00Z",
                     *           "period_end": "2026-07-31T23:59:59Z",
                     *           "subtotal_cents": 900,
                     *           "tax_cents": 0,
                     *           "total_cents": 900,
                     *           "amount_paid_cents": 900,
                     *           "currency": "eur",
                     *           "pdf_available": true,
                     *           "created_at": "2026-08-01T01:23:45Z"
                     *         }
                     *       ],
                     *       "next_before": null
                     *     }
                     */
                    "application/json": components["schemas"]["InvoiceListResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getBillingPortal: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Portal URL + payment-method summary (any field may be empty when the box has no portal configured). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "url": "https://billing.example.com/portal?account=9f1d8e7c-6b5a-4d3c-2b1a-0fedcba98765",
                     *       "payment_method": {
                     *         "brand": "visa",
                     *         "last4": "4242",
                     *         "exp_month": 12,
                     *         "exp_year": 2027
                     *       }
                     *     }
                     */
                    "application/json": components["schemas"]["BillingPortalResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    retryLatestCharge: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description New charge attempt created. The CLI prints attempt + provider reference IDs. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "attempt_id": "in_1abcd",
                     *       "provider_ref_id": "pi_1abcd",
                     *       "status": "pending_provider_confirmation",
                     *       "next_billing_at": null
                     *     }
                     */
                    "application/json": components["schemas"]["BillingRetryResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description No open charge to retry — the account is in good standing, or the operator has not configured a billing provider. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Provider-side failure (Stripe API error / Paddle timeout). The CLI surfaces this as 'retry failed'. */
            502: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    cancelAtPeriodEnd: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Cancellation scheduled; account remains on the plan until period end. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "cancel_scheduled": true,
                     *       "effective_at": "2026-09-08T00:00:00Z"
                     *     }
                     */
                    "application/json": components["schemas"]["BillingCancelResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description Already cancelled. CLI renders a friendly hint. */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Provider-side failure. */
            502: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    issueAccountCredit: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Target account UUID. */
                id: string;
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": {
                    /** @description Credit amount in EUR cents (integer). */
                    cents: number;
                    /** @description Operator-supplied audit reason. */
                    reason: string;
                };
            };
        };
        responses: {
            /** @description Credit issued. Returns the new credit row. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AccountCreditResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            /** @description code: admin_required — call requires a Bearer with the admin scope AND an email in FAAS_ADMIN_EMAILS. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    setGithubWebhookSecret: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AdminSetGithubWebhookSecretRequest"];
            };
        };
        responses: {
            /** @description Secret persisted. The new upgraded_at/upgraded_by are returned for the audit trail. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AdminSetGithubWebhookSecretResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            /** @description Code admin_required — admin-scoped Bearer + email in FAAS_ADMIN_EMAILS allowlist. PR-D widens the scope to cover per-tenant webhook secret rotation; the table-level pg_notify side-effect (installation_id) is intentional and consumed by githubd. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    consumeInvoiceCredits: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Invoice row UUID from GET /v1/invoices. */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Reducer ran (or replayed). consumed_cents is the floored integer cents drained against this invoice. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ConsumeInvoiceResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            /** @description code: admin_required — call requires an admin-scoped Bearer with the caller email in FAAS_ADMIN_EMAILS AND a verified MFA factor. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listPaddleCatalog: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Catalog snapshot. Entries may be empty if no hydration has run. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BillingCatalogResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description code: admin_required — GET /v1/admin/billing-paddle-catalog requires a Bearer with the admin scope AND an email in FAAS_ADMIN_EMAILS. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
            /** @description code: billing_op_unsupported — active provider does not implement the paddle.OpProvider surface. */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    resetPaddleCatalog: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Reset signal recorded. Always returns empty entries. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BillingCatalogResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description code: admin_required — DELETE /v1/admin/billing-paddle-catalog requires a Bearer with the admin scope AND an email in FAAS_ADMIN_EMAILS. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
            /** @description code: billing_op_unsupported — provider does not implement paddle.OpProvider. */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    syncPaddleCatalog: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Post-sync catalog. synced_at is "now" by construction. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BillingCatalogResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description code: admin_required — POST /v1/admin/billing-paddle-catalog/sync requires a Bearer with the admin scope AND an email in FAAS_ADMIN_EMAILS. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
            /** @description code: billing_op_unsupported — active provider does not advertise paddle.OpProvider; the type assertion at the dispatcher fails. */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description code: billing_sync_failed — Paddle SDK round-trip failed. */
            502: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    getBillingPaddleOveragePreflight: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Schema probe result. Booleans reflect the current DB shape. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BillingPaddleOveragePreflightResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description code: admin_required — GET /v1/admin/billing-paddle-overage/preflight requires a Bearer with the admin scope AND an email in FAAS_ADMIN_EMAILS. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    reconcileAccount: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Account UUID whose 30-day reconcile window the operator wants to inspect. */
                id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Reconcile ran. mb_seconds is the SDK-returned total for [start, end). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["BillingReconcileResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            /** @description code: admin_required — POST /v1/admin/billing-reconcile/{id} requires a Bearer with the admin scope AND an email in FAAS_ADMIN_EMAILS. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
            /** @description code: billing_reconcile_unsupported — provider does not implement ReconcileUsage. */
            501: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description code: billing_reconcile_failed — SDK round-trip failed. */
            502: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    getRekeyProgress: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Current rekey progress snapshot. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RekeyProgress"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description code: admin_required — GET /v1/admin/secrets/rekey-progress requires a Bearer with the admin scope AND an email in FAAS_ADMIN_EMAILS. */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
            /**
             * @description Runner not running. The detail distinguishes two cases
             *     via the `code` field:
             *       - `rekey_disabled` — FAAS_REKEY_ENABLED is unset on
             *         this apid; the background re-seal runner is not
             *         running. Set the env flag and restart apid to opt in.
             *       - `rekey_no_identities` — FAAS_REKEY_ENABLED=true but
             *         no host age identities loaded (FAAS_HOST_AGE_IDENTITY_PATH
             *         missing or empty). Set the identity path alongside
             *         the flag, then restart apid.
             */
            503: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    listOrgs: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The org list (may contain at most 1 personal + N shared). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "orgs": [
                     *         {
                     *           "id": "0123456789abcdef0123456789abcdef",
                     *           "slug": "acme",
                     *           "name": "Acme Inc.",
                     *           "personal": false,
                     *           "plan": "pro",
                     *           "status": "active",
                     *           "created_at": "2026-08-02T11:25:00Z",
                     *           "updated_at": "2026-08-02T11:25:00Z"
                     *         },
                     *         {
                     *           "id": "abcdef0123456789abcdef0123456789",
                     *           "slug": "u-1a2b3c4d5e6f",
                     *           "name": "Personal",
                     *           "personal": true,
                     *           "plan": "free",
                     *           "status": "active",
                     *           "created_at": "2026-06-15T08:00:00Z",
                     *           "updated_at": "2026-06-15T08:00:00Z"
                     *         }
                     *       ]
                     *     }
                     */
                    "application/json": components["schemas"]["OrgListResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    createOrg: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                /**
                 * @example {
                 *       "slug": "acme",
                 *       "name": "Acme Inc."
                 *     }
                 */
                "application/json": components["schemas"]["CreateOrgRequest"];
            };
        };
        responses: {
            /** @description The new shared org. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrgResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            /**
             * @description `409 Conflict` — slug already taken (`org_slug_taken`) or the
             *     caller is already a member of an org with the same slug
             *     (`org_already_member`).
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description `422 Unprocessable Entity` — slug violates the `OrgSlugPattern`
             *     regex (lowercase alphanumeric + dashes, 3..32 chars).
             *     Stable code `org_slug_invalid`.
             */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    getOrg: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The org + the caller's role. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrgResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /**
             * @description `403 Forbidden` — caller is authenticated but lacks the
             *     `org.view` action on the target org. Stable code
             *     `org_role_forbidden`.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    deleteOrg: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Org soft-deleted. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            /**
             * @description `403 Forbidden` — caller is authenticated but lacks the
             *     `org.delete` action. Owner-only. Stable code
             *     `org_role_forbidden`.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            /**
             * @description `409 Conflict` — the target org is the caller's personal
             *     org (created by the PR 3 backfill); personal orgs are immutable on PATCH (name/plan).
             *     immutable and cannot be modified by PATCH. Stable code
             *     `org_personal_immutable`.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    updateOrg: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PatchOrgRequest"];
            };
        };
        responses: {
            /** @description The updated org. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrgResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            /**
             * @description `403 Forbidden` — caller lacks `org.manage_billing` (name)
             *     or `org.change_plan` (plan), OR the resulting member count
             *     would exceed the plan cap (`OrgMembersMax`).
             *     Stable codes `org_role_forbidden` | `org_member_cap_exceeded`.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            /**
             * @description `409 Conflict` — the target org is the caller's personal
             *     org (created by the PR 3 backfill); personal orgs cannot be soft-deleted.
             *     immutable and cannot be modified by PATCH. Stable code
             *     `org_personal_immutable`.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description `422 Unprocessable Entity` — slug violates the
             *     `OrgSlugPattern` regex (lowercase alphanumeric + dashes,
             *     3..32 chars). Stable code `org_slug_invalid`.
             */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    listOrgMembers: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The active member list. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MemberListResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /**
             * @description `403 Forbidden` — caller is authenticated but lacks the
             *     `org.view` action. Stable code `org_role_forbidden`.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    addOrgMember: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["InviteMemberRequest"];
            };
        };
        responses: {
            /** @description The invitation + the one-time plaintext token. */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InvitationWithTokenResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            /**
             * @description `403 Forbidden` — caller lacks `org.invite_members`, OR
             *     the resulting pending-invitation count would exceed the
             *     plan cap (`OrgPendingInvitationsMax`).
             *     Stable codes `org_role_forbidden` | `org_invitation_cap_exceeded`.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description `409 Conflict` — the target org is the caller's personal
             *     org (created by the PR 3 backfill); personal orgs are immutable on POST /members (invite).
             *     immutable and cannot be modified by PATCH. Stable code
             *     `org_personal_immutable`.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    removeOrgMember: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
                /**
                 * @description Account UUID (the membership row's account_id). The
                 *     path segment is named `user_id` for backwards
                 *     compatibility with the dashboard's existing
                 *     /members/:user_id routes.
                 */
                user_id: components["parameters"]["OrgAccountID"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Member removed. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            /**
             * @description `403 Forbidden` — caller is authenticated but lacks the
             *     `org.remove_members` action. Owner-only. Self-removal
             *     is refused at the boundary. Stable code
             *     `org_role_forbidden`.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description `404 Not Found` — caller is authenticated but the
             *     target slug is unknown, OR `{user_id}` does not
             *     correspond to an active member, OR the org belongs
             *     to another tenant (IDOR-safe cross-tenant collapse).
             *     Stable codes `org_role_forbidden` | `NotFound`.
             */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description `409 Conflict` — caller attempted to remove (DELETE) the only
             *     the only remaining active owner of the org. The
             *     exactly-one-owner invariant lives on the
             *     `org_memberships_one_owner_idx` partial unique index
             *     (migration 00099). Stable code `org_last_owner`.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    updateOrgMemberRole: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
                /**
                 * @description Account UUID (the membership row's account_id). The
                 *     path segment is named `user_id` for backwards
                 *     compatibility with the dashboard's existing
                 *     /members/:user_id routes.
                 */
                user_id: components["parameters"]["OrgAccountID"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ChangeMemberRoleRequest"];
            };
        };
        responses: {
            /** @description The updated member row. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrgMemberResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            /**
             * @description `403 Forbidden` — caller is authenticated but lacks the
             *     `org.change_role` action. Owner-only. Stable code
             *     `org_role_forbidden`.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description `404 Not Found` — caller is authenticated but the
             *     `{user_id}` path param does not correspond to any active
             *     member of the target org, OR the target org belongs to
             *     another tenant (IDOR-safe cross-tenant collapse).
             *     Stable code `org_role_forbidden`.
             */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description `409 Conflict` — caller attempted to demote or remove
             *     the only remaining active owner of the org. The
             *     exactly-one-owner invariant lives on the
             *     `org_memberships_one_owner_idx` partial unique index
             *     (migration 00099). Stable code `org_last_owner`.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    transferOrgOwnership: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["TransferOwnershipRequest"];
            };
        };
        responses: {
            /** @description The org (post-transfer). */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrgResponse"];
                };
            };
            400: components["responses"]["ValidationFailed"];
            401: components["responses"]["Unauthorized"];
            /**
             * @description `403 Forbidden` — caller is authenticated but is not the
             *     active owner of the target org. Stable code
             *     `org_role_forbidden`.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description code: NotFound (new owner is not a member) */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description code: org_last_owner (caller not the active owner OR new owner already owner) */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    peekInvitation: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Plaintext or base64url-encoded invitation token. Tokens are
                 *     returned ONCE on the create call (`POST /v1/orgs/{slug}/members`)
                 *     and never re-served.
                 */
                token: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The pending invitation. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrgInvitationResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /** @description code: org_invitation_invalid | org_invitation_expired | org_invitation_invalid (already consumed/revoked) */
            410: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    acceptInvitation: {
        parameters: {
            query?: never;
            header?: {
                /**
                 * @description Idempotency key for the POST. Stored for 24h. On replay the server
                 *     returns the original response with `Idempotent-Replayed: true`.
                 */
                "Idempotency-Key"?: components["parameters"]["IdempotencyKey"];
            };
            path: {
                /**
                 * @description Plaintext or base64url-encoded invitation token (same
                 *     shape as the GET /v1/invitations/{token} parameter).
                 */
                token: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The new membership row. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["OrgMemberResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /**
             * @description `403 Forbidden` — the org is at the member cap. Stable
             *     code `org_member_cap_exceeded`.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description `409 Conflict` — the bearer is already an active member
             *     of the target org. The wire detail carries the
             *     caller's current role. Stable code `org_already_member`.
             */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description `410 Gone` — the token is unknown, already consumed,
             *     already revoked, or expired. Stable codes
             *     `org_invitation_invalid` | `org_invitation_expired`.
             */
            410: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    revokeInvitation: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
                /**
                 * @description Plaintext or base64url-encoded invitation token. The URL
                 *     is org-scoped here (vs the no-org-scoped peek at
                 *     `GET /v1/invitations/{token}`), so admin/owner gate fires
                 *     before the consume path resolves the hash.
                 */
                token: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Revoked. */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            401: components["responses"]["Unauthorized"];
            /**
             * @description `403 Forbidden` — caller lacks `org.invite_members`.
             *     Stable code `org_role_forbidden`.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            /**
             * @description `410 Gone` — the token is unknown, already consumed,
             *     or already revoked. Stable code `org_invitation_invalid`.
             */
            410: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            429: components["responses"]["TooManyRequests"];
        };
    };
    getOrgSeatUsage: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The seat usage. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SeatUsageResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /**
             * @description `403 Forbidden` — caller lacks `org.view`. Stable code
             *     `org_role_forbidden`.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    listOrgInvitations: {
        parameters: {
            query?: {
                /**
                 * @description Opaque cursor for the row immediately preceding the
                 *     next page. Base64-url encoding of a JSON object
                 *     `{"created_at":"...","id":"..."}` (the compound key,
                 *     encoded by the server in `next_before`). Pass the
                 *     prior response's `next_before` unchanged. Omit on
                 *     the first page. Malformed cursors return 400 with
                 *     the stable code `invalid_cursor` (PR-9).
                 */
                before?: string;
                /** @description Max invitations to return per page. Silently capped at 100. */
                limit?: number;
            };
            header?: never;
            path: {
                /**
                 * @description Org slug. Lowercase letters, digits, hyphens; must start
                 *     and end with alnum. 3..32 chars. Mirrors `OrgSlugPattern`
                 *     in `pkg/api/errors.go` exactly so the spec drift gate
                 *     (`make spec-check`) stays green.
                 */
                slug: components["parameters"]["OrgSlug"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The page of invitations. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InvitationListResponse"];
                };
            };
            /**
             * @description `400 Bad Request` — `?limit=` is malformed, < 1, or
             *     > 100. Stable code `validation_failed`.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            401: components["responses"]["Unauthorized"];
            /**
             * @description `403 Forbidden` — caller is not a member of the
             *     active org. Stable code `org_role_forbidden`.
             */
            403: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            404: components["responses"]["NotFound"];
            429: components["responses"]["TooManyRequests"];
        };
    };
    getOpenAPISpec: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The OpenAPI 3.1 document for this API. */
            200: {
                headers: {
                    "Cache-Control"?: string;
                    [name: string]: unknown;
                };
                content: {
                    "application/yaml": string;
                };
            };
        };
    };
    getOpenAPISpecJSON: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The OpenAPI 3.1 document for this API, as JSON. */
            200: {
                headers: {
                    "Cache-Control"?: string;
                    [name: string]: unknown;
                };
                content: {
                    "application/json": Record<string, never>;
                };
            };
        };
    };
    getTrace: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description 32-hex W3C trace_id (or wake_id UUIDv7 hex form). */
                trace_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The trace tree. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Trace"];
                };
            };
            /**
             * @description Missing or wrong observer token. The endpoint is operator-
             *     gated; the customer-facing dashboard session does not
             *     grant access.
             */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /**
             * @description Trace not in the ring (never seen, or evicted by the 24h
             *     retention sweep / LRU cap).
             */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
}
